# 開発ログ 2026-06-11 ── 観測層（可視化・第1層）／ブランチ整理＆マージ／参考EPIC量産

Core 純ロジックの急増に「盤面で何も見えない」乖離を構造的に潰すための**観測層（read-only 可視化）**を入れた日。あわせて `claude/ccx-1-parallel-fanout` を整理して master へ統合し、roadmap を最新化。TestHarness は **1037 緑**。

## 1. 観測層 第1層＝可視化（read-only）
> 狙い＝「Core はテストで大量に着地するが盤面で何も起きない/見えない」乖離を**生成と観測を歩調させる**ことで潰す。第1層は**眺める窓**に徹し状態は一切変えない。第2層「操作化」（プレイヤーがレバーを回す）はここから手で昇格させる。

- **`CampaignObserverOverlay`（G キー）**：手仕上げのヒーロー表示。`StrategySession.Campaign` 配下の全 `FactionState`（王朝/統治体/組織/共同体＋包摂度/税/国庫＋合成の総合・実効安定度）をバー付きでライブダンプ。崩壊中は赤・末人/組織崩壊を明示。Strategy/Battle 両シーン自動生成。
- **`CoreStateInspector`（J キー）**：型非依存の**汎用版**。登録ルートをリフレクションで再帰ダンプ（float 0..1=バー/数値/真偽/列挙/リスト/辞書/入れ子・Ginei名前空間のみ展開・深さ/件数/循環ガード）＋**用語集**でフィールド名に日本語説明を併記。既定ルート＝`StrategySession.Campaign`/`Provinces`/`Clock`。
- **規約（CLAUDE.md「観測層」節に明文化）**：新 Core state を実装したら ―
  1. 既存ルートから到達できない**独立した新ルート**を作ったときだけ `CoreStateInspector.Register("ラベル", () => 対象)` を1行足す（既存ルート配下なら再帰で自動表示＝追加不要）。
  2. 新フィールドを足したら `glossary` に `{ "名", "説明" }` を1行足す。
  - 自動コード化ルーチンも Core state 型を生やすたびにこの2点を同時に行う＝**観測の自動追従**。
- 入力（G/J）は `GameInput`（#107）の `観測オーバーレイ切替`/`状態インスペクタ切替` に集約・HelpOverlay 掲載。

## 2. ブランチ整理＆ master 統合（CCX-1）
- **コミット漏れの `.meta` 追補**：観測層2機能（`CampaignObserverOverlay`/`CoreStateInspector`）＋追加テスト3本（Commerce/Disclosure/Supply Extra）の `.cs.meta` が、本体 `.cs` はコミット済なのに**メタだけ欠落**＝他環境で Import エラーになる状態を解消（`695947e`）。
- **破壊的な未コミット差分を revert**：
  - `JapaneseFont_TMP.asset`＝フォントアトラス（MS Gothic 1024×1024・1MB字形）を**丸ごと削除**する差分が working tree に出ていた（06-10 にグリフ追加で成長させたものの逆）。コミットすれば日本語表示が壊れるため revert。
  - `GineiPanelSetting.asset`＝実質 CRLF のみの差分 → revert でノイズ除去。
- **迷子 zip**：`Assets/Scripts.zip`（gitignore済）由来の `.zip.meta` を `.gitignore`（`/Assets/**/*.zip.meta`）へ。
- **統合**：`origin/master` 先行8コミット（CI/ワークフロー）と当ブランチ（docs＋観測層）は**ファイル重複なし＝コンフリクトなし**を確認。PR #1144 を作成（master への自己マージは要・作者承認のため最終マージは保留）。

## 3. 参考 EPIC 量産（`/worldview-epic` パイプライン）
- 自動バックログ補充ルーチン（6時間ごと・閾値16冊・最大20冊・質＞量）が稼働。今セッションで設計起票：
  - 軍事：**孫子 #1125**（欺瞞/用間/現地調達/攻勢終末点）・**クラウゼヴィッツ #1132**（摩擦/政戦連接/三位一体/重心）
  - 政治/戦略：**マキャヴェリ #1138**（征服統治三様/恐怖と憎悪/直言参謀）・**三国志演義 #1102**（多極均衡/献策/盟誓/離間）
  - 経済/基盤：**化学SCM #1109**（連産品/スプレッド/ブルウィップ）・**終盤スケーラビリティ #1117**（ステラリス反面教師の性能規律）
- これらは**コード実装でなく設計の在庫**＝開示エンジン/純ロジックへの入力。オープン Issue は 949件（EPIC 146件）に。

## 検証
- TestHarness（`dotnet test`）：**1037 / 1037 緑**。
- 観測オーバーレイ（G/J）の見た目・自動生成は実機 Play 目視が要（入力注入が環境制約のため）。

## ドキュメント
- `docs/roadmap.md`：日付 06-11・テスト 1037・Issue 949（EPIC146）・観測層 第1層・CCX-1/6・新設参考EPIC群を反映。
- CLAUDE.md：観測層節＋コンポーネント表（`CampaignObserverOverlay`/`CoreStateInspector`）は反映済み。
