# 仕様1 最終実機QAの過渡状態記録
Task: fleet-formation-hold-observation-20260910
Issue: https://github.com/yamamax55/ginei-game/issues/2253

Unity Play OFFをChatGPTが確認して依頼。プログラム担当はClaude。仕様2未開始。既存の対象17/17＋追加3件個別合格を反復しない。

今回の観測:
- input-review revision3のBattleViewport仮説は今回成立せず両関門通過。PauseButtonの自動クリックは通常EventSystem経由で再開→停止成功。
- メニュー位置で押下が残り約33秒後の解放で2隊が矩形選択された。記録 fleet-formation-input-trace-20260910.txt。入力器はUpdate観測なので「記録なし＝OSから未着」等の断定を修正すること。
- Claude編集停止を画面で再確認後、空白クリックで選択解除→自軍2を1隊選択→円陣保持・直接命令→通常攻撃をQA敵1へ発令する導線が成功。再開後、QA敵1/敵2が撃沈されBattleManager.CheckVictoryからStrategyへ自動帰還。標的消失直後の保持を読み取る前に終了したため未合格。
- SinkAttackTargetは支援専用状態を探すのでなくEnemyCorpsの生存先頭1隊へ通常TakeDamageを通す。既存補助の利用で十分なら増やさない。標的と撃沈対象が一致することを必ず確認。

依頼はQA用の観測だけ。製品の陣形/命令/AI挙動は変えない:
1. 既存QAを再利用し、通常UIからの命令実行時に、短い状態変化をシーン帰還後も読み返せる有限の記録をEditor限定で追加してください。開始/停止/出力を明示。結果を強制設定しない。静的保持やイベント購読の後始末、次のセッションへの混入防止、ログ件数上限を用意。
2. 対象QA艦隊の安定した識別子、フレーム/時刻、保持/指定元/陣形、攻撃標的の識別子と有無、override、敗走/士気/生存/所属、該当通知を記録。陣形指定、支援受諾、標的喪失、敗走開始/解除/会戦終了などの変化時に前後のスキルPも照合できるようにする。自然回復によるログ洪水は避ける。記録時点/観測の限界を明記し、記録できない遷移を完了扱いにしない。
3. 残QA3項目（自然敗走継続、支援受諾通知と費用、攻撃完了/標的喪失後保持）を既存メニュー＋通常UIで有限に検証する手順を提示。自然敗走は非交戦時即回復の既知問題を踏まえ、交戦中なら必ず継続すると断定しない。
4. InputDiagnosticsの断定的な読み方だけを正す（Update記録なしはOS未着の証明ではない、フレーム差だけでドラッグ確定ではない）。新たな製品入力変更は不要。

大規模な仕組みを足さず、既存記録/通知/QAの再利用を優先。セーブ/v5/無関係変更保護、commit/pushしない。作業受理時claude-status.jsonをこのtask_id/revision1で更新。完了後に変更ファイルと検証証跡と残件を結果文書へ記録しreview_ready/editing_stopped=true。ChatGPTがその後に実機検証する。
