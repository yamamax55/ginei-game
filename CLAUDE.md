# CLAUDE.md — 銀英伝風 戦術艦隊戦（プロジェクトメモリ）

> Claude Code が毎セッション開始時に自動で読み込む。プロジェクトルート（`Assets/` や `ProjectSettings/` のある階層）に置く。
> これは「プロジェクトの最上位ルール」。プロンプトと衝突したら、このファイルが優先。

## プロジェクト概要
- Unity 6.4（6000.4.x） / 2D (URP)。
- ジャンル：PS版『銀河英雄伝説』風の戦術艦隊戦（リアルタイム会戦）。将来は戦略レイヤー・複数勢力（諸侯型）へ拡張予定。
- 名前空間：全クラス `namespace Ginei`。
- 入力：新 Input System（`UnityEngine.InputSystem`）で統一。旧 `Input.xxx` は使わない。
- UI：uGUI + TextMeshPro。
- シーン遷移を伴う一連の流れ（タイトル→会戦→結果）を持つ。提督・シナリオはデータ（ScriptableObject）駆動。

## 絶対規約（必ず守る）
- 2D。座標は XY 平面、回転は Z 軸のみ。
- 艦の正面は `Transform.up`。
- 調整値は `public` にし、`[Header]` / `[Tooltip]` を付ける。
- 移動・回転・タイマーはフレームレート非依存（`Time.deltaTime`）。timeScale に追従させる（ポーズ=0／倍速対応）。
- コメントは日本語で簡潔に。
- 既存スクリプトを壊さない。指示外のファイルを大きく書き換えない。大きな変更は段階的に。
- **実効値パターン**：能力を一時的に上下させるときは基準値（publicフィールド）を上書きせず、ローカル変数で実効値を計算する。現状の実装例：
  - `FleetMovement.GetMobilityFactor()`（提督機動・士気・交戦状態を掛け合わせて実効速度/回頭速度を出す）。
  - `FleetMorale.GetMoraleFactor()`（士気から能力補正倍率を返す。0.5〜1.0）。
  - `FleetWeapon.PerformAttack()`（提督攻撃・士気・側背面倍率を掛けて最終ダメージを算出）。
  - `FleetStrength.TakeDamage()`（提督防御で被ダメージを軽減。基準ダメージは変えない）。

## コーディング規約
- 1ファイル1クラス、ファイル名=クラス名。命名：型/メソッド/public定数=PascalCase、フィールド=camelCase。
- 依存コンポーネントは `[RequireComponent]` で明示（`FleetWeapon`→`WeaponArc`、`FleetAI`→`FleetMovement`/`FleetWeapon`/`FleetStrength`、`FactionColor`→`FleetStrength`、`FleetMorale`→`FleetStrength`）。
- 他コンポーネント参照は Awake/Start でキャッシュし、使用前に null チェック。
- マジックナンバー禁止（`public` か `const`）。
- データ（提督・シナリオ・勢力など）は ScriptableObject に分離する方針。
- 実行時生成した `Material` は `OnDestroy` で `Destroy` してリークを防ぐ（`WeaponArc`/`FleetWeapon`/`DamagePopup` が踏襲）。

## シーン構成とゲームフロー
- シーンは3つ：**Title** → **Battle** → **Result**（シーン名はこの文字列。`SceneLoader.LoadScene(name)` で遷移）。
- 起動〜会戦の流れ：
  1. `TitleManager.StartBattle()`（「会戦開始」ボタン）は**シナリオ選択画面を開くだけ**。選択画面の「この設定で会戦開始」＝`BeginBattle()` が現在の設定を `SaveManager.Save()` し、`GameSettings.ResetStats()` 後に `SceneLoader.LoadScene("Battle")`。
  2. Battle シーンで `BattleSetup`（`[DefaultExecutionOrder(-100)]`、Awake）が `ScenarioData` を解決し、手置き艦隊をクリアしてから `fleetPrefab` を生成・配置。**Battle シーン以外（Title 等）では Awake で即 return し何もしない**（誤配置で戦闘が始まらないようガード）。
  3. `BattleManager`（Start）が開始時の隻数を記録し、`checkInterval`（1秒）ごとに全滅判定。決着で `Time.timeScale=0` → 戦績を `GameSettings` に記録 → `SceneLoader.LoadScene("Result")`。
  4. `ResultManager` が `GameSettings` の戦績を表示、`BackToTitle()` で Title へ。
- `TitleManager.ContinueBattle()` は `SaveManager.Load()` で直近セットアップを復元して会戦へ。`continueButton` は `SaveManager.HasSave()` の時だけ活性。

## シングルトン（`Instance` ＋ `DontDestroyOnLoad`）
シーンを跨いで生きる。`Instance` ゲッターが無ければ自動生成する（手置き不要）。重複させない。
| クラス | 役割 | 主なフィールド/API |
|---|---|---|
| `GameSettings` | 設定と戦績の保管庫（シーン間共有の唯一の窓口） | `playerFaction`/`scenarioName`/`selectedAdmiral`/`masterVolume`/`defaultTimeScale`/`alwaysShowGizmos`/`cameraStartZoom`(会戦開始ズーム、`CameraController` が Start で参照)、戦績 `winner`/`imperialSunkCount`/`allianceSunkCount`/`remainingStrength`（旧2勢力・後方互換）＋**`factionStats`(`List<FactionStat>`＝勢力名キーの撃沈/残存。多勢力対応)**、`ResetStats()`。システム設定(`masterVolume`/`defaultTimeScale`/`alwaysShowGizmos`/`cameraStartZoom`)は `LoadPrefs()`/`SavePrefs()` で **PlayerPrefs に永続化**(Awake で復元、設定画面の「戻る」で保存) |
| `AudioManager` | BGM/SE の一元管理 | `PlayBGM`/`StopBGM`/`PlaySE`/`PlayBeam`/`PlayHit`/`PlayExplosion`/`PlayUIClick`。音量は `bgmVolume`/`seVolume`×`GameSettings.masterVolume`。**SEはボイスプール(`seVoiceCount`)で重ね発音**。**`PlayBeam` は多数艦の同時発砲で氾濫しないよう実時間で間引き(`beamMinInterval`)＋1発ごとにピッチ揺らぎ(`beamPitchJitter`)**。ビーム音は Inspector 未割当なら `Resources/shot_1`(`beamClipResource`) を自動ロード＝コード生成の AudioManager でも鳴る |
| `SceneLoader` | 非同期シーン遷移＋ロード画面（コードで自動生成、`unscaledTime` 駆動） | `LoadScene(string)` |

## 時間・暦・通知システム（TIME #946 / NOTIF #964）
> 戦略と会戦で**同じ game-time が連続して流れる**統一時間と、通知の単一窓口。純ロジックは Core で test-first。
- **`GameClock`(Core)**：統一クロック＝速度/ポーズの**唯一の権威**。`elapsedSeconds`/`speed`/`paused`/`Advance(realDt)`/`SetSpeed`/`Pause`。`StrategySession.Clock`(static) に1つ。**戦略=`GalaxyView`／会戦=`BattleManager` が同一クロックを進める**（潜行/復帰で時間が止まらない＝TIME-5）。
- **`GameDate`(Core)**：game-seconds→宇宙暦SE/帝国暦IC の純変換＋`TimeString`(HH:MM)。`DateParams`(secondsPerDay/daysPerMonth/monthsPerYear/imperialOffset)。
- **`CalendarTick`/`CalendarDispatcher`(Core・TIME-6)**：暦の日/月/年**境界**を数え、跨いだぶんコールバック発火（per-day/month/year フック）。`GalaxyView` が日次で財政(`CampaignRules.TickEconomyDay`)・支持低下イベント・造船・年次で加齢を回す。ポーズで停止・倍速で暦比一定。
- **`TimeFlowRules`(Core・TIME-7 自動スロー)**：平時は暦を圧縮して速く流し（既定 1年≈12分）、会戦の生起・前線突入など**観るべき瞬間**は実時間へ自動減速。`GalaxyView` が `clock.Advance(realDt×compression)` で暦だけ伸縮（実時間アクションの速さは不変）。
- **`AutoBattleSim`(Core・TIME-4)**：自動解決を Lanchester 二乗則で時間積分し**所要 game-time** を返す（観戦会戦と同じ時間を消費）。
- **`TimeDisplay`(Game)**：右上に二重暦＋時刻＋速度を表示。+/- で速度変更。Strategy/Battle 両シーン自動生成。
- **`NotificationCenter`(Core・NOTIF-1)**：通知の**単一窓口**。`Push(category,severity,msg)`→seq採番／`Since(seq)`差分／`Recent`/`Capacity`リングバッファ/`Clear`。`NotificationCategory`{システム/戦闘/建艦/占領/政治/人事/内政/外交}×`NotificationSeverity`{情報/注意/警告}。**新規通知は battleMsg 等を増やさずここへ Push**。
- **`NotificationFeed`(Game・NOTIF-2)**：画面**左下**にトーストを縦積み、実時間で自動フェード。両シーン自動生成・重要度色分け・クリックスルー。`GalaxyView` の会戦結果/占領/造船完成/提督死去は NotificationCenter 経由でここに出る（バナーは現在状態のみ）。

## 艦隊編成プール（#148 / #884 造船供給）
> 「勢力の総艦艇プール → 各艦隊へ配分 → 提督/副提督/参謀を配属」を戦略マップから操作する。
- **`FleetPool`(Core)**：勢力の**保有総艦艇**ストア（`Get`/`Set`/`Add`/`Clear`・非負・勢力独立）。編成画面の総プールの出所。
- **`FleetPoolRules`(Core)**：プールに対し各艦隊 `baseStrength` の合計が超過しないよう配分（`Allocated`/`Available`/`CanAllocate`/`SetAllocation`/`Adjust`・FleetPool ストア版オーバーロード）＋損耗 `ApplyAttrition`。
- **`ShipyardRules.CommissionToPool`(Core)**：造船の完成艦を特定艦隊でなく `FleetPool` へ就役＝**建艦で総艦艇が増える**（既存 `Commission`=特定艦隊就役 とは別経路）。生産力は `ProductionFactor(Province)`(安定度比例・BUILD-2)。
- **`AnnualLifecycleRules`(Core)**：暦の年境界で人物ロスターを1年ぶん老衰判定し死亡者を返す（`LifecycleRules` 委譲・決定論 roll）。`GalaxyView` の onYear で提督が老いて死ぬ。
- **`FleetOrganizationPanel`(Game)**：Strategy シーンに **B キー**で開閉（ポーズ）。勢力プール（総/割当/残）＋艦隊一覧。各艦隊で艦艇数±／司令・副提督・参謀を階級ゲート(○×)付きで配属。`OrderOfBattlePanel`(Battle・梯団管理)の姉妹。
- **`GalaxyView` のデモ配線**：星系ごとの造船所（全勢力＝AIも建艦）を暦の日次で Tick→プール供給、会戦の戦力喪失を `ResolveEncountersWithAttrition` でプール損耗へ。

## データ / 列挙 / ScriptableObject
- `Faction.cs`：`enum Faction { 帝国, 同盟 }`（旧式・後方互換用）。陣営の置き場所は `FleetStrength.faction`。
- **多勢力化（B-1〜B-4）**：`FactionData`(ScriptableObject、メニュー `Ginei/Create Faction Data`→`Resources/Factions/`)＝`factionName`/`color`/`ideology`/`nonHostileFactions`/`legacyFaction`＋`IsHostileTo(other)`（既定=異勢力は敵）。`FactionRelations.IsHostile(...)` が敵対判定の唯一の窓口（双方に `FactionData` があればそれで、無ければ enum 違いで判定＝後方互換）。`IShipTarget.FactionData`／`FleetStrength.factionData`／`ScenarioData.FleetEntry.factionData`／`GameSettings.playerFactionData` で勢力を持たせる。**`FactionData` 未割当なら従来の2勢力 enum 動作のまま**。3勢力目はアセット追加＋シナリオ割当のみで動く（コード変更不要）。`legacyFaction` は既存UI/セーブ/プレイヤー操作判定の橋渡し。**階級**：`FactionData.ranks`(`List<RankEntry>`＝`tier`(序列・大きいほど上位)＋`rankName`。tier 欠番可＝その勢力に無い階級)＋ヘルパ`GetRankName(tier)`/`GetTier(name)`/`HighestTier()`/`GetRanksSorted()`。**勢力をまたいだ同列判定・比較・昇進は `RankSystem`(static) が唯一の窓口**：`AreEquivalent(tierA,tierB)`(=tier一致)/`Compare`/`NextRankTier`/`PreviousRankTier`/`ResolveTier`(欠番をその勢力の直近 tier へ丸め)。既定ラダーは准将5/少将6/中将7/大将8/元帥10（帝国は上級大将9 を足す）。**階級名解決(#14)**：`ResolveRankName(faction,tier)`(tier≤0/null→"")／`DefaultRankName(tier)`(既定ラダー＝5准将/6少将/7中将/8大将/9上級大将/10元帥)／`ResolveRankNameOrDefault(faction,tier)`(FactionData が無くても既定ラダーで階級名を返す)。`FleetHUDManager` が提督名の前に階級名を前置表示。
- `Formation`：`enum { 紡錘陣, 鶴翼陣, 円陣, 横陣, 方陣 }`（定義は Core の `Formation.cs`・#496 で切り出し。既定=紡錘陣）。すべて旗艦＝中心(原点)・左右対称・旗艦の向き(Transform.up=前方)に追従。`ChangeFormation(int)` のインデックスはこの並び順。
- `AIState`：`enum { 接近, 交戦, 撤退 }`（定義は `FleetAI.cs` 内）。
- `ShipRole`（#128・`ShipRole.cs`）：`enum { 戦闘艦, 偵察艦, 入植艦, 輸送艦 }`。部隊の役割（戦闘/非戦闘）。陣営の置き場と同じく `FleetStrength.shipRole` が出所（同一GameObjectの旗艦コア）＋`FleetStrength.IsCombatant`。**非戦闘艦は攻撃せず（`FleetWeapon`/配下艦`EscortShip` が発砲停止）・交戦回避（`FleetAI` が近い敵から逃げる`nonCombatEvadeRange`）・勝敗カウント外（`BattleManager.CountsForVictory`＝`HasHostilePair`/`DetermineWinner` が戦闘艦のみ集計）**。撃沈はされうる（`IShipTarget` 維持＝護衛/通商破壊が成立）。既定=戦闘艦で従来動作（後方互換）。#80（戦闘艦の艦種）とは直交。固有機能は各専用Issue（偵察#119/入植#117/輸送Convoy#94）。**混成禁止(#883)**：編制台帳の `FleetUnitData.shipRole` ＋`ShipRoleRules`(static＝`IsCombatant`/`AreCompatible`(戦闘同士or非戦闘同士のみ)/`IsHomogeneous`/`CompatibleWithGroup`)＝**梯団(#147)は戦闘艦隊と非戦闘艦隊を混成できない**（`OrderOfBattle.AttachFleet` が `CanAttachFleet` で拒否・役割は `FleetRoster` から解決・未登録は戦闘艦扱い）。
- `AdmiralData`（ScriptableObject、メニュー `Ginei/Admiral Data`）：提督能力。`leadership`(統率)/`attack`(攻撃)/`defense`(防御)/`mobility`(機動)/`operation`(運営・将来用)/`intelligence`(情報・将来用)＋`baseStrength`/`admiralName`/`faction`。**参謀**：`staffOfficers`(最大`MaxStaff`=3名、提督データを流用)＋`staffBonusRatio`(0〜1)。各能力の**実効値**は `EffectiveLeadership`/`EffectiveAttack`/`EffectiveDefense`/`EffectiveMobility`/`EffectiveOperation`/`EffectiveIntelligence`＝「基準値＋参謀の当該能力の最高値×`staffBonusRatio`」(上限`MaxStatValue`=100、基準フィールドは非破壊)。`HasStaff`/`GetStaffNames()`。**能力を読む側は必ず Effectivexxx を参照する**（基準フィールドを直接読まない）。**得意陣形(#104)**：`preferredFormation`(`Formation`)＋`hasPreferredFormation`(false＝未設定＝後方互換)＋`IsPreferredFormation(current)`。部隊の現在陣形が得意陣形と一致する間だけ移動・防御にボーナス（下記「提督能力が効く場所」参照）。**構造化命名(#523)**：任意フィールド `givenName`/`middleName`/`familyName`/`nobleParticle`(貴族の前置詞・既定"フォン")/`regnalNumber`(世数)/`epithet`(異名)/`callName`(呼称)＋合成API `FullName`(名・[middle]・[particle]・姓＋世数。空なら `admiralName` フォールバック)/`ShortName`(callName→family→given→admiralName)/`EpithetName`(異名＋短縮名)/`RegnalSuffix(n)`(1〜99→漢数字＋"世")。const `NameSeparator="・"`/`NobleParticleDefault="フォン"`。頭上ラベル＝`EpithetName`、HUD＝`FullName`。**全フィールド任意＝旧アセットは `admiralName` のまま動作（後方互換）**。`GetStaffNames()` は各参謀の `ShortName`。**主人公フラグ(GON-6)**：`isProtagonist`(主人公はAI非制御)。**階級(#14)**：`rankTier`(序列・HUDで階級名を前置表示)。
- `ScenarioData`（ScriptableObject、メニュー `Ginei/Scenario Data`）：会戦定義。`scenarioName` と `List<FleetEntry> fleets`（各エントリ＝`admiral`/`faction`/`spawnPosition`/`formation`＋任意 `fleetNumber`/`fleetName`(#146)・`corps`/`armyGroup`(#147 梯団。番号付き艦隊のみ有効)・`loyalty`/`intrigue`(#817 旗幟。既定1/0＝従来動作)）。**勝利条件**：`VictoryCondition`(`enum { 殲滅, 時間防衛, 旗艦撃破, 護衛 }`)＋`objectiveFaction`(時間防衛の防衛側)/`timeLimit`(秒、0以下で無制限)/`targetAdmiral`(旗艦撃破・護衛のVIP)。`static ActiveScenario`（`BattleSetup` が解決時に公開、`BattleManager` が参照）／`static Resolve(name)`（フォールバック解決）。`BattleSetup` が `Resources` 全走査で `scenarioName` 一致を解決。サンプル会戦・提督アセットはエディタメニュー `Ginei/Create Sample Scenarios`（`Assets/Editor/SampleScenarioCreator.cs`）でワンクリック生成（シナリオ→`Resources/`、提督→`Assets/Data/Admirals/`、既存提督は上書きしない）。
- `SaveData`（`[Serializable]` 平データ）：`playerFaction`(int)/`playerFactionName`(string・多勢力対応：選択 FactionData 名。空なら enum フォールバック)/`scenarioName`/`selectedAdmiral`。`SaveManager`(static) が `persistentDataPath/setup_save.json` に JSON 保存（`JsonUtility`＝欠落フィールドは既定値＝旧セーブ後方互換）。`TitleManager.ContinueBattle` が `playerFactionName` を `Resources/Factions` から解決して `playerFactionData` を復元（見つからなければ enum）。
- `ProtagonistRules`（static・GON-6）：主人公提督のAI制御可否の唯一の窓口。`IsProtagonist(AdmiralData)`(null安全)／`ShouldEnableAI(admiral, isPlayerControlled)`＝`!isPlayerControlled && !IsProtagonist(admiral)`。`BattleSetup` が `FleetAI.enabled` をここで決める（主人公はプレイヤー非操作でもAIに乗っ取られない）。

## 提督能力が効く場所（実効値パターンで反映）
> 下記はすべて `AdmiralData.Effectivexxx`（参謀補完済みの実効値）を読む。基準フィールドを直接読まないこと。
- `leadership`(→`EffectiveLeadership`) → `FleetStrength.ApplyAdmiralData()` が `maxStrength` を補正。`FleetMorale` の `maxMorale`。
- `attack`(→`EffectiveAttack`) → `ShipCombat.ComputeDamage()` のダメージ倍率（50で1.0倍、100で1.5倍、0で0.5倍）。
- `defense`(→`EffectiveDefense`) → `FleetStrength.TakeDamage()` の被ダメージ軽減（最大90%カット）。
- `mobility`(→`EffectiveMobility`) → `FleetMovement.GetMobilityFactor()` の速度/回頭補正。
- `operation`/`intelligence`(→`EffectiveOperation`/`EffectiveIntelligence`) は現状未使用（将来用）。
- **得意陣形(#104・`AdmiralData.preferredFormation`)** → 部隊の現在陣形(`Squadron.currentFormation`)が一致する間だけ：`FleetMovement.GetMobilityFactor()` に `preferredFormationMobilityBonus`(既定1.15) を乗算（移動・回頭↑）＋`FleetStrength.TakeDamage()` で `preferredFormationDamageReduction`(既定0.15) ぶん被ダメージ軽減。判定は `AdmiralData.IsPreferredFormation`、`hasPreferredFormation=false` なら無効。HUD(`FleetHUDManager`)に得意陣形を表示し一致時は★金色で強調。実効値パターン（基準値非破壊）。既定割当はエディタ生成(`RosterCreator`/`SampleScenarioCreator`)＝攻撃型:紡錘陣/機動型:鶴翼陣/防御型:円陣・方陣。
- **参謀**：`AdmiralData.staffOfficers`(最大3名)が各能力の実効値を「最高値×`staffBonusRatio`」で底上げ。HUDは `FleetHUDManager` が提督名の下に参謀名を表示。基準値は書き換えない。

## 個艦戦闘モデル（部隊＝旗艦＋配下艦）
- 部隊は「旗艦(`FleetStrength`)＋配下艦(`EscortShip`)」で構成。攻撃対象は**個々の艦艇**で、共通インターフェイス `IShipTarget`(`Transform`/`Faction`/`IsAlive`/`TakeDamage(int)`) を旗艦・配下艦の両方が実装する。
- **選択・指揮・勝敗カウントは従来どおり部隊(旗艦)単位**。配下艦は個別選択しない（クリックは `GetComponentInParent` で旗艦の `Selectable` に解決）。勝敗は生存旗艦数で数える。
- 旗艦の「艦艇数」は既存 `strength`（兵力）を流用（多め）。配下艦は1部隊 `Squadron.escortCount`(既定50)隻、1隻あたり `EscortShip.shipCount`=`Squadron.escortShipCount`(既定200)で合計兵力が過大にならないよう調整。
- 攻撃ロジックは `ShipCombat`(static) に集約：`FindNearestEnemyInArc`/`FindPrioritizedEnemyInArc`/`FindNearestEnemyInArcOfFleet`(指定艦隊の射界内最寄り)/`GetSquadronOf`(個艦→所属部隊)/`AnyEnemyInArc`/`IsInArc`/`ComputeDamage`(提督攻撃×士気×側背面)/`IsValidTarget`(破棄済み判定)。**ダメージ式や敵探索を各所に重複実装しない**。各艦が自分の1発を撃つ（二重計算しない）。
- 配下艦(`EscortShip`)の自動索敵は `FindPrioritizedEnemyInArc` を使う：第一優先＝射界内かつ**射線の通る敵旗艦**（最寄り）、無ければ第二優先＝射界内の敵配下艦（最寄り）。射線判定 `HasClearShot` は from→旗艦の線分上に（標的以外の）**敵配下艦**がいれば遮蔽とみなし、その旗艦を第一候補から除外する＝配下艦が旗艦を守るスクリーンになる。旗艦の `FleetWeapon` は従来どおり `FindNearestEnemyInArc`／手動指定（`SetManualTarget`）に従い、射線判定は適用しない。
- 旗艦の艦艇数が0になると**破棄せず部隊退却**（`FleetStrength.BeginRetreat`）。退却部隊は `IsAlive=false` となり、標的・発砲・勝敗カウントから一括除外される。

## 既存コンポーネント（現状＝正。これに合わせる）
### システム・管理層
| クラス | 対象 | 責務 / 主なAPI |
|---|---|---|
| `BattleSetup` | Battle シーンに1つ | `[DefaultExecutionOrder(-100)]`。**`SceneManager.GetActiveScene().name != "Battle"` の場合は Awake で即 return**（Title 等に誤配置されても艦隊を湧かせない）。`ScenarioData` から艦隊生成・配置。`fleetPrefab` 必須。生成時に手置き艦隊をクリア＋`FleetRegistry.Clear()`。生成位置は原点中心に `spawnSeparation`(既定2.5)倍して両軍を離す。生成後 `OrientFleetsToEnemy` で各艦を相手陣営重心へ正対させる。プレイヤー陣営以外のみ `FleetAI` を有効化。`scenarioOverride` で直接指定可。**惑星攻城(#131)**：`BattleHandoff.IsPlanetSiege` なら `SetupPlanetSiege`＝中心に `SiegeArena`(惑星＋首飾り射程リング)、攻城艦隊を惑星周囲に円環配置（射程外・惑星へ正対・プレイヤー操作）。 |
| `BattleManager` | Battle シーンに1つ | 勝敗判定（`checkInterval` 秒ごと、`EvaluateVictory`）・戦績記録。**勝利条件**は `ScenarioData.victoryCondition` に従う：殲滅/時間防衛(`objectiveFaction` が `timeLimit` 秒生存)/旗艦撃破(`targetAdmiral` 撃破)/護衛(`targetAdmiral` を守り切る)。**汎用の終了条件は「敵対する旗艦ペアが残っていない」**（`HasHostilePair`＝多勢力対応の殲滅）。勝者勢力は残存旗艦数最多（同数は兵力）＝`DetermineWinner`、`winner`(legacy enum)＋`winnerName`(勢力名)＋`victoryReason`＋MVPを `GameSettings` に記録し `ResultManager` が表示（3勢力以上対応）。**戦績は勢力名キーで集計**：開始時数 `CountInitialByFaction`→決着時 `RecordFactionStats` で勢力ごとの残存数/残存兵力/喪失数を `GameSettings.factionStats` に格納（旧 imperial/alliance フィールドも後方互換で併記）。`ResultManager` は `factionStats` があれば勢力数可変で表示、無ければ2勢力にフォールバック。生存数・敵対判定は `FleetRegistry.AllFlagships`＋`FactionRelations.IsHostile`/`LegacyOf`（退却・破棄・配下艦は数えない）。**非戦闘艦(#128)は勝敗判定から除外**＝`CountsForVictory`(生存かつ `IsCombatant`)で `HasHostilePair`/`DetermineWinner`/勝者代表を絞る（撃沈はされうるが終了条件・勝者算定に数えない）。経過時間 `battleElapsed` は timeScale 追従（ポーズで停止・倍速で加速）。開始時隻数は全 Start 完了後の最初の Update で記録。決着で timeScale=0→Result へ遷移。Rキーでリスタート。**戦略からの実会戦(C-2 #586)**：`BattleHandoff.Pending` なら決着で `WriteHandoffResultAndReturn`(残存兵力を `StrengthScale` で割り戻し→`returnScene` へ)。**Backspace でいつでも戦略マップへ復帰**＝暫定優勢側(`LeadingFaction`)を勝者として書き戻し撤収（以後は自動委任）。開始時にHUDで操作を通知。 |
| `FleetRegistry` | static（シーン内在庫） | 全 `IShipTarget`（旗艦＋配下艦）を**陣営非依存の単一リスト**で保持（多勢力対応）。各艦が出現時 `Register`／破棄・退却時 `Unregister`。`AllTargets`(全個艦)/`AllFlagships`(全旗艦)/`Clear()`。敵味方は探索側が `FactionRelations.IsHostile` で判定する（陣営別バケットは廃止＝`GetEnemies`/`GetFlagships(faction)` は無い。全艦を巡回して判定）。`ShipCombat`/`FleetWeapon`/`EscortShip`/`FleetAI`/`BattleManager`/`FleetCommander`/`BlackHole`/`FleetMovement` がここを参照（`FindObjectsByType` を置換）。`BattleSetup.Awake` が `Clear()`。 |
| `TitleManager` | Title シーン | 新規開始/続きから/設定/終了。`SaveManager` と連携、`continueButton`/`settingsPanel` 参照、`SetVolume`。**シナリオ選択／プレイ陣営選択UIを実行時生成**（自前 Canvas＋全画面ディマーのモーダル、初期非表示）。フロー：「会戦開始」=`StartBattle()`→`ShowScenarioSelect()` で選択画面表示／`SelectScenario(string)` で `GameSettings` に反映＆ハイライト更新。**プレイ陣営は `Resources/Factions` の `FactionData` を列挙してボタン動的生成**＝`SelectPlayerFactionData(FactionData)`(`playerFactionData`＋`legacyFaction`を`playerFaction`へ)。**アセットが無ければ従来の2勢力 enum ボタンにフォールバック**＝`SelectPlayerFaction(int)`。3勢力目は `Resources/Factions` にアセット追加だけでタイトルから選べる／「この設定で会戦開始」=`BeginBattle()` で Save＋ResetStats＋`LoadScene("Battle")`／「戻る」=`HideScenarioSelect()`。Canvas/EventSystem(`InputSystemUIInputModule`) が無ければ生成。**設定画面もフルスクリーンで実行時生成**(`OpenSettings()`→`BuildSettingsScreen()`、銀河背景＋暗幕の専用 Canvas sortingOrder=70)：スライダーで「マスター音量」「開始ズーム(`cameraStartZoom`)」、ボタンで「ゲーム速度(`defaultTimeScale` 1x/2x/3x)」「射界表示(`alwaysShowGizmos`)」を設定し即時反映、「戻る」=`CloseSettings()` で `GameSettings.SavePrefs()` 永続化。uGUI Slider はコード生成(`CreateSlider`)。旧式の手置き `settingsPanel` は不使用(非表示)。 |
| `ResultManager` | Result シーン | `GameSettings` の戦績を `winnerText`/`statsText` に表示。`BackToTitle()`。 |
| `PauseManager` | Battle シーン | Space=ポーズ／Esc=ポーズメニュー／数字1-3=倍速。`Time.timeScale` 制御。ポーズUIをコードで自動生成し EventSystem(`InputSystemUIInputModule`) を保証。`SetVolume`/`SetAlwaysShowGizmos`。 |

### 入力・UI・カメラ
| クラス | 対象 | 責務 / 主なAPI |
|---|---|---|
| `FleetCommander` | 入力統括(1) | 選択の唯一の窓口 `SelectedFleets`(`List<Selectable>`)。左ク=選択（空白で解除／別艦で切替、UIは無視）／右ク=`CommandMenu`。移動先指定モード `StartWaitingForMoveTarget()`＋`IsWaitingForMoveTarget`：カーソルで位置→**右押下で目標確定→押したままドラッグで向き→離して確定**（`SetDestination(pos, 向き)`）、Esc/で取消。`FormationPreview` で配置を半透明表示。**後退指定モード** `StartWaitingForReverseTarget()`：同じ `isWaitingForMoveTarget` フローを `moveIsReverse` で共用（Esc/Pause連携も流用）。向きドラッグ無し＝**現在の向き（射界）を維持**して位置のみ指定、右クリックで確定し `SetReverseDestination(pos)`（追尾は解除＝後退と競合させない、射界内の敵には自動発砲継続）。**前方（向き側）には移動先プレビューを出さず確定も不可**（`IsReversibleTarget`＝前方成分>0で不可。後退で到達できない場所を指示させない）。攻撃目標指定モード `StartWaitingForAttackTarget()`＋`IsWaitingForAttackTarget`：**左クリック=通常攻撃を即時発令／右クリック=攻撃種別メニュー(`CommandMenu.OpenAttackTypeMenu`、通常/ミサイル)を開く**。敵艦（旗艦/配下艦）を親までさかのぼり**その敵艦隊全体**を選択中全部隊の手動攻撃目標に設定（`ConfirmAttack`→`SetManualTargetFleet`＋`SetMissileMode`、確定で `ConfirmPendingAttack`）、`FleetHUDManager.ShowMessage` で「攻撃対象：◯◯艦隊（種別）」を表示、Escで取消。**艦隊円**(`UpdateFleetCircles`/LateUpdate、`Squadron.GetBoundingCircle`＋LineRenderer プール、マテリアル共有・頂点色で色分け)：選択中の艦隊は常時緑円、攻撃目標指定中は全敵艦隊を黄円（カーソル下は橙）で囲う。**ZOC円(#81)**：選択中の自艦隊は薄い `zocSelfColor` 円、攻撃目標指定中のみ敵艦隊を薄赤 `zocEnemyColor` 円で囲う（半径は `ZoneOfControl.GetRadius`＝外接円×zocRadiusScale、`DrawCircleRadius`）。色/線幅は `selectionCircleColor`/`targetCircleColor`/`targetHoverColor`/`zocSelfColor`/`zocEnemyColor`/`circleWidth`。確定/取消後 `DeselectAll`。`SelectFleet`/`GetMouseWorldPosition()`。**陣形変更の実体 `ChangeFormation(int)`**（`CommandMenu`/`FleetHUDManager` はここへ委譲）。**部隊グループ(#83・`HandleControlGroups`)**：Alt＋1/2/3（`GameInput.グループ選択1〜3`＝会戦コンテキスト・**Alt修飾で倍速（素の数字）・Unityエディタの Ctrl＋数字（ウィンドウ切替）と分離**）で、選択中の艦隊があれば `controlGroups` へ割り当て、無ければそのグループを呼び出して選択（破棄・退却艦は除外＝`IsAlive`）。割当/呼出は同じキーで状況により切替。 |
| `CameraController` | Main Camera | パン(WASD/矢印/中ドラッグ)・**画面端スクロール(#87・`edgeScrollEnabled`/`edgeMargin`/`edgeScrollSpeed`＝マウス画面端でパン・非フォーカス/画面外/中ドラッグ中は無効・`GameSettings.edgeScrollEnabled` を毎フレ反映)**・**カーソル中心ズーム(#87・ホイール前後でカーソル下ワールド座標を維持＝`ScreenToWorldPoint` 差分で位置補正)**(`zoomSpeed`/min/maxZoom)・開始時は `startZoom`／`GameSettings.cameraStartZoom`・F=選択艦隊フォーカス・min/maxBounds クランプ・撃沈時 `Shake()`（unscaled で減衰、LateUpdate でオフセット復元）。設定トグルは `TitleManager` 設定画面（射界表示の隣・`edgeScrollEnabled` を PlayerPrefs 永続化）。 |
| `CommandMenu` | uGUI | 右クリックメニュー。選択状況で 移動/後退/攻撃/陣形変更/情報/選択 を動的生成。「後退」は `FleetCommander.StartWaitingForReverseTarget()`（向きを保って下がる）。「攻撃」は選択中つねに表示し、押すと `FleetCommander.StartWaitingForAttackTarget()` で目標指定モードへ移行（左ク=通常攻撃／右ク=攻撃種別メニュー）。`OpenAttackTypeMenu(screenPos)` で通常/ミサイルの選択メニューを開く。陣形サブメニューのボタンは `Formation` enum から動的生成（`BuildFormationButtons`、シーン手配線に非依存）。`OpenMenu`/`CloseMenu`/`IsOpen`/`ChangeFormation(int)`。画面端クランプ。 |
| `FleetHUDManager` | uGUI | 選択艦隊の 提督/陣営/兵力バー/士気バー/陣形 を表示。任意で `shipCountText`(旗艦艦艇数＋配下艦残存数)を表示。`ChangeFormation(int)`。`ShowMessage(text, duration)` で画面上部に通知を実時間で一定時間表示（攻撃対象通知などに使用、TMPテキストを実行時生成）。 |
| `OrderOfBattlePanel` | Battle シーン | **O キーで開閉する編制管理UI（#147・`OrderOfBattle`/`FleetRoster` ビューア＋組み替え）**。プレイヤー勢力の梯団ツリー（軍集団 ⊃ 軍団 ⊃ 第N艦隊＋司令の階級名）を表示し、**艦隊の［移動］→軍団の［ここへ］で別軍団へ移す（中身流動）**＝`OrderOfBattle.AttachFleet`＋該当 `FleetStrength.corpsName`/`armyGroupName` を更新してHUDへ即反映。未編入艦隊も一覧。**梯団の［任命］→司令選任モード**＝在席提督を**階級ゲート(#14)付き**で一覧（必要tier未満は×・選べない＝ゲートをUIで体感）／解任可（`OrderOfBattle.AssignCommander`/`CanCommand`/`UnassignCommander`）。数値ロジックは持たず static 窓口を読むだけ。**表示中 `Time.timeScale=0` でポーズ**（`PauseManager` は `OrderOfBattlePanel.IsOpen` の間ポーズ入力を譲る）。`HelpOverlay` と同じ `RuntimeInitializeOnLoadMethod` で Battle に自動生成。`IsOpen`/`Toggle`/`Close`。**UI Toolkit で実装（UI段階移行のパイロット）**＝flexbox＋`ScrollView` でレイアウト自動＝見切れが原理的に起きない。基盤は `GineiUITK`。 |
| `HelpOverlay` | Battle シーン | H キーで開閉する操作ヘルプオーバーレイ。TimeScale 非依存（ポーズ中も開閉可）。`[RuntimeInitializeOnLoadMethod]` で Battle シーンに自動生成（シーン手配線不要）。Canvas/ディマー/スクロールビュー/操作一覧（選択・移動・攻撃・陣形・カメラ・ポーズ・倍速）をコードで構築。`Toggle()`/`SetVisible(bool)`。 |

### 艦隊コンポーネント（旗艦 GameObject に付く）
| クラス | 責務 / 主なAPI |
|---|---|
| `Selectable` | 選択状態と `selectionRing` 表示。`SetSelected(bool)`/`IsSelected`。 |
| `Squadron` | 旗艦中心の陣形配置(SmoothDamp追従)。Start で旗艦スプライトを流用し `escortCount`(既定50)隻まで配下艦を生成（手置きの子があれば含めて補う）。配下艦は `memberScale`(既定0.8)で縮小（root スケールは変えない）。各配下艦に `EscortShip` 付与＋艦艇数=`escortShipCount`(既定200)設定(`SetupEscorts`)。生成後 `FactionColor.ApplyColors` で着色。Awake で `FlagshipMarker` 自動付与。陣形スロットは隻数/陣形変化時のみ再計算しキャッシュ。交戦中(`FleetWeapon.IsInCombat`)は `combatSmoothTime`(既定1.2)でゆっくり追従（穴埋め移動も緩やか）、非交戦時は `smoothTime`(0.3)。消滅艦は `RemoveMember` で除外（`velocities` と添字同期）。**配下艦挙動の現実化(#69)**：追従速度を旗艦 `maxSpeed`×`catchUpRatio`(既定1.3)で上限クランプ（ワープ・外周艦の振り回しを防止＝SmoothDamp の maxSpeed 引数＋1フレーム移動量の二重クランプ）／**向きは固定同期をやめ**、`headingMoveThreshold`(既定0.5 units/s)以上で移動中は進行方向(+Y=前方)へ `escortRotationSpeed`(既定180°/s)で弧を描いて回頭・定位置付近では旗艦の向き（射界）へ `alignToFlagshipSpeed`(既定360°/s)で復帰（移動中は射界が外れて撃ちにくい＝仕様として許容）／同一部隊内の重なり・すり抜けを `minSeparation`(既定0.6)・`separationStrength`(0〜1既定0.5)の押し離しで解消（`separationUpdateInterval` 既定0.1秒で間引き＋初回位相分散）。すべて `Time.deltaTime`/`Time.time` 基準で timeScale 追従、**root スケールは不変**。`GetEscortStatus`(HUD用)/`GetEscortClassBreakdown`(艦種内訳#80)/`GetFormationSlots`/`GetShipSprite`/`GetShipColor`(プレビュー用)。**艦種差別化(#80)**：`AssignShipClasses` が編成比率(`battleshipRatio`/`cruiserRatio`/`destroyerRatio`)で各配下艦に `ShipClass`(戦艦/巡航艦/駆逐艦)を割当（戦艦=前方添字/駆逐=後方添字、スロット配置自体は艦種非依存＝最小実装）。艦種倍率は `ShipClassStats`(`durabilityMultiplier`/`firepowerMultiplier`/`speedMultiplier`/`scaleMultiplier`/`tint`・Inspector 調整可・実効値パターン)＝`SetupEscorts` で耐久(艦艇数)・火力・速度・スケールに反映、色味は `EscortShip.classTint`→`FactionColor` が陣営色×classTint で塗る（再着色でも維持）。速度倍率は移動ループで `memberEscorts`(添字同期キャッシュ)を読み上限速度に乗算（**`FleetMovement` は不変**＝戦艦は遅く・駆逐は速い遊撃）。`currentFormation`/`spacing`/`smoothTime`/`combatSmoothTime`/`catchUpRatio`/`escortRotationSpeed`/`headingMoveThreshold`/`alignToFlagshipSpeed`/`minSeparation`/`separationStrength`/`separationUpdateInterval`/`escortCount`/`escortShipCount`/`memberShips`/`memberScale`/`battleshipRatio`/`cruiserRatio`/`destroyerRatio`/`battleshipStats`/`cruiserStats`/`destroyerStats`。`ShipClassStats` 定義元（`Formation`/`ShipClass` enum は #496 で Core の単独ファイルへ切り出し済み）。 |
| `EscortShip` | 配下艦の戦闘単位。`IShipTarget` 実装。`shipCount`(艦艇数、旗艦より少なめ既定200、通常は `Squadron.escortShipCount`×艦種耐久倍率が設定)＋陣営は所属旗艦に従属。出現時 `FleetRegistry.Register`／消滅・退却時 `Unregister`、初回発砲位相をランダム化。`TakeDamage` で減算、0以下で `Squadron.RemoveMember`＋`Destroy`。被弾判定用 `CircleCollider2D`(trigger) を Awake で自動付与。攻撃は旗艦の `WeaponArc`/`FleetWeapon` を流用し、自分の位置・向きで射界内最寄り敵を撃つ（自前 LineRenderer ビーム、`fireInterval` 間隔にスロットル）。**艦種(#80・`ShipClass`{戦艦/巡航艦/駆逐艦}＝Core の `ShipClass.cs` で定義・#496)**：`shipClass`＋`firepowerMultiplier`(基準ダメージに乗算＝旗艦基準は非破壊)/`speedMultiplier`(`Squadron` が追従上限速度に乗算・`SpeedMultiplier`)/`classTint`(`FactionColor` が陣営色×これで塗る)＝`Squadron.SetupEscorts` が設定。旗艦退却中は `IsAlive=false`＋発砲停止。**`Squadron` が Start で自動付与**。 |
| `FlagshipMarker` | 旗艦識別マーカー。頭上に金色＋黒フチのダイヤ型アイコン（子 `"FlagshipMarker"`(SpriteRenderer)）＋その背後の**発光ハロー**（子 `"FlagshipMarkerGlow"`(SpriteRenderer、放射グラデ円)）を実行時生成。配下艦には付かないので旗艦の目印になる。**金ダイヤ＝旗艦の固定マーク**(`markerColor`、既定=金、`FactionColor` 着色対象外)、**ハロー＝陣営色**(`FactionColor` が `FlagshipMarkerGlow` を陣営色×`flagshipGlowAlpha` で塗る)で役割を分離＝旗艦か配下か・帝国か同盟かを一目で識別。ハローは `pulseSpeed`/`pulseAmount` で `unscaledTime` 脈動して目立たせ、`sortingOrder`(既定30、ビーム等より前面)で常時前面。常に艦の真上・水平に表示(LateUpdate ビルボード)。`height`/`markerScale`/`sortingOrder`/`markerColor`/`glowScale`/`pulseSpeed`/`pulseAmount`/`defaultGlowColor`。**root スケールは変えない**（陣形計算が狂うため）。`[RequireComponent] Squadron`。**`Squadron` が Awake で自動付与する**（プレハブ編集不要。色等を調整したい時だけ手動で付けて値を変える）。 |
| `FleetMovement` | `SetDestination(Vector2 pos, float? facingAngleZ=null)`（回頭→加減速前進。到着後 facing 指定があればその場回頭してから停止）／`SetReverseDestination(Vector2 pos)`（**後退**：回頭せず現在の向き＝射界を保ったまま並進。速度は `reverseSpeedRatio` 倍＝前進より遅い。**前方へは進まない**＝目標の前方成分(Transform.up方向)を除去し後方/横成分のみで移動（真正面指定は動かない）。戦いながらの離脱用。加減速・実効値パターンは前進と共通）／`FaceTarget(Vector2)`（その場回頭、交戦中の射界維持用）。実効速度は `GetMobilityFactor()`（提督機動×士気×交戦`combatMobilityRatio`×**混雑ペナルティ**）。**混雑ペナルティ**＝他艦隊との接触/密集時の減速：自部隊と他旗艦の `Squadron.GetBoundingCircle()` の重なり量を `FleetRegistry.AllFlagships`（陣営非依存の全旗艦を巡回）で集計し、`minCongestionFactor`〜1.0 の倍率を `GetMobilityFactor()` に乗算（基準値は非破壊）。近接スキャンは `congestionUpdateInterval` で間引き＋初回位相を分散、係数は `MoveTowards`(`congestionSmoothSpeed`) で滑らかに追従。**ZOC減速(#81)**＝敵対部隊の支配領域内で減速：`ZoneOfControl.HostileIntensityAt` の侵入度(0..1)を `1.0〜zocSpeedFactor` に写し(`GetZocFactor`)、混雑とは**別系統**で算出。両者を掛けて `minCombinedFactor` を下限に `GetMobilityFactor` へ乗算（基準値非破壊、完全停止防止）。ZOCスキャンも `zocUpdateInterval` で間引き＋位相分散。public: maxSpeed/acceleration/deceleration/rotationSpeed/faceThreshold/arriveDistance/enableCongestionPenalty/congestionRadiusScale/congestionStrength/minCongestionFactor/congestionUpdateInterval/congestionSmoothSpeed/**enableZoc/zocRadiusScale/zocSpeedFactor/minCombinedFactor/zocUpdateInterval**。 |
| `WeaponArc` | range/halfAngle/gizmoColor。`IsInArc(Transform)`。`OnDrawGizmos` で扇形描画。実行時は子 `"WeaponArcLine"`(LineRenderer) を生成し、`GameSettings.alwaysShowGizmos` が true の時だけ表示。 |
| `FleetWeapon` | 射界内の最寄り敵**個艦(`IShipTarget`)**を自動攻撃(`fireInterval`)。手動攻撃目標は**艦隊単位**が基本＝`SetManualTargetFleet(Squadron)`（旗艦単艦ではなく敵艦隊全体を標的。射界内のその艦隊の最寄り艦を撃つ）。`SetManualTarget(IShipTarget)`(単艦・後方互換)/`ClearManualTarget()`/`HasManualTarget`。手動目標があると `HandlePursuit`→`PursueToward` で**追尾**：射程外(`pursuitStopRatio`)は `FleetMovement.SetDestination` で接近、射程内は `FaceTarget` で停止＆敵を向き続ける。艦隊目標は旗艦位置を追尾基準にし、敵旗艦が退却＝艦隊消滅で自動解除（単艦目標は撃沈・退却で解除）。AI制御中(`FleetAI.enabled`)の艦は追尾しない。移動命令(`FleetCommander.ExecuteMoveCommand`)で手動目標は解除される。**ミサイル攻撃**：`SetMissileMode(bool)`／`missileAmmo`(残弾)／`missileDamageMultiplier`／`missileBeamColor`／`MissileAmmo`。ミサイルモード中は残弾がある間だけ威力強化×倍率で1発消費、弾切れで自動的に通常攻撃へ移行（目標艦隊消滅・`ClearManualTarget` でも解除）。敵探索・ダメージ計算は `ShipCombat` に集約。側背面ボーナス(`flankMultiplier`、真後ろで最大)。`IsInCombat`。`combatMobilityRatio` はここに置く。旗艦退却中(`FleetStrength.IsRetreating`)・**非戦闘艦(`!FleetStrength.IsCombatant`・#128)**は発砲停止。ビーム演出、`DamagePopup.Show`、`AudioManager.PlayBeam`。`[RequireComponent] WeaponArc`。 |
| `FleetStrength` | `IShipTarget` 実装（旗艦＝個艦）。admiralData/admiralName/strength(=旗艦艦艇数)/maxStrength/faction。`ApplyAdmiralData()`（能力反映＋色再適用）、`TakeDamage(int)`（防御で軽減→士気低下→被弾フラッシュ→**0で破棄せず `BeginRetreat()`＝部隊退却**）。`IsRetreating`/`IsAlive`(退却で false)/`retreatDistance`。退却時：AI停止＋敵と反対方向へ離脱、以降は標的・勝敗カウントから除外。頭上ラベル(legacy `TextMesh`、子名 `"StrengthDisplay"`)。**役割(#128)**：`shipRole`(`ShipRole`＝戦闘/偵察/入植/輸送・既定=戦闘艦＝従来動作)＋`IsCombatant`。非戦闘艦は発砲・勝敗カウント外（`FleetWeapon`/`FleetAI`/`BattleManager` が参照）。**旗幟(#817)**：`loyalty`(既定1)/`intrigue`(既定0)＋`battleStance`(`BattleAllegianceManager` が解決)＋`IsFighting`(静観=発砲しない・`FleetWeapon`/`EscortShip`/`FleetAI` が参照)＋`Defect(newData,newLegacy)`(寝返り＝陣営フリップ＋色再適用)。`BeginRetreat(withEffects)` は静観退き（戦わず去る＝演出なし）からも呼ばれる。 |
| `FleetMorale` | 士気管理。非交戦で回復・交戦で低下・被弾で低下(`OnTakeDamage`)。`GetMoraleFactor()`/`IsRouted`。**敗走(士気0)は交戦が `routedRecoveryDelay` 秒途切れたら自然回復し復帰**する。頭上ラベル(子名 `"MoraleLabel"`、低下/敗走時に表示)。`[RequireComponent] FleetStrength`。 |
| `FleetAI` | 敵/非プレイヤー艦隊。`enum AIState{接近,交戦,撤退}`。retreatRatio/searchInterval。敗走(`IsRouted`)や兵力低下で撤退へ。**撤退**は敵が近い間(`reverseRetreatRange`以内・`useReverseRetreat`)は後退移動(`SetReverseDestination`)で背中を見せず下がり、遠ければ通常移動で素早く離脱。**ブラックホール回避**(`avoidBlackHoles`/`blackHoleSafeMargin`/`blackHoleAvoidStrength`)：`BlackHole.All` を参照し、引力圏(`pullRadius`＋マージン)内なら離脱を全状態に優先(`TryGetBlackHoleEscape`)、進路上に引力圏があれば横へ回り込むよう目標座標を補正(`SteerAroundBlackHoles`)。**移動本体(`FleetMovement`)は変更せず AI 側で `SetDestination` の目標を補正する方針**。**ZOC回避(#81・`avoidEnemyZoc`/`zocAvoidStrength`)**：交戦意図のない移動で敵ZOCを横切らないよう `ZoneOfControl.SteerAround` で目標を補正（接近は交戦対象のZOCは無視＝踏み込む、撤退は全敵ZOCを迂回）。ブラックホール回避と同方針。プレイヤー操作艦は `FleetAI` 無効＝対象外。**非戦闘艦回避(#128)**：`!FleetStrength.IsCombatant` なら接近・交戦せず `UpdateNonCombatEvade`＝`nonCombatEvadeRange` 以内の敵から逃げる（近ければ後退・遠ければ通常移動、脅威なしは待機）。`[RequireComponent] FleetMovement/FleetWeapon/FleetStrength`。 |
| `FactionColor` | 陣営色で子SpriteRenderer全部(`"SelectionRing"`/`"FlagshipMarker"`除外)/TextMesh/`FleetWeapon.beamColor`/`WeaponArc.gizmoColor` を着色。例外で `"FlagshipMarkerGlow"`(旗艦の発光ハロー)だけは陣営色×`flagshipGlowAlpha` を乗せる（陣営識別＋マーカー強調）。配下艦は陣営色×`EscortShip.classTint`(艦種色味#80)で塗る＝再着色しても艦種の識別色が維持される。`ApplyColors()`。imperialColor/allianceColor/flagshipGlowAlpha。`[RequireComponent] FleetStrength`。 |

### 表示・背景
| クラス | 責務 / 主なAPI |
|---|---|
| `DamagePopup` | 動的生成のダメージ数値ポップアップ。`static Show(worldPos, damage, isFlank)`。連続生成は縦に段積み(`StackSlots`/`StackStep`)＋水平に微ジッターして団子化防止、timeScale 追従でフェード。側背面は赤橙＋強調。**同時表示数を `MaxActive`(24) に制限**し、多数の配下艦が同時に撃っても出しすぎない（超過分は間引く）。`LabelZoomScaler` で文字サイズをズーム追従。 |
| `BeamFx` | ビーム（艦の主砲）の見た目を一元化する **static ヘルパー**。`CreateMaterial()`(Sprites/Default＝URP安全)／`ConfigureLine(lr, width)`(中央が太いテーパー幅カーブ＋丸端)／`ApplyGradient(lr, beam)`(白熱コア`CoreWhiteness`→陣営色のグラデ・色変化時のみ呼ぶ＝GC節約)／`Play(lr, mat, width, dur, origin, end)`(発射→アルファ＆幅をしぼめる放電フェードの IEnumerator、`Time.deltaTime` 追従)。**旗艦(`FleetWeapon`)・配下艦(`EscortShip`)の両方がここを使い、ビーム描画を重複させない**。マテリアルは呼び出し側が生成し OnDestroy で破棄。 |
| `LabelZoomScaler` | ワールド空間ラベル(TextMesh)の `localScale` をカメラ `orthographicSize` に追従させ、画面上の見かけ大きさをほぼ一定に保つ（密集時の重なり・極小/極大表示を防ぐ）。`Configure(baseScale, referenceOrthoSize)`。基準ズームは `CameraController.startZoom`(16) と揃える。`StrengthDisplay`(頭上ラベル)・`DamagePopup` が利用。**旗艦 root には付けない**（陣形計算が狂う）＝ラベル専用の子/単独オブジェクト限定。 |
| `SpaceBackground` | ParticleSystem で星生成、パララックス。SortingLayer `"Background"`/order -100。 |
| `FormationPreview` | 移動先決定中に選択部隊の陣形を半透明表示（旗艦中心＋配下艦スロットに艦スプライトを淡い陣営色 alpha≒0.3 で描画）。`FleetCommander` が実行時生成し `Show(Squadron)`/`SetPose(pos, angleZ)`/`Hide()`。スロットは `Squadron.GetFormationSlots()` を利用。 |
| `BlackHole` | 特殊地形：ブラックホール（A-5）。`pullRadius` 内の全 `IShipTarget`（旗艦＋配下艦）に引力をかけ、`coreRadius` に入った艦を `TakeDamage(CoreKillDamage)` で戦闘除外（旗艦→`BeginRetreat`、配下艦→`Destroy`）。引力は LateUpdate に外力としてトランスフォームへ加算（`FleetMovement` に触れない）、`Time.deltaTime` 依存で timeScale 追従。艦の列挙は `FleetRegistry.AllTargets`（陣営非依存の全艦在庫、`FindObjectsByType` 不使用）。**静的レジストリ `BlackHole.All`**（OnEnable/OnDisable で登録/解除）で FleetAI の回避判定にシーン内の全 BlackHole を提供。**吸引演出**：渦巻き吸引パーティクル(`showPullEffect`/`pullEffect*`)＋`coreCaptureScale`(コア近傍の確実吸引)。ビジュアルはランタイム生成のラジアルグラデスプライト（コアディスク＋降着円盤リング、sortingOrder -50/-51）。Battle シーン開始時に `[RuntimeInitializeOnLoadMethod]` で自動配置（`AutoSpawnEnabled` で無効化可）。**艦隊への重なり回避**(`avoidFleetsOnSpawn`/`spawnClearance`)：旗艦のレジストリ登録は Start のため、**初回 LateUpdate（描画前・登録後）に安全位置へ退避**(`PositionClearOfFleets`)。既定位置(8,8)が旗艦の引力圏(`pullRadius`＋`spawnClearance`)に重なる場合のみ、原点対称な候補位置(`SpawnCandidates`)から十分離れた場所へ移動（安全な候補が無ければ最遠を選択）。 |
| `FleetDetailPanel` | 選択艦隊の詳細情報モーダル（HUDとは別）。`static Show(Squadron)`/`Hide()`/`IsOpen`。提督/陣営/兵力/士気/陣形/配下艦/参謀などを一覧表示。**開いている間は `Time.timeScale=0` でポーズ**（Esc/閉じる/ディマーで閉じて復帰）。`CommandMenu` の「情報」から開く。名前衝突回避で UI ボタンは `UnityEngine.UI.Selectable.Transition.None` を明示（`Ginei.Selectable` と区別）。`PauseManager` は `FleetDetailPanel.IsOpen` の間ポーズ入力を譲る。コードで実行時生成。 |
| `SystemDetailPanel` | 戦略マップの星系/惑星 情報モーダル（#759・`FleetDetailPanel` の星系版）。`static Show(StarSystem, Province, neighborCount, fleetSummary)`/`IsOpen`。所有/思想/Pop/**安定度バー(緑→赤)**/統合度/産出倍率/反乱リスク（`Province`＋`GovernanceRules`）＋防衛惑星の制空権%/ドメイン状態/侵略%（`Planet`）＋在席艦隊サマリを表示。数値ロジックは持たず static 窓口を読むだけ。**表示中 `Time.timeScale=0` でポーズ**（Esc/閉じる/ディマー）。`GalaxyView` が `I` キーでマウス直下の星系について開く（`GalaxyView.Update` は `SystemDetailPanel.IsOpen` の間 return＝入力・進行を譲る）。実行時生成。 |

### 戦略レイヤー（Phase C／純ロジックは test-first）
> 銀河グラフ（星系＝ノード／回廊＝エッジ）上を戦略艦隊が時限ワープで移動し、敵対勢力に挟まれた回廊（前線）では亜光速で侵入→接触で実会戦（Battle シーン）に移行する。**敵対判定は `FactionRelations`、艦在庫は `StrategicFleetRegistry`、戦闘ロジックは `StrategyRules`（static）が唯一の窓口**。非 MonoBehaviour の純データ/純ロジックは EditMode テストで担保（`Assets/Tests/EditMode`）。
| クラス | 責務 / 主なAPI |
|---|---|
| `StarSystem` | `[Serializable]` 星系ノード。`id`/`systemName`/`position`(Vector2)/`owner`(Faction)/`ownerData`(FactionData)＋`planet`(`Planet`・任意。null=無防備＝停泊で占領＝後方互換／非null=防衛惑星＝攻城が要る)。ctor`(id,name,pos,owner=帝国)`。 |
| `Corridor` | `[Serializable]` 回廊エッジ＋`enum CorridorType{通商,要衝}`。`aId`/`bId`/`length`/`type`。`Connects(id)`/`Other(id)`（無向）。 |
| `GalaxyMap` | 銀河グラフ。`List<StarSystem> systems`/`List<Corridor> corridors`。`GetSystem`/`GetCorridor(a,b)`(無向)/`AreConnected`/`Neighbors(id)`。 |
| `GalaxyPathfinder` | static 経路探索。`FindPath(map,start,goal,avoidFtlBlocked=false)`(Dijkstra)/`PathCost`。 |
| `StrategicFleet` | 戦略艦隊（盤面駒）。`id`/`faction`/`strength`(既定100)/`warpSpeed`/`sublightFactor`(前線は減速)＋現在位置・経路を内部保持。`BeginWarp(map,destId,holdFrac=1)`/`WarpTo(map,goalId)`(移動中の再経路可・前線越え可)/`HoldOnCorridor(map,towardId,fraction)`(回廊上の任意位置で停止保持)/`Tick(map,dt)`。`IsOnCorridor`/`IsMoving`/`IsHolding`/`IsSublight`/`Progress`/`Eta`/`CurrentSpeed`(前線は `sublightFactor` 倍)。前線（敵対端点の回廊）は FTL 不可だが亜光速で進入できる。**交戦固着(#586)**：`engaged`(true の間 `Tick` で前進せず回廊上に留まる＝交戦中の回廊。決着で解除)。 |
| `StrategicFleetRegistry` | シーン内の戦略艦隊在庫。`GalaxyMap map`＋`List<StrategicFleet> fleets`。`Add/Remove/GetFleet`/`Tick(dt)`/`FleetsAt(systemId)`(回廊上は除外)。 |
| `StrategyRules` | static 戦略ロジック。`FindEncounters`/`AnyEncounter`/`TryFindCollision`(回廊上で実際に接触したペア＝`FleetsCollided` の位置交差判定)/`CollidedEncounters`(接触済み全ペア)/`BeginEngagements`(接触ペアを `engaged` 固着)/`TryGetEngagementOnCorridor`(指定回廊の交戦ペア=潜行先特定)/`IsFtlBlocked(map,corridor)`(端点所有者が敵対＝前線)/`ResolveOccupation`/`ResolveAllOccupations`(星系所有変動。**`StarSystem.planet` 有り＝防衛惑星は停泊で落ちず `TickSieges` 経由**)/`TickSieges(map,reg,dt[,SiegeParams])`(防衛惑星の攻城＝停泊した単一敵対勢力の合計兵力をS-AV戦力に `PlanetSiegeRules.Tick`、係争中=防衛艦在席は進めない、占領で `StarSystem.owner` 同期＋新所有者が制空権再建)/`ResolveCorridorBattle(aStr,dStr)`/`ResolveEncounters`/`ApplyBattleResult`(決着で勝者の `engaged` も解除)/`ApplyHandoffResult(reg)`。**戦闘式・接触判定・攻城をここに集約**。 |
| `GalaxyView` | 戦略マップのデモ可視化（MonoBehaviour）。`StrategySession` から銀河を復元（往復で状態保持）。星系/回廊/艦隊を実行時生成（ディスクスプライト・LineRenderer・TextMesh）。左ク=選択(Shift追加)／右ク=星系へ進軍 or 回廊上で停止保持（近い方=星系/回廊で曖昧解決）。Space/1/2/3 で速度。前線回廊は赤。**二層遷移(#586)**：回廊接触は即・実会戦へ強制遷移せず `BeginEngagements` で「交戦中の回廊」として固着表示（赤点滅・⚔交戦ラベル）。**交戦中の回廊をダブルクリック=潜行**（`TryDescend`→`BattleHandoff.Queue`→`Battle` シーン＝手動指揮）。放置は `autoResolveDelay`(銀河時間)後に `ResolveEncounters` で自動解決（Space停止で猶予停止＝intervene 可）。`Start` で `ApplyHandoffResult` 反映後、観ていない他戦線を `ResolveEncounters` で自動侵攻（統一時間の近似）。**惑星攻城(#131)**：デモは帝国星系に `Planet`(制空権持ち)を付与し、`Update` で `StrategyRules.TickSieges` を回す＝停泊した敵対艦隊が制空権制圧→侵略→占領。星系下に攻城状態(制空権残%/侵略%)を表示。攻城中の惑星を**ダブルクリックで戦術マップへ突入**(`TryDescendPlanet`→`BattleHandoff.QueuePlanetSiege`→Battle)。ハブ星系は `systemClickRadius` で星系クリックを回廊より優先（惑星へ進入可）。**システムビュー**：戦闘・攻城が無い平時の星系も**ダブルクリックで戦術マップ（恒星系の閲覧）へ入れる**(`TryEnterSystem`→`BattleHandoff.QueueSystemView`→Battle)。潜行＞攻城突入＞システムビューの優先順。**内政(#109/#759)**：各星系に `Province` を持たせ `Update` で `GovernanceRules.Tick`（所有変化で `OnOccupied`＝不安定化→時間で統合・安定）。デモ勢力に思想を持たせ（帝国=専制/同盟=民主）住民思想は開始所有勢力＝占領で燻る。`I` キーでマウス直下の星系の `SystemDetailPanel` を開く。※Province はデモ内ローカルで `StrategySession` には未永続（Battle往復でリセット）。**国家状態→旗幟(#817)**：`StrategySession.Campaign`(`CampaignState`) を `SetupGovernance` で用意し `Update` で `CampaignRules.Tick`、`TryDescend`（潜行）時に `FactionLoyaltyRules.BaselineLoyalty`/`BribeSusceptibility` を `BattleHandoff.loyalty/intrigue` へ積む＝国家の腐敗が会戦の寝返りに直結。 |
| `BattleHandoff` | static シーン跨ぎの会戦受け渡し（C-3）。`StrengthScale`(40＝戦略兵力↔戦術 baseStrength)。`Pending`/`Resolved`/`factionA/B`/`strengthA/B`/`admiralA/B`/`fleetIdA/B`/`returnScene`/`sideAWon`/`survivorStrength`。`Queue(a,b,returnScene)`/`SetResult(aWon,survivors)`/`Clear()`。`BattleSetup` が Awake で `Pending` なら `SetupFromHandoff()` で2勢力を生成、`BattleManager` が決着で `WriteHandoffResultAndReturn`→残存兵力を `StrengthScale` で割り戻して `SetResult`→`returnScene` へ。**惑星攻城モード(#131)**：`IsPlanetSiege`/`planetSystemId`/`planetName`/`planetOwner`/`planetDefenseRatio`/`planetInvasionRatio`/`besiegerFaction`/`besiegerStrength`＋`QueuePlanetSiege(...)`＝戦略マップで惑星到着→ダブルクリックで戦術マップ突入（`BattleSetup.SetupPlanetSiege`）。**攻城進捗の書き戻し**：`siegeResolved`/`siegeResultDefense`/`siegeResultInvasion`/`siegeResultCaptured`＋`SetSiegeResult(...)`＝`BattleManager.ReturnFromPlanetSiege`(Backspace) が `SiegeArena` の進捗を割合で書き込み、戻った `GalaxyView.ApplySiegeResult` が惑星へ反映（占領なら所有フリップ＋再建）。**システムビューモード**：`IsSystemView`/`systemViewId`/`systemViewName`/`systemViewOwner`＋`QueueSystemView(...)`＝戦闘中でない星系をダブルクリックで戦術マップへ入る非戦闘モード（`BattleSetup.SetupSystemView`＝`SystemView` 生成）。`BattleManager` はこのモード中、戦闘判定せず Backspace で戦略へ戻すだけ（`ReturnToStrategyView`）。各モード(`IsPlanetSiege`/`IsSystemView`)は排他。**旗幟(#817)**：`loyaltyA/B`(既定1)/`intrigueA/B`(既定0)＝`GalaxyView.TryDescend` が国家状態(`StrategySession.Campaign`)から `FactionLoyaltyRules` で積み、`BattleSetup.SetupFromHandoff` が旗艦へ反映＝**腐った国の艦隊は会戦中に寝返りうる**。 |
| `SiegeArena` | 惑星攻城の戦術アリーナ（#131 PB-1/PB-3/PB-5・Battle シーン）。`BattleSetup.SetupPlanetSiege` がフィールド設定後 `Build()`。中心に惑星＋**アルテミスの首飾り射程＝接近限界リング**(`approachRadius`)を描き、`LateUpdate` で旗艦がリング内（制空圏）へ入れないよう押し出す（`FleetMovement` 非改変＝BlackHole 方式）。**接近限界は `planet.DomainDown` 連動(#755 PB-6)**＝制空権が健在な間だけ押し出し＆リング表示、ダウン後（コロニーは最初から制空権0＝即ダウン）は接近限界なし＝艦隊が侵攻のため寄れる。中心ラベルは種別を前置（要塞/コロニー）。**攻城進行**：在席の攻城旗艦数×`siegeSpeedPerFleet` を S-AV戦力として `PlanetSiegeRules.Tick` を回し、制空権制圧→侵略値蓄積（`Planet` を `initialDefenseRatio`/`initialInvasionRatio` で引き継ぎ）。**S-AV演出**：攻城旗艦から惑星へ突入するクラフトをループ表示（リングを越える＝S-AVは制空圏を突破）。制空権/占領のゲージ＋状況ラベル。`DefenseRatio`/`InvasionRatio`/`Captured` を公開し、`BattleManager.ReturnFromPlanetSiege` が `BattleHandoff.SetSiegeResult` で戦略へ書き戻す。実行時生成、`OnDestroy` で破棄。 |
| `SystemView` | 星系の非戦闘ビュー（恒星系の閲覧・Battle シーン）。戦略マップで**戦闘/攻城が無い星系をダブルクリック**したとき `BattleSetup.SetupSystemView` が生成。中心に**恒星**(ラジアルグラデのディスク)＋星系名＋**軌道リング上に第一惑星・第二惑星…を象徴配置**(#767・色違いの惑星＋「第N惑星」ラベル・`orbitRadii` 要素数＝惑星数・実スケールは採らない)を描く。恒星と惑星はディスクスプライトを共有。**惑星単位の内政(#767 惑星層)**：各惑星に `Province` を決定的生成し、**惑星クリック(`SelectPlanet`)でその惑星の内政**(住民思想/人口/安定度/統合度/産出)を表示＋選択リング。**星系の集約サマリ**(`GovernanceRules.AggregateSystem`)を星系名の下に常時表示＝星系は惑星の集約ビュー(#767 ハイブリッド)。艦隊・勝利条件なし＝`BattleManager` は戦闘判定せず Backspace で戦略へ戻す。ブラックホール自動配置は `BattleHandoff.IsSystemView` 時抑制。**内政は三層（勢力＝首都/戦略メニュー・星系＝戦略マップ・惑星＝システムビュー）。後続：惑星内政の操作(政策レバー)・戦略側UIへの集約表示**。実行時生成、`OnDestroy` で破棄。 |
| `StrategySession` | static 戦略世界状態（銀河グラフ＋艦隊レジストリ）をシーン遷移を跨いで保持（C-3）。`Map`/`Reg`/`HasState`/`Set`/`Clear`。Battle へ往復しても盤面を失わない（再生中は static が生存）。 |
| `Planet` | 惑星攻城の対象（#131 惑星戦・PB-3〜7）。`[Serializable]` 純データ。`systemId`/`owner`/`orbitalDefense`(制空権＝ピラー・ドメイン/超兵器・>0で艦隊接近不可)/`maxOrbitalDefense`/`invasionProgress`(侵略値)/`invasionThreshold`＋`DomainDown`(制空権崩壊)/`Captured`(占領)/`FleetApproachBlocked`(接近限界 PB-5)。**種別(#755 PB-6)**：`kind`(`SiegeTargetKind{惑星,要塞,コロニー}`・既定=惑星＝後方互換)＋`KindName`。要塞・コロニーも**完全に同枠**（数値ロジックは値駆動＝種別非依存）。コロニーは `maxOrbitalDefense=0`＝制空権なし＝即 `DomainDown`＝接近限界なしで直接侵攻。攻略の解決は `PlanetSiegeRules` が唯一の窓口。 |
| `PlanetSiegeRules` | static 惑星攻城ロジック（#131・#208 自動解決の数値モデル・純ロジック）。二段階：①S-AV(#757)が制空権を抑制→`DomainDown`／②ダウン後に侵略値を蓄積→閾値で占領（`owner` を攻撃側へフリップ）。`Tick(planet,attacker,attackerSAV,dt[,SiegeParams])`(1tick一段階・状態遷移 `SiegeTickResult{domainWentDown,captured}` を返す)＋`SiegeParams{suppressRate,invadeRate,defenseRegen}`(`Default`=1/1/0)。**種別別の規模プリセット(#755 PB-6)**＝`SiegeProfile`(制空権最大/占領閾値)＋`DefaultProfile(kind)`(要塞＞惑星＞コロニー・const で調整可)＋統一生成窓口 `CreateTarget(systemId,owner,kind)`。戦闘式・占領判定・規模差をここへ集約。 |
| `Allegiance` | 関ヶ原型「戦う前に決まる戦い」の一諸侯の旗幟（#817 SEKI・非MB純データ）。`side`/`strength`(名目兵力)/`loyalty`(忠誠0..1)/`intrigue`(調略の浸透0..1)/`locked`/`stance`(`enum Stance{未定,戦う,静観,寝返り}`)＋`FightsFor(side)`(実効的に side のため戦うか＝戦う or 敵側からの寝返り)。 |
| `LoyaltyRules` | static 関ヶ原型ロジック（#817・SEKI-1〜3 / #822・純ロジック・test-first）。旗幟を忠誠/調略/趨勢から解決し**寝返りカスケード**(ナッシュ均衡崩壊 #818)で**実効兵力**を確定＝勝敗は名目でなく実際に戦う兵力で決まる（静観=フリーライダー #820 は数えない）。`ResolveStance`(忠誠勝→戦う/調略+劣勢→寝返り/他→静観)/`EffectiveStrength`(戦う＋敵からの寝返り)/`Momentum`/`ResolveCascade`(不動点反復)/`ResolveWinner`(実効多数が勝者)/`ApplyIntrigue`(調略=家康の手紙 #819)/`NominalStrength`/`ResolveEngagement`(SEKI-5・損害は戦った少数に集中＝静観/寝返りは無傷)。戦前の調略で勝敗を仕込むのが核。 |
| `BattleAllegianceRules` | static **会戦中の旗幟解決の唯一の窓口**（#817 会戦配線・純ロジック・test-first）。カスケード本体は `LoyaltyRules.ResolveCascade` に委譲し、会戦適用に必要な差分だけ足す：`ResolveTransitions`(再解決して `StanceChange{id,from,to}` を収集・**寝返りは locked＝不可逆**＝寝返り返りしない)／`ShouldWithdraw`(自軍の実効兵力0かつ敵残存＝静観組が戦わず去る条件)。 |
| `BattleAllegianceManager` | Battle シーン（`RuntimeInitializeOnLoadMethod` 自動生成・手置き不要）。**旗幟を `evaluateInterval`(2s・timeScale追従) ごとに再評価し、寝返り/静観を実艦隊へ発火**（#817 会戦配線）：現在戦力を `Allegiance` へ同期→`BattleAllegianceRules.ResolveTransitions`→適用＝**寝返り**(`FleetStrength.Defect`＝陣営フリップ＋色再適用・AI/選択の整理＝来た側がプレイヤーなら手動指揮/離れたらAI(`ProtagonistRules`)・旧敵の手動目標解除 `FleetWeapon.ClearManualTargetIfFleet`・HUD通知)／**静観**(発砲停止＝`FleetStrength.IsFighting` ゲート)／**静観退き**(`ShouldWithdraw` 成立で `BeginRetreat(withEffects:false)`＝戦わず去る＝決着を停滞させない)。**全艦隊が完全忠誠(1/0)なら休止＝従来動作（後方互換）**。legacy 2陣営の会戦のみ対象（3勢力以上は対象外＝最小実装）。`fightThreshold`/`defectThreshold`/`initialDelay`。 |
| `Organization`／`SuccessionRules` | static カリスマの日常化＝英雄死後の組織存続（#812/#814/#816・本線 SPINE-1 #795・純ロジック）。`Organization`(結束/制度化/個人カリスマ/崩壊フラグ)。継承=結束のうち制度化分は残り個人カリスマ分は後継者(正統性×カリスマ)だけ引継ぐ＝属人組織は英雄と滅び(信玄)・制度化は超えて続く(幕藩)。`ResolveSuccession`/`InvestInstitution`(制度化投資)/`Refactor`(急な中央集権化=bug3離反)/`MythPressure`(bug2)/`LegitimacyConflict`(bug1)。 |
| `Polity`／`ConsentRules` | static 「権力は借り物」（ガンジー #836/#837・純ロジック）。`Polity`(被支配者人口/支配側戦力/協力/正統性/抑圧)。実効統治力=直接戦力+協力×人口＝少数が多数を統べるのは協力ゆえ。協力は抑圧で下がり正統性で回復し、閾値割れor `Withdraw`(非協力・ボイコット #838)で**戦わずに統治不能**。`Tick`/`ControlStrength`/`IsUngovernable`/`Withdraw`。関ヶ原の条件付き忠誠の文明規模版。 |
| `Community`／`HopeRules` | static 希望と末人＝ロンドン派（フロストパンク #852-856・FRONT 末人 #847・純ロジック）。`Community`(希望/抑圧/末人フラグ)。希望が尽きると末人(ロンドン派)が立つ。`Shift`(出来事)/`UpdateDissent`(末人発火)/`Faith`(意味を捏造し希望UP=信仰ルート)/`Order`(力で抑圧し鎮圧=秩序ルート)/`IsTyranny`(専制=虚構の暴走 #856)。救いの暗い鏡 #857 へ。 |
| `Regime`／`DynastyRules` | static 天命と易姓革命（孔子 #867・転換エンジン #801/#823・日常化 #814・純ロジック）。`Regime`(正統性/腐敗/徳)。`Tick`で腐敗(制度疲労)が徳の分だけ遅く進み正統性を蝕む→`MandateLost`(天命喪失)/`ReformerArises`(改革者=ルター #824)。`Reform`(制度更新=明治近代化で腐敗↓正統性↑)/`Revolution`(易姓革命=王朝交代で再起動)。東西の秩序転換を回す共通エンジン。 |
| `LogisticsRules` | static 物流・版図の一体化（地政学 GEO-3 #844・純ロジック）。既存 `GalaxyMap` 上で所有星系のみを通る最大連結成分から一体化度(0..1)を出す＝敵に分断/散在する版図は国力を出し切れない（島嶼・帝国の縮図 #839）。`OwnedSystemIds`/`LargestConnectedComponent`/`CohesionFactor`。 |
| `FactionState`／`FactionStateRules` | static 国家状態＝社会・政治シミュ層の**合成**（統合層・純ロジック）。`FactionState`(王朝 `Regime`/統治体 `Polity`/組織 `Organization`/共同体 `Community`＋統治スタイル `inclusiveness` 収奪0↔包摂1)。`Tick` が因果連鎖を回す：腐敗→正統性低下→収奪/包摂が抑圧を決める→合意の増減→希望のドリフト→末人判定。＝収奪は即効だが崩れ、包摂＋有徳は続く。`Stability`(正統性/合意/結束/希望の平均)/`IsCollapsing`(天命喪失/統治不能/組織崩壊/末人)。per-system の `Province`(内政)とは別＝勢力レベルの合成。 |
| `Movement`／`NonviolenceRules` | static 非暴力＝道徳の柔術（公民権 #831/#832・ガンジー #836・純ロジック）。`Movement`(支持/献身)。`Repress`(可視化された弾圧 brutality×mediaReach×commitment で沈黙の多数を支持へ転換＝見られて勝つ・不可視は転換せず)/`IsTriumphant`(支持が閾値で勝利)/`PressurePolity`(支持を `ConsentRules.Withdraw` で統治体へ非協力波及)。情報戦#819の反転。 |
| `FocusRules` | static 三密＝身・口・意の同期（空海 #872・謙信の無我 #810 の精密化・純ロジック）。`Sync`(身×口×意の積＝一つ欠けると崩れる)/`OutputMultiplier`(base+bonus×同期度＝3つ揃うと一時的最大出力)。実効値パターンの精神版(基準非破壊)。戦闘の極限集中バフに転用可。 |
| `FactionLoyaltyRules` | static 国家状態↔諸侯の忠誠の連結（社会シミュ ↔ 関ヶ原 #817・純ロジック）。`BaselineLoyalty(FactionState)`(正統性/結束/希望の平均)/`BribeSusceptibility`(1-基準忠誠)/`ApplyBaseline(Allegiance,...)`。腐った国ほど寝返りやすい＝武力でなく腐敗で「戦う前に決まる戦い」に負ける。 |
| `CampaignState`／`CampaignRules` | static 戦役の世界状態＝社会シミュ×地理盤面の**最上層統合**（純ロジック）。`CampaignState`(`GalaxyMap`＋勢力ごとの `FactionState`)。`EnsureStates`(所有勢力に国家状態を用意・冪等)/`GetState`/`Tick`(全勢力進行)/`EffectiveStability`(安定度×版図一体化度 `LogisticsRules`＝散在は割引)/`LeadingFaction`(実効安定度最大)。全モジュールを盤面で一つに回す。**セーブ(FND-2 #495)**は `CampaignSerializer`/`CampaignSaveData`/`CampaignSaveManager`。 |
| `CampaignSaveData`／`CampaignSerializer`／`CampaignSaveManager` | 戦役セーブ＝世界状態のバージョン付きJSON永続化（FND-2 #495・`CampaignState` 一本化）。`CampaignSaveData`(平データDTO：星系/惑星/回廊/勢力国家状態＋`schemaVersion`。SO参照=FactionData は名前文字列で持つ)。`CampaignSerializer`(static・純ロジック・test-first)＝`ToSaveData`/`FromSaveData`(状態↔DTO)／`ToJson`/`Parse`/`FromJson`(JsonUtility・空/不正は null・欠落は既定値=前方互換)。`CampaignSaveManager`(Unity層)＝`persistentDataPath/campaign_save.json` 入出力＋復元時に `ownerData` を `Resources/Factions` から名前で解決(`Save`/`Load`/`HasSave`/`Delete`)。設定/戦績は `GameSettings`、会戦セットアップは `SaveManager`、戦役世界状態はこちら。 |
| `Person`／`PersonRules` | static 人物システム＝**軍人/文民**と**適材適所（正名 #866）**の純ロジック。`Person`(`role`＝軍人/文民・`rankTier`＝階級#14・`isPolitician`＋`AdmiralData` と同枠の6能力。`ICharacter` 実装)。軍才 `MilitaryAptitude`(統率/攻撃/防御/機動の平均)／文才 `CivilAptitude`(運営/情報の平均)。`PersonRules`：`Aptitude(p,post)`(軍務=軍才/政務=文才)／`RoleMatches`(軍人↔軍務・文民↔政務)／`Effectiveness`(適性×役割一致なら1.0/不一致は `PersonParams.mismatchPenalty` 既定0.5倍)／`BestFor`(勢力内で役職に最適な人物を自動配属)。基準値非破壊（実効値パターン）。提督＝軍人 Person に相当・列伝/殿堂 #785/#784・主人公 #735 と接続。 |
| `ICharacter`／`Office`／`OfficeRules`／`GovernmentRegistry` | 政府役職システムの基盤（GOV-1 #142／GOV-3 #144）。`ICharacter`＝役職保持者の共通窓口（`Person` が実装・軍人/文民どちらも役職に就ける）。`Office`(`officeName`/`scope`(国家⊇方面⊇星系)/`domain`(軍事/内政/外交/財政/元首)/`civilianOnly`/`militaryOnly`/`politicalAppointmentOnly`/`requiredTier`/`careerCeiling`/`slots`)＝1役職の定義。`OfficeRules`(static)＝`CanHold`(就任資格＝軍人/文民/政治任用/必要階級)／`CoversScope`／`CanPropose`(保持役職の所掌×スコープで提案権限・元首は全所掌)。`GovernmentRegistry`(static)＝任命台帳：`TryAppoint`(資格・定員チェック)／`Dismiss`／`GetHolder`/`GetHolders`(スコープキー付き＝星系総督)／`GetOffices`／`HolderCount`／`Clear`。階級のみの旧経路はフォールバック（後方互換）。`FleetRegistry`/`OrderOfBattle`（軍）とは別物＝政府人事。 |
| `CivilianControlType`／`CivilianControlRules` | 軍政関係＝文民統制（GOV-4 #145・政体 #117 が型を決める）。`enum CivilianControlType{文民統制,君主統帥,党軍,軍部優位,未分化}`。`CivilianControlRules`(static)＝`MilitaryMayHoldPoliticalOffice`(軍人の政治兼任可否＝軍部優位/未分化のみ)／`CiviliansAppointMilitary`(軍人事を文民/上位が握るか)／`DefaultMilitaryOnly`/`DefaultCivilianOnly`(政体から役職の既定資格を導出＝#141 兼任マトリクスの裁定)／`CoupRisk`(統制弱・支持低#113・敗戦で上昇／統制強で抑圧)／`WouldCoup`(`ControlParams.coupThreshold` 既定0.6)。型未設定なら従来動作（制約・クーデター無し）。 |
| `Ministry`／`MinistryRules` | 省庁＝行政機構の編制ツリー（GOV-5 #158・軍の `OrderOfBattle` の文民版）。`Ministry`(`ministryName`/`domain`/`parentId`/`childIds`/`headOfficeId`(大臣ポスト=`Office`)/`staffSlots`/`isTemporary`(臨時官庁)/`institutionalInterest`(省益=縦割り強度・内部勢力#113)/`staffIds`(配属官僚=`Person.id`))。`MinistryRules`(static・`List<Ministry>` 上で動く＝別レジストリ非新設)＝`AttachChild`/`DetachChild`(単一親・循環防止)／`AssignOfficial`/`RemoveOfficial`/`Transfer`(定員順守・単一所属)／`AllOfficialsUnder`/`CountStaffUnder`(再帰集計)／`CreateTemporary`/`Dissolve`(臨時省庁の新設/廃止＝配属を再配置)／`SectionalismFriction`(関与省庁の省益平均＝横断政策への抵抗)。 |
| `Party`／`PartyFaction`／`PartyRules` | 政党と最小選挙・官僚の役職上限（GOV-6 #159）。`Party`(`partyName`/`platform`/`classBase`(#110)/`leaderId`(党首=政治家)/`memberIds`(議員)/`support`(支持/議席#113)/`factions`)＝内部勢力#113の一種。`PartyFaction`(`bossId` 領袖/`memberIds`/`Weight`)。`PartyRules`(static)＝`RulingParty`(最大支持の党)／`Premier`(与党党首=政府の長＝党勢で首班決定)／`MarkDemocraticAppointments`(民主国家で必要tier>ceilingの政治職を `Office.politicalAppointmentOnly` へ昇格＝官僚は事務次官級まで・上は政治家。軍/非民主は不変)。 |
| `Calendar`／`LifecycleRules` | 人物ライフサイクル＝年齢・死亡（LIFE-1/2 #151/#152）。`Calendar`(`currentYear`/`yearsPerTurn`/`Advance`＝唯一の時間窓口。会戦モードは進めない＝加齢なし)。`LifecycleRules`(static)＝`Age(birthYear/ICharacter, currentYear)`(生年から導出)／`AnnualMortality`(年齢で上がる死亡率カーブ・`LifespanParams` 既定 基礎0.5%/60歳から+2%)／`ShouldDieOfAge`(roll で決定論・yearsPerTurn 換算)／`Kill`(没年を立てる)。年齢は `ICharacter.BirthYear`／`IsDeceased`／`IsAvailable`(生存かつ自由)で共通アクセス。`Person` に `birthYear`/`deathYear`/`captiveStatus`/`heldBy`＋経歴(`hammockNumber`/`graduationYear`/`schoolId`/`examRank`)＋専門能力(`research`/`engineering`/`planning`/`production`・`TechnicalAptitude`)。 |
| `VacancyRules` | static 後任補充＝継承（LIFE-2 #152）。死亡/捕虜で空いた席を埋める。`SelectSuccessor`(有資格＝`IsAvailable`の最高tier・同点は若い順)／`FillVacancy`(故人保持者を解任→`GovernmentRegistry` で後任任命・適任不在は空席のまま＝機能低下を許容)／`ClearDeparted`(台帳から任に就けない保持者を一掃)。新しい任免窓口は作らず `OfficeRules`/`GovernmentRegistry` に委譲。 |
| `Population`／`DemographicsRules` | 人口動態（LIFE-3 #153）。`Population`(年少0-14/生産年齢15-64/高齢65+の3コホート＋`Total`/`Dependents`)。`DemographicsRules`(static)＝`DependencyRatio`((年少+高齢)/生産年齢)／`Phase`(人口ボーナス/中立/人口オーナス・`DemographicsParams` 既定 従属0.5未満/0.7超)／`OutputFactor`(ボーナス1.2/オーナス0.8＝#106係数で生産#93・徴募#96・安定#111へ)／`Tick`(出生→年少／年少→生産→高齢の加齢／高齢死亡・`VitalRates`)。マクロ背景として効かせる（タイクン化回避）。 |
| `SeniorityRules` | static 席次主義 vs 実力主義（LIFE-5/6 共有 #155/#156）。軍のハンモックナンバーと文官の合格順位を同型で扱う。`InitialTier`(席次→初期tier・`SeniorityParams` 既定 首席6/20席で−1/下限5)／`PoliticalRigidity`(政体で席次の固さ＝王党0.9/民主0.6/共産0.3/軍閥0.2)／`EffectiveStanding`(席次1/rank と merit を rigidity で混合)／`MeritOvertakes`(下位席次の俊英が上位の凡才を追い越せるか＝政体で変わる)。基準値非破壊。 |
| `CareerTrack`／`CareerPipelineRules` | static 人材の出自経路（LIFE-5/6/7 #155/#156/#157＝武/官/技の三系統）。`enum CareerTrack{士官学校,科挙,有力者,テクノクラート}`。`TrackRole`(士官学校=軍人/他=文民)／`TrackIsTechnical`／`Stamp`(ネームド化＝役割・卒業年・制度・席次を刻む。軍は `hammockNumber`/科挙は `examRank`)／`CliqueBond`(同窓+同期で学閥/文官閥/専門閥の結束・`CliqueParams` 既定 同窓0.3+同期0.4)／`TechnocratEffectiveness`(専門才＝実力本位)／`BestTechnocrat`(技術系役職の自動配属)。席次→序列は `SeniorityRules` に委譲。閥は #113 内部勢力の一種。 |
| `CaptivityRules` | static 捕虜化・解放・処断（LIFE-4 #154・死亡#152と対の可逆ルート）。`CaptiveStatus{自由,捕虜,処断済}`(Person)＋`CaptiveDisposition{解放,処断,登用}`。`CaptureChance`/`IsCaptured`(包囲#81・指揮・士気で逃げ切る)／`Capture`(自由→捕虜＝席は空くが生存)／`Release`(元勢力復帰)／`Execute`(処断済＋`LifecycleRules.Kill`＝死亡#152へ合流)／`Recruit`(自陣営へ寝返り)／`RecruitChance`(思想差小・厚遇で成立・最大0.4＝稀)／`DefaultDisposition`(政体#145で AI傾向＝王党解放/共産処断/軍閥解放/民主解放/未分化処断)／`ExecutionSupportPenalty`(著名提督の処断は支持#113を最大30%削る)。乱数は roll を渡す決定論。 |
| `LeadershipElectionRules` | static 党首選出＝総裁選（GOV-7 #165・自民党型）。`VoteParams`(党員票/議員票の比重・`Default`=0.5/0.5)＋`Candidate`(党員票/議員票)。`Score`(加重和)／`Elect`(最大得点の候補)／`HasTwist`(党員票トップ≠議員票トップ＝人気と党内基盤の乖離)／`TallyLegislatorVotesByFaction`(派閥推薦→所属議員数を議員票へ集計＝領袖が票を束ねる談合)。党首交代は #116 イベント想定（ここは票の解決のみ）。 |
| `Province` | 内政（#109 P-1/P-2 最小ループ）の純データ。`[Serializable]`。`systemId`/`nativeIdeology`(住民の思想＝占領しても即は変わらない)/`population`(Pop)/`stability`(安定度0..100)/`integration`(占領統合度0..1)。所有勢力は `StarSystem.owner` が出所＝ここには持たない。解決は `GovernanceRules` が唯一の窓口。タイクン回避＝建設マイクロ/通貨経済は持たない。 |
| `ColonyMission`／`ColonizationRules` | 入植＝1惑星→銀河へ拡張（#129・#117）。`StarSystem` に `habitable`/`isColonized`(既定true＝後方互換)。`ColonyMission`(対象星系/勢力/`colonistIdeology`＝入植元の政体思想/progress)。`ColonizationRules`(static)＝`CanColonize`(未入植かつ居住可能かつ探索済み＝探索は G-2 #119 未実装ゆえ引数で受ける)／`Tick`/`IsComplete`(到着後の成立進捗・`ColonizationParams` 既定 buildTime30/初期人口20)／`Establish`(星系を支配化＋内政対象 `Province` を生成＝`GovernanceRules.OnOccupied` で未統合・低安定に初期化＝時間で統合)／`Colonize`(即時成立の簡易窓口)。住民思想は入植元政体に従って発生（階級発生は #110/#120）。入植艦は成立で使い切り（艦の消費は戦略レイヤー側）。 |
| `Shipyard`／`BuildOrder`／`ShipyardRules` | 造船・建艦（#884 BUILD-1〜4）。`Shipyard`(星系の造船能力＝`parallelCapacity` 船渠数/`buildPower` 建艦速度/`queue`)＋`BuildOrder`(艦種#80×役割#128/`cost`/`progress`/`strengthYield`/`reinforceFleetNumber`)。`ShipyardRules`(static)＝`Cost`/`StrengthYield`(BUILD-3＝戦闘艦は艦種・非戦闘艦は役割でコスト差)／`ProductionFactor(Province)`(BUILD-2＝`GovernanceRules.OutputFactor` 安定度比例＝支配≠即建艦)／`Enqueue`/`EnqueueReinforcement`/`Tick`(先頭 capacity 件を buildPower×生産力で進め完成を返す)／`Commission`(BUILD-4＝完成を `FleetRoster` へ新造登録 or 既存艦隊の兵力補充)。技術#123/動員#158 は `Tick` の productionFactor へ掛け込む（BUILD-5/6・新モジュール不要）。タイクン化回避＝係数で背景的に。 |
| `ResourceStockpile`／`ResourceProductionRules`／`SupplyRules`／`CommerceRaidingRules` | 戦時兵站レイヤー（EPIC #92 L-1〜L-3）。**L-1 資源生産(#93)**：`ResourceType{物資,弾薬,燃料}`＋`SystemType{工業,農業,鉱業,居住}`(StarSystem.systemType・既定居住)＋`ResourceStockpile`(勢力備蓄・`Add`/`TryConsume`/`AddAll`/`IsDepleted`)。`ResourceProductionRules.Produce`(類型×安定度比例 `GovernanceRules.OutputFactor` で時間産出・建設マイクロ無し)。**L-2 補給線(#94)**：`SupplyRules.SuppliedSystems`/`IsSupplied`(補給源から所有回廊で到達・敵ZOC下 `blocked` は遮断＝断たれると前線が枯れる)／`TickFront`(補給で増・補給切れで枯渇＝滅びの時計)。**L-3 通商破壊(#95)**：`CommerceRaidingRules.ConvoyDestroyed`(襲撃側>護衛+自衛で撃破)／`EscortNeeded`／`DeliveredSupply`／`ResolveInterception`(撃破なら補給0＝敵が干上がる・護衛で守る)。混成禁止#883＝護衛は別部隊で随伴。L-4 人口/徴募(#96)は Phase D。タイクン化回避＝通貨/多段生産ツリー無し。 |
| `FiscalState`／`FiscalRules` | 財政・経済の純ロジック（EPIC #163・#161 PB/国債/金利/為替・#162 税/社会保障）。`FiscalState`(歳入/歳出(利払い除く)/債務/税率/社会保障水準)。`FiscalRules`(static)＝**#161**：`PrimaryBalance`(歳入−歳出)／`InterestRate`(基準＋債務比率のリスクプレミアム)／`InterestPayment`/`OverallBalance`／`Tick`(黒字=減債/赤字=国債発行)／`IsDebtSpiral`(高債務で利払い>PB黒字＝複利膨張)／`DebtRatio`／`FiscalHealthFactor`(0..1・安定度#109/支持#113へ係数#106)／`ExchangeRateFactor`(財政悪化→通貨安・交易#94/フェザーン#160)。**#162**：`TaxRevenue`(課税ベース×税率・階級別#110)／`WelfareCost`(扶養×水準＝人口オーナス#153連動)／`Revenue`/`Expenditure`組立／`TaxBurdenPenalty`(高税の不満)/`WelfareHopeBonus`(高福祉の希望#852)。`FiscalParams` 既定 基準金利2%/安全債務比0.6/危機2.0。タイクン化回避＝高位の決断＋創発的帰結（債務スパイラル・通貨危機）。 |
| `FiscalClass`／`TaxStructure`／`RedistributionRules` | 再分配＝税の階級別負担（#163 #162）。`FiscalClass{富裕層,中間層,貧困層}`＋`TaxStructure`(各層の税率/人口シェア/富シェア)。`RedistributionRules`(static)＝`Progressivity`(富裕税率−貧困税率＝累進+/逆進−)／`EffectiveTaxRate`(富で重み付け平均＝`FiscalRules.TaxRevenue` へ合流)／`ClassTax`/`TotalTax`／`ClassSupportDelta`(累進＝貧困層支持↑富裕層↓・逆進で逆・支持#113)／`ClassTension`(再分配が極端なほど階級対立↑・内部勢力#113/反乱#109)。「高税高福祉↔低税低福祉」を政治争点に。 |
| `GameEventDef`／`EventRules`／`EventEngine` | イベントエンジン＝横断基盤（#116・データ駆動：条件発火→通知/選択肢→効果）。`GameEventDef`(id/title/body/`choices`(`EventChoice`=ラベル＋効果デリゲート)/weight/repeatable/cooldown/`condition`(`Func<EventContext,bool>`)/`AddChoice`/`When`/`IsNotification`)＋`EventContext`(faction/systemId/payload＝Province/Stockpile等を運ぶ)＋`EventRuntimeState`(fireCount/lastFireTime)。`EventRules`(static)＝`IsEligible`(条件＋一回限り＋クールダウン)／`SelectWeighted`(重み付き抽選 roll∈[0,1))／`MarkFired`／`ApplyChoice`。`EventEngine`(plain・headless)＝`Register`/`Tick`(1件発火→キュー・多重提示なし)/`Current`(提示UIが読む)/`Resolve`(効果適用＋pop)/`ClearPending`。内政P-6#115・政治#14・戦略イベント(危機/英雄/援軍/離反)がこれに乗る。サンプル=`SampleEvents`(英雄登場/補給危機2択)。提示モーダルUI＋`EventManager`(MonoBehaviour駆動)は実機配線。 |
| `DisclosureEntry`／`DisclosureRules`／`DisclosureLedger` | 開示エンジン＝物語の背骨（FND-4 #495・秘史開示 #450）。世界観EPIC（秘史/天井CAP/ID vs進化論/ニーチェ/啓蒙/エンディング…）を「条件→開示/変化」の<b>データ入力</b>として乗せる。`DisclosureEntry`(id/title/body/category(秘史/真相/予言/エンディング)/`prerequisites`(先行開示)/`condition`(`Func<EventContext,bool>`)/`onReveal`(効果＝イデオロギー変化/エンディング解放)＋`Requires`/`When`/`OnReveal`)＋`DisclosureState`(`revealedIds`・直列化可＝FND-2セーブ)。`DisclosureRules`(static)＝`PrerequisitesMet`/`ConditionMet`/`CanReveal`/`TryReveal`(開示＋効果)/`Progress`(0..1)。`DisclosureLedger`(plain・headless)＝`Register`/`Evaluate`(<b>連鎖開示を不動点まで</b>＝秘史Aの開示が秘史Bの前提を満たす)/`IsRevealed`/`Progress`。`EventContext`(#116)共有＝GameEvent効果から `TryReveal` 連動可。サンプル=`SampleDisclosures`(断片→真相→エンディング解放の連鎖)。提示演出UIは実機配線。 |
| `FleetUnitData` | 艦隊ユニット（#146・ScriptableObject）。「提督＝艦隊」を「艦隊ユニット(番号/勢力/編制)＋配属提督」へ分離する核。`fleetNumber`/`fleetName`/`faction`/`factionData`/`assignedAdmiral`(提督=司令・null=空席)/`shipRole`(#128/#883)/`baseStrength`/`formation`/`meritScore`/`status`(`FleetStatus{現役,解隊,永久欠番}`)＋**指揮班3ネームド(#885)**：`viceCommander`(副提督=次席)/`chiefOfStaff`(参謀=幕僚)＋`Commander`/`HasVice`/`HasChief`/`HasFullCommandStaff`。`IsActive`/`HasAdmiral`/`DisplayName`(固有名→「第N艦隊」)。管理は `FleetRoster` が唯一の窓口。**会戦中の艦在庫 `FleetRegistry`(ランタイム)とは別物**。3スロット未設定なら提督単独で従来動作。 |
| `CommandStaffRules` | static 艦隊指揮班＝提督・副提督・参謀の3ネームド（#885 CMD-2/3/4・唯一の窓口）。**配置(CMD-2・階級ゲート#14)**：`CanAssignCommander`(requiredTier以上)/`CanAssignVice`(提督が居て提督以下の階級・別人)/`CanAssignChief`(提督/副提督と別人)＋`AssignVice`/`AssignChief`。**能力反映(CMD-3・実効値パターン・基準値非破壊)**：`EffectiveLeadership`/`EffectiveDefense`(提督＋副提督の補佐＝二重指揮の冗長性・`viceAssistRatio` 既定0.25)／`EffectiveOperation`/`EffectiveIntelligence`(提督＋参謀の底上げ・`chiefAssistRatio` 既定0.20)、上限 `AdmiralData.MaxStatValue`。**継承(CMD-4)**：`NeedsSuccession`/`PromoteVice`(提督空席で副提督昇格)/`Succeed`(昇格＋空いた副提督席を候補プールから補充・適任不在は空席＝無痛補充にしない)。`AdmiralData.staffOfficers`(提督自身の幕僚)は別系統で併存。 |
| `FleetRoster` | static 艦隊編制台帳（#146・オーダー・オブ・バトル）。勢力ごとに番号→`FleetUnitData` を解決。`GetFleet`/`AllFleets`/`NextAvailableNumber`(解隊は再利用・永久欠番は飛ばす)/`CreateFleet`/`Register`/`AssignAdmiral`/`Unassign`/`ReassignAdmiral`/`CanAssign`(階級ゲート＝`AdmiralData.rankTier`≥requiredTier・#14)/`Disband`(番号再利用可)/`RetireNumber`(永久欠番)/`IsRetired`/`Clear`。番号体系は勢力ごとに独立。**`FleetRegistry` を流用・拡張しない**。`BattleSetup` が `Awake` で `Clear`、`ScenarioData.FleetEntry.fleetNumber>0` の艦隊を登録＋配属。 |
| `MilitaryFormation` | 上位梯団ノード（#147・軍団/軍集団）。`[Serializable]`。`id`/`name`/`echelon`(`EchelonType{艦隊,軍団,軍集団}`)/`faction`/`commander`(配属司令)/`parentId`/`childFormationIds`(下位梯団)/`fleetNumbers`(直下の艦隊＝#146番号)＋`HasCommander`/`DisplayName`。艦隊そのものは #146 `FleetUnitData`/`FleetRoster` が持ち、ここは番号で参照（別レジストリを作らない）。木構造の管理は `OrderOfBattle` が唯一の窓口。 |
| `OrderOfBattle` | static 編制ツリー台帳（#147・軍集団⊃軍団⊃艦隊#146）。**①司令部固定・中身流動**＝`AttachFleet`/`DetachFleet`(艦隊を梯団へ・単一所属で中身流動。**戦闘×非戦闘の混成は拒否#883**＝`CanAttachFleet`/`ShipRoleRules`)/`AttachFormation`/`DetachFormation`(下位梯団・循環防止・単一親)。**梯団別の司令配属＝階級ゲート#14**＝`RequiredTier`(艦隊7中将/軍団8大将/軍集団10元帥)/`CanCommand`/`AssignCommander`/`UnassignCommander`。集計＝`AllFleetNumbersUnder`/`CountFleetsUnder`(ツリー再帰)。`Create`/`GetOrCreate`(同勢力同echelon同名を解決orつくる＝シナリオ構築用)/`Get`/`AllFormations`/`Clear`。#146 を木構造へ拡張（`FleetRegistry` とも別物）。**`BattleSetup` が `Awake` で `Clear`、`ScenarioData.FleetEntry.corps`/`armyGroup` から梯団ツリーを構築**（`FleetStrength.corpsName`/`armyGroupName`＋`EchelonPath` で HUD/`FleetDetailPanel` に「軍集団 ⊃ 軍団 ⊃ 第N艦隊」表示）。②直轄投資・③任務戦術は後段。 |
| `GovernanceRules` | static 内政ロジック（#109 P-1/P-2・純ロジック test-first）。安定度は目標値へ収束：目標＝基準＋思想一致(±)−戦時−補給不足−(未統合ぶんの占領不満)。占領直後は `integration`=0 で不満最大→時間で統合→安定回復。`Tick(province,ownerData,supplyOk,atWar,dt)`／`EquilibriumStability(integration,ideologyMod,supplyOk,atWar)`(純関数)／`OnOccupied(province)`(統合リセット＝攻城 `ApplySiegeResult` から呼ぶ想定)／`OutputFactor(province)`(安定度比例＝支配≠即産出)／`IsUnrest`/`RebelPressure`/`IdeologyModifier`。調整値は const に集約。**内政の三層分担＝ハイブリッド(#767)**：単一の真実は**惑星 `Province`**（下位）、**星系＝惑星の集約ビュー**＝`AggregateSystem(planets)`→`SystemGovernance`(人口加重の安定度/統合度・人口最多の支配思想・Σ産出・反乱最悪値)、**勢力＝薄い国策レイヤー**（`ownerData` の思想/政策を modifier として下位へ波及）。星系・勢力は固有データを重複して持たず集約する＝同期バグなし・微操作回避（タイクン回避）。 |

## 壊すと不具合になる依存・命名
- 旗艦の子オブジェクト名は固定。`FactionColor`/`FleetStrength.Flash`/`Squadron` がこれらの名前で除外・再利用判定する。**変えない・重複生成しない**：
  - `"SelectionRing"`（選択リング。陣営着色・配下艦集計から除外、黄色固定）。
  - `"StrengthDisplay"`（兵力ラベル。`FleetStrength` が実行時生成する legacy `TextMesh`）。
  - `"MoraleLabel"`（士気ラベル。`FleetMorale` が生成）。
  - `"WeaponArcLine"`（射界線。`WeaponArc` が生成。`FleetWeapon` のビーム用 LineRenderer と別物）。
  - `"FlagshipMarker"`（旗艦識別マーカー。`FlagshipMarker` が生成する子 SpriteRenderer。固定の金色＝`FactionColor` も `FleetStrength.Flash` も着色対象から除外する。`Squadron` の配下艦自動収集もこの名前を除外する）。
  - `"FlagshipMarkerGlow"`（旗艦の発光ハロー。`FlagshipMarker` が `"FlagshipMarker"` の子として生成する SpriteRenderer。**`FactionColor` だけは陣営色で塗る**が、`FleetStrength.Flash`(被弾フラッシュ)・`Squadron` の配下艦スプライト流用元探索は**除外**する。`Squadron.CollectExistingMembers` は直下の子のみ走査するため入れ子のハローは元から対象外）。
- 旗艦と配下艦の見分けは「`FlagshipMarker` のマーカー」＋「`Squadron.memberScale` による配下艦の縮小」で行う。**旗艦 root のスケールは変えない**（`Squadron` の陣形計算が `TransformPoint` を使うため、root 拡大で陣形間隔が狂う）。
- `combatMobilityRatio` は `FleetWeapon` にある。`FleetMovement` が `IsInCombat` と共に読む。移動側に重複定義しない。
- 選択中艦隊の唯一の窓口は `FleetCommander.SelectedFleets`。別の場所に選択管理を新設しない。
- シーン間で共有する設定・戦績は `GameSettings.Instance` の一択。別の保管所を作らない。
- `Squadron.memberShips` は Inspector で明示割り当て推奨（未設定だと上記マーカー名を除く全ての子を配下艦扱いにする）。
- Esc の優先順位は「`CommandMenu` を閉じる ＞ 移動/攻撃目標指定キャンセル ＞ ポーズメニュー」。`PauseManager` は `CommandMenu.IsOpen`/`FleetCommander.IsWaitingForMoveTarget`/`FleetCommander.IsWaitingForAttackTarget` を見て譲る。崩さない。
- シーン名 `"Title"`/`"Battle"`/`"Result"` と Build Settings の登録を一致させる。
- `ScenarioData.scenarioName` と `GameSettings.scenarioName` を一致させる（`BattleSetup` が名前一致で解決）。会戦アセットは `Resources` 配下に置く。

## 既知の重複・将来の整理対象（新規はここに寄せる・増やさない）
- **キーバインド/入力は `GameInput`(static) に集約（#107）**：各クラスは `Keyboard.current` 直読みをやめ `GameInput.WasPressed(GameAction)`/`IsHeld(...)` で問い合わせる。キー定義は1か所(`Bindings`)＝`GameAction`(論理アクション)×`InputContext`(共通/タイトル/戦略/会戦)×修飾キー。**シーンの管理クラスが `SetContext` で絞る**＝同キーがシーン違いで衝突せず、`ActionsInContext` で各シーンの操作一覧をヘルプに出せる（迷わない）。`FindConflicts`(衝突検出・EditModeで担保)／`KeyLabel`(HUD表示)。#83=Ctrl+数字は修飾キーで倍速と分離。**移行済み**＝`HelpOverlay`(H)・`BattleManager`(R/Backspace/SetContext会戦)・`PauseManager`(Space/1-3/Esc)・`CameraController`(WASD/矢印/F)・`TitleManager`(SetContext タイトル)・`GalaxyView`(SetContext 戦略)。**未移行（段階移行）**＝`FleetCommander`(クリック/Esc・Esc優先順位チェーン核のため慎重に移行)・`GalaxyView`(Space/1-3/I の直読み・新規 GameAction が要る)。新規入力は必ず `GameAction` に足してここで読む。Esc優先順位チェーン（CommandMenu>キャンセル>ポーズ）は各consumer側に維持。リバインドUI(#20/#87)の土台。HelpOverlay のキー行は `keyboardSections` 配列から自動生成（#107 D）＝新アクション追加で自動反映。
- **SO索引/名前解決は `ContentDatabase`(static) に集約（FND-1 #496・#495）**：`FactionByName`/`ScenarioByName`/`AdmiralByName`/`AllFactions`/`AllScenarios`＝`Resources.LoadAll` を一度走査して索引化（`EnsureBuilt`／テストは `Register*`+`Clear` で走査回避）。`ScenarioData.Resolve`・`CampaignSaveManager`・`BattleSetup`（シナリオ解決）・`TitleManager`（シナリオ/勢力列挙・名前解決）まで**集約済み（#496 完了）**。新規の SO 名前解決は必ずここを使う（`Resources.Load` を増やさない）。フォント/テクスチャ等の非SOアセットは対象外（従来どおり `Resources.Load`）。
- 陣営色は **`FactionData.color` が唯一の出所**（`FactionColor`/`FleetHUDManager` は `FactionData` があればその色、無ければ enum 既定色＝`imperial/alliance/empire/allianceColor` にフォールバック）。新たな陣営色のハードコードを増やさない。enum 既定色は後方互換のフォールバック専用。
- `ChangeFormation(int)` の実体は `FleetCommander.ChangeFormation(int)` に一本化済み（`CommandMenu`＝委譲＋`CloseMenu`、`FleetHUDManager`＝委譲）。新規の陣形変更もここに集約する。
- 敵探索は `FleetRegistry`（単一在庫）＋`FactionRelations.IsHostile` に集約済み。新たに `FindObjectsByType` での索敵や「`faction` 違い＝敵」の直書きを増やさない（敵対判定は必ず `FactionRelations` 経由）。`BattleSetup.ClearExistingFleets` の開始時除去のみ `FindObjectsByType` 例外＝登録前の手置き艦を拾うため。配下艦の発砲・索敵は `fireInterval` 間隔＋初回位相をランダムにずらして全艦同時更新を避ける。
- **外交は `FactionRelations.IsHostile` を駆動する（#189・DIP-1 入口）**：`DiplomacyState`(勢力ペア→opinion＋外交状態enum`{平時/同盟/不可侵/属国/交戦}`・直列化可)＋`DiplomacyRules`(static＝状態遷移`SignTreaty`/`BreakTreaty`/`DeclareWar`/`MakePeace`・opinion修正子`TargetOpinion`/`DriftOpinion`・写像`HostileOverride`/`IsHostile`・提案可否`CanProposeAlliance`)。**並行する敵対システムを作らない**＝`FactionRelations.ActiveDiplomacy`(static・既定null) に張れば、交戦→敵対/同盟・不可侵・属国→非敵対が `IsHostile` を上書き、平時/レコード無し/null は従来の FactionData/enum 判定にフォールバック（**後方互換の核**）。新たな外交状態の直書きを増やさずここへ集約する。条約データ(`TreatyData`)/casus belli/戦争目標は今後 DIP-2/3 で足す。
- **ZOC（支配領域）判定は `ZoneOfControl`(static) が唯一の窓口(#81)**：`CanProject`(退却=IsAlive false・敗走=IsRouted でZOC消失)／`GetRadius`(外接円×`FleetMovement.zocRadiusScale`)／`HostileIntensityAt`(敵ZOC最深侵入度0..1)／`SteerAround`(AI回避の目標補正)。半径倍率等の調整値は各艦の `FleetMovement` に持たせ `ZoneOfControl` はそれを読むだけ（重複パラメータを増やさない）。列挙は `FleetRegistry.AllFlagships`＋`FactionRelations.IsHostile`。新たなZOC判定の直書きを増やさずここへ集約する。
- 日本語フォント読み込みは `FontProvider.JapaneseFont`(static) に集約済み（`FleetStrength`/`FleetMorale`/`DamagePopup` が参照）。新規の legacy TextMesh も必ずここを使う。
- ビーム描画は `BeamFx`(static) に集約済み（`FleetWeapon`/`EscortShip` が参照）。LineRenderer のマテリアル/幅カーブ/グラデ/フェードをここで一元化。新規のビーム表現も `Sprites/Default` 直書きを増やさずここに寄せる。
- **戦闘・移動の係数公式は `CombatModifiers`(static・Core) ＋ `ModifierStack`(struct) に集約（#106）**：提督能力→倍率 `AbilityFactor(stat)=1+(stat-50)/100`（攻撃/機動共通）・防御→被ダメ倍率 `DefenseDamageFactor(def)=1-Clamp(def/200,0,0.9)`・側背面 `FlankFactor(dot,min,out isFlank)=Lerp(min,1,(dot+1)/2)`(閾値 `FlankHitThreshold`=1.3)。倍率合成は `ModifierStack`(基準1.0→`Mul`で積む→`Value`/`ClampMin(min)`・ゼロ確保)。`FleetMovement.GetMobilityFactor`/`ShipCombat.ComputeDamage`/`FleetStrength.TakeDamage` が消費。**新規の係数(#72陣形特性/#104得意陣形/提督スキル/成長等)はインライン計算を増やさず CombatModifiers に公式を足し ModifierStack で積む**。公式は `CombatModifiersTests` が従来式と厳密一致で固定（EditMode/TestHarness）。

## UI Toolkit 段階移行（#UI統一・見切れ防止）
- 新規/複雑なUIは **UI Toolkit（UITK）** へ段階移行する（既存の uGUI コード生成パネルは順次置換）。flexbox＋`ScrollView` でレイアウト自動＝**見切れが原理的に起きない**、テーマ USS で**統一**。
- 基盤＝`GineiUITK`(static)：`Attach(host, sortingOrder, out root)` で `UIDocument`＋`PanelSettings` を配線し、共通テーマ `Resources/GineiTheme.uss` と日本語フォントを適用したルートを返す。UIは VisualElement/Label/Button/ScrollView を C# で組む（コード生成・自動生成の方針は uGUI 時代と同じ）。
- **★ランタイムUITKには `PanelSettings` アセットが1つ必須**：Project で Create → UI Toolkit → Panel Settings Asset を作り、`Assets/Resources/GineiPanelSettings` に置く（名前固定・テーマは既定で可）。無いと `GineiUITK.Attach` が警告し描画されない。
- 日本語フォントは `GineiUITK.ApplyJapaneseFont`＝`Resources/JapaneseFont_TMP`(FontAsset) を優先、無ければ `FontProvider.JapaneseFont`(legacy Font) にフォールバック。
- パイロット＝`OrderOfBattlePanel`（編制管理UI）。以後この型に倣って他パネルを移行する。

## 日本語表示の注意
- 頭上ラベル/ダメージ表示は legacy `TextMesh`：実行時フォントは **`FontProvider.JapaneseFont`** が解決する（唯一の窓口）。中身は `Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")`。**Unity6 で `"Arial.ttf"` は廃止され例外を投げる**（過去に一瞬で艦隊が消えるバグの原因）。エディタ内では `Assets/Fonts/msgothic.ttc` があれば優先。
- UI の TMP は `Resources` の `"JapaneseFont_TMP"`(TMP_FontAsset) をフォールバックに使う。

## NG リスト
- 旧 Input API、3D前提コード（全軸Quaternion回転・zを使った移動）、Inspector非公開のハードコード。
- 基準値を直接上書きする一時的な能力変更（実効値パターンを使う）。
- 陣営色・陣形変更ロジックの重複実装、`FleetCommander` 以外への選択管理新設、`GameSettings` 以外へのシーン間状態の新設。
- シングルトン（`GameSettings`/`AudioManager`/`SceneLoader`）の重複生成・別実装。
- 固定の子オブジェクト名（`SelectionRing`/`StrengthDisplay`/`MoraleLabel`/`WeaponArcLine`）の変更や二重生成。

## テスト基盤（EditMode）
- アセンブリ定義は **4分割（FND-1 #496）**：`Ginei.Core`(`Assets/Scripts/Core`＝純ロジック・SO型・enum。**MonoBehaviour/シーン/シングルトン非依存**。UnityEngine 参照は可)／`Ginei.Data`(`Assets/Scripts/Data`＝`ContentDatabase`/`ScenarioData`/`SaveManager`/`CampaignSaveManager`＝データアクセス・IO層。Core を参照)／`Ginei.Game`(`Assets/Scripts/Game`＝MonoBehaviour・シーン・UI。Core/Data を参照)／`Ginei.Editor`(`Assets/Editor`・全部参照)／`Ginei.Tests.EditMode`(`Assets/Tests/EditMode`、Core/Data/Game＋`nunit.framework`参照、`defineConstraints: UNITY_INCLUDE_TESTS`)。依存は **Core←Data←Game の一方向・循環厳禁**。**新規ファイルは置き場所で所属が決まる**：純ロジック→`Core/`・SOアクセス/セーブIO→`Data/`・MonoBehaviour/UI→`Game/`。Core から `GameSettings`/`AudioManager`/`SceneLoader`/`FleetRegistry` 等の Game 型を参照しない。
- **純ロジック（非 MonoBehaviour）は test-first**＝Test Runner(EditMode) で担保する：`RankSystem`/`FactionData`/`FactionRelations`/`AdmiralData`(命名)/`ProtagonistRules`/`DamagePopup`(スタイル)＋戦略レイヤー(`GalaxyMap`/`StrategicFleet`/`GalaxyPathfinder`/`StrategicFleetRegistry`/`StrategyRules`)＋惑星攻城(`PlanetSiegeRules`/`Planet`)＋内政(`GovernanceRules`/`Province`)＋入植(`ColonizationRules`・#129)＋造船(`ShipyardRules`/`Shipyard`/`BuildOrder`・#884)＋戦時兵站(`ResourceProductionRules`/`SupplyRules`/`CommerceRaidingRules`/`ResourceStockpile`・#92 L-1〜3)＋財政経済(`FiscalRules`/`FiscalState`＋再分配`RedistributionRules`/`TaxStructure`・#163 PB/国債/金利/為替/税/社会保障/階級別負担)＋艦隊編制(`FleetRoster`/`FleetUnitData`)＋編制ツリー(`OrderOfBattle`/`MilitaryFormation`)＋役割混成禁止(`ShipRoleRules`・#883)＋指揮班3ネームド(`CommandStaffRules`・#885)＋イベントエンジン(`EventRules`/`EventEngine`/`GameEventDef`・#116)＋開示エンジン(`DisclosureRules`/`DisclosureLedger`/`DisclosureEntry`・FND-4 #495)＋戦役セーブ(`CampaignSerializer`/`CampaignSaveData`・FND-2 #495)＋SO索引(`ContentDatabase`・FND-1 #496)＋入力集約(`GameInput`・#107＝衝突検出/コンテキスト)＋外交(`DiplomacyRules`/`DiplomacyState`・#189 DIP-1＝外交状態enum×opinion修正子×状態遷移→`FactionRelations.IsHostile` 駆動)＋**社会・政治シミュ層**：関ヶ原型(`LoyaltyRules`/`Allegiance`・#817＋会戦配線`BattleAllegianceRules`＝遷移収集/寝返りロック/静観退き)／継承(`SuccessionRules`/`Organization`・#812)／合意(`ConsentRules`/`Polity`・#836)／希望・末人(`HopeRules`/`Community`・#852)／王朝サイクル(`DynastyRules`/`Regime`・#867)／物流(`LogisticsRules`・#844)／非暴力(`NonviolenceRules`/`Movement`・#831)／三密(`FocusRules`・#872)／人物・適材適所(`PersonRules`/`Person`＝軍人/文民の役割×役職・正名 #866)／**政府役職層**(`OfficeRules`/`Office`/`GovernmentRegistry`/`ICharacter`＝役職・任命・提案権限 GOV-1/3・`CivilianControlRules`＝文民統制/クーデター GOV-4・`MinistryRules`/`Ministry`＝省庁ツリー GOV-5・`PartyRules`/`Party`＝政党/最小選挙 GOV-6・`LeadershipElectionRules`＝総裁選 GOV-7)／**人物ライフサイクル層**(`LifecycleRules`/`Calendar`＝年齢/死亡 LIFE-1/2・`VacancyRules`＝後任補充 LIFE-2・`DemographicsRules`/`Population`＝人口ボーナス/オーナス LIFE-3・`CaptivityRules`＝捕虜化/処遇 LIFE-4・`SeniorityRules`＝席次vs実力 LIFE-5/6・`CareerPipelineRules`＝出自パイプライン武/官/技 LIFE-5/6/7)／**統合層**(`FactionStateRules`/`FactionState`＝部品の合成・`FactionLoyaltyRules`＝国家↔諸侯・`CampaignRules`/`CampaignState`＝盤面×社会の最上層)／**群像・政治の純ロジック追加（Wave1・配線待ち）**：成長(`GrowthRules`/`Growth`・#537-543＝経験→実効能力。4アーキタイプ{首席型/在野俊英型/老練型/叩き上げ}の成長曲線＝`ForArchetype`/`GainExperience`/`EffectiveStatBonus`。基準非破壊・上限`AdmiralData.MaxStatValue`)／退役(`RetirementRules`＋enum`ServiceStatus`{現役/予備役/退役}・#530-536＝階級別停年`MandatoryRetirementAge`/アップオアアウト`ShouldUpOrOut`/元帥終身`IsMarshalTenure`/戦時召集`CanRecall`/恩給`PensionFactor`)／提督パッシブスキル(`AdmiralSkillRules`/`AdmiralSkill`・#137-140＝enum`SkillEffectType`×`SkillCondition`{常時/劣勢時/交戦時/側背面時}・条件成立スキルの修正子のみ返す`EffectiveMultiplier`/`EffectiveBonus`＝実効値パターン・将来#106へ合流)／自律分散(`AutonomyRules`＋enum`CommandDoctrine`{集団依存/自律分散}・#544-550＝`DoctrineFactor`/創発シナジー`EmergentSynergy`(全員好成績時のみ加算)/傑物前提`IsFunctional`(高能力×高autonomyのみ機能)/信頼vs結束`TrustVsCohesion`)／軍功授爵(`MeritRankRules`・#900-905始皇帝モデル＝戦功→爵位`MeritToTier`/インセンティブ士気`IncentiveMoraleBonus`(QIN-2制度駆動)/制度の畏怖`InstitutionalAwe`(QIN-3)/法家の罠`ExtractiveDecay`(QIN-5短期最強・長期崩壊))／宗教(`ReligionRules`/`Religion`・#172-175＝改宗圧力`ConversionPressure`/均衡収束`Tick`(GovernanceRules流儀)/異端`IsHeresy`/社会効果`SocialEffect`/聖戦圧力`HolyWarPressure`)／文化・民族(`CultureRules`/`Culture`・#194＝同化`AssimilationPressure`/`Tick`・分離独立`SeparatismRisk`・ナショナリズム`NationalismFactor`・亡命`ExileLikelihood`(決定論roll))／市場経済(`MarketRules`/`Good`/`Market`＋enum`GoodType`・#179-182＝需給均衡価格`ClearingPrice`/`Tick`(自動均衡)・生活水準`StandardOfLiving`・SoL→支持`SoLToSupport`(経済が革命を生む))／正義の天秤(`JusticeRules`＋enum`JusticeView`{功利主義/ロールズ/リバタリアン/アリストテレス/共通善}・サンデル#918-923＝各正義観の是認`Approval`/住民構成への加重和→正統性`LegitimacyDelta`/最も不満な正義観`DominantGrievance`(イベント火種))。**いずれも盤面/UI未配線＝純ロジックのみ（後方互換）。Game層型は不参照、`Province`/`AdmiralData`/`RankSystem`等はread-only**。／**政治・経済・群像・世界観の純ロジック追加（Wave2・配線待ち）**：立憲主義(`ConstitutionRules`/`Constitution`・#170＝制約権力`ConstrainedAuthority`/権利→正統性`RightsLegitimacy`/`IsConstitutionalMonarchy`)／封建制(`FeudalRules`/`Fief`・#168/#169＝徴募`LevyContribution`/反乱`VassalRebellionRisk`/門地開放`MonopolyOpening`)／クーデター(`CoupRules`＋enum`CoupType`{軍部/宮廷/革命}・`CoupOutcome`{成功/粛清/内戦}・#215-219＝成功率`CoupSuccessChance`/`Resolve`(roll)/`PostCoupLegitimacy`)／寡頭支配(`PowerRules`/`PowerActor`・#164＝実権`EffectivePower`/`IsPuppet`/`IsShadowRuler`)／秘密警察(`SecurityRules`/`SecurityApparatus`・#166＝`DissentSuppression`/`CoupDetectionChance`/`RepressionSupportPenalty`)／三権分立(`SeparationOfPowersRules`＋enum`Branch`・#171＝`CheckBalance`/`TyrannyRisk`/`IsGridlocked`)／外交条約(`TreatyRules`/`Treaty`＋enum`TreatyType`・DIP-2 #191＝`OpinionEffect`/`Leverage`/`IsBreach`。DiplomacyState非編集)／戦争目標(`WarGoalRules`＋enum`CasusBelli`・DIP-3 #192＝厭戦`WarWeariness`/`GoalLegitimacy`/講和`PeaceAcceptance`)／資本(`CapitalRules`/`CapitalState`・ピケティ#917＝r>g集中`ConcentrationDrift`/累進資本課税/格差→反乱`InequalityUnrest`)／株式市場(`StockMarketRules`/`Company`・#185＝`FairPrice`/`DividendYield`/`CrashRisk`/`Tick`)／銀行信用(`BankRules`/`Bank`・#186＝信用創造`CreditCreation`/取付け`BankRunRisk`/`IsInsolvent`)／研究(`ResearchRules`/`ResearchProject`＋enum`ResearchField`・#123-127＝`ResearchOutput`/`Tick`/政体偏り`IdeologyBias`)／継承法(`SuccessionLawRules`＋enum`SuccessionLaw`{長子/分割/指名/選挙}・PDX-1 #646＝`HeirShare`/`SuccessionCrisisRisk`)／政略結婚(`MarriageRules`/`MarriageAlliance`・PDX-2 #647＝`AllianceBond`/請求権継承`ClaimInheritance`)／諜報(`EspionageRules`/`SpyNetwork`・決定論roll＝`MissionSuccessChance`/`InfoGain`/`DetectionRisk`/`SabotageEffect`)／ゲーム理論(`GameTheoryRules`＋enum`Move`{協調/裏切り}・#388＝利得表`Payoff`(囚人のジレンマ既定)/`NashEquilibrium`/`TitForTat`/`IsZeroSum`)／マズロー欲求(`NeedsRules`＋enum`NeedLevel`6段・#403＝`DominantNeed`(下位優先)/`Motivation`/`MoraleContribution`)／マグナカルタ(`MagnaCartaRules`/`Charter`・#624＝制約権力`EffectiveRoyalAuthority`/課税同意`TaxRequiresConsent`/抵抗権`ResistanceTriggered`/慣習法化`HabituationDrift`)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**時間・編成・通知（2026-06-10・配線済み）**：統一時間(`GameClock`/`GameDate`/`AutoBattleSim`・TIME-1〜4)＋暦粒度Tick(`CalendarTick`/`CalendarDispatcher`・TIME-6)＋自動スロー(`TimeFlowRules`・TIME-7)／加齢オーケストレーション(`AnnualLifecycleRules`・LIFE-2)／艦隊プール(`FleetPool`/`FleetPoolRules`＋造船供給`ShipyardRules.CommissionToPool`・#148/#884)／通知(`NotificationCenter`・NOTIF-1)。**これらは Game へ配線済み（GalaxyView/BattleManager/各HUD）**。／**戦術・戦略の現実化の純ロジック追加（Wave3・配線待ち）**：偵察・戦場の霧(`ReconRules`＋`ReconParams`・#119＝偵察精度で推定誤差`ErrorFraction`/点推定`EstimateStrength`(roll決定論)/信頼区間`EstimateBand`/探知`DetectionChance`(距離×精度×ステルス)・真値非破壊)／名声(`ReputationRules`/`Reputation`＝勝敗で名声増減`RenownAfterBattle`(格上撃破ほど跳ねる)/士気`MoraleBonus`/徴募`RecruitmentBonus`/敵威圧`IntimidationFactor`/平時減衰`Decay`)／世論戦(`PropagandaRules`/`PropagandaState`＝効果`Effectiveness`(到達×信用×主張×(1−検閲))/支持綱引き`SupportShift`/信用浸食`UpdateCredibility`(乖離が信用を蝕む)/製造された合意`IsManufacturedConsent`)／回廊封鎖(`BlockadeRules`＋`BlockadeParams`＝戦力比で通過率`Throughput`(線形に締まる)/封鎖判定`IsBlockaded`/備蓄増減`StockpileDelta`/突破`RunnerSuccessChance`。通商破壊#95＝個別船団とは別系統の面のarea-denial)／回廊要塞(`FortressRules`/`Fortress`・イゼルローン型＝シールド比例の防御倍率`DefenseMultiplier`/実効防御`EffectiveDefense`/主砲`MainGunDamage`(シールド健在下のみ)/通過封鎖`BlocksPassage`/力攻め比に満たない難攻不落`IsImpregnable`。攻城#131=惑星`PlanetSiegeRules`とは別系統)／傭兵(`MercenaryRules`/`MercenaryBand`＝給与で忠誠`LoyaltyAfterPay`/離反確率`DefectChance`(roll決定論)/解散`WillDisband`/維持費`Upkeep`/戦闘信頼性`CombatReliability`。理念でなく金で雇う戦力)／星間地形(`TerrainRules`＋enum`SpaceTerrain`{通常宙域/星雲/小惑星帯/重力圏/恒星近傍}＝地形別の探知/機動/命中/遮蔽倍率`Profile`/`DetectionFactor`/`MovementFactor`/`AccuracyFactor`/`CoverBonus`/守備有利指標`DefensiveBias`・基準値非破壊)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**軍政・人的資源の純ロジック追加（Wave4・配線待ち）**：徴募(`ConscriptionRules`・L-4 #96＝`Population.working`(徴募源)から上限内で兵力化`DraftCapacity`/`Draft`(人口を実際に削る)/産出減`OutputFactor`/支持減`SupportPenalty`/復員`Demobilize`。人口動態=`DemographicsRules`・艦補充=`FleetPool`とは別)／練度(`VeterancyRules`＋enum`ExperienceLevel`{新兵/一般/精鋭/古参}＝会戦で経験蓄積`GainFromBattle`(激戦比例)/段階`LevelOf`/戦闘倍率`CombatFactor`(eliteXpで上限の線形)/新兵補充の希釈`DiluteOnReinforce`(加重平均＝歴戦は買い戻せない))／修理(`RepairRules`＝応急修理は`fieldRepairCap`まで`FieldRepairTick`/完全回復はドックのみ`DockRepairTick`/所要`TimeToFull`/要否`NeedsDock`。新造=`ShipyardRules`とは別系統)／白兵戦(`BoardingRules`＋enum`BoardingOutcome`{撃退/制圧/膠着}・ローゼンリッター型移乗攻撃＝実効戦力(兵数×技量×健全度)の比で解決`Resolve`/損耗は劣勢側ほど重い`AttackerCasualties`/`DefenderCasualties`/拿捕`PrizeValue`(健全度比例＝壊しすぎると値打ちが落ちる))／戦時動員(`MobilizationRules`＋enum`MobilizationLevel`{平時/部分動員/総力戦}＝軍事/民需の生産倍率`MilitaryFactor`/`CivilianFactor`/長期総力戦の過熱減衰`EffectiveMilitaryFactor`(短期決戦でしか引き合わない)/支持低下`SupportDrain`)／難民(`RefugeeRules`＝戦火比例の流出`Displaced`/受け入れ負担`HostBurden`(未統合分だけ摩擦)/溶け込み`IntegrationTick`/安全な故郷へ帰還`ReturnTick`(統合済みは定住)。占領不満=`GovernanceRules`とは別系統)／軍紀・査問(`DisciplineRules`＝厳格さの引き締め`OrderAfterEnforcement`と士気コスト`MoraleCostOfHarshness`(諸刃の剣)/抗命`InsubordinationRisk`(roll決定論)/人望士官の査問ペナルティ`InquiryMoralePenalty`(ヤン査問会型)/戦闘秩序倍率`CommandEfficiency`(0.5..1)。`FleetMorale`=Game層とは別＝係数算出のみ)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照（`Population`はread-write＝徴募/復員で人口増減）、TestHar/EditModeで担保**。／**会戦後・謀略・政経の純ロジック追加（Wave5・配線待ち）**：追撃戦(`PursuitRules`＝速度比で振り切り`CleanBreak`/苛烈度`PursuitSeverity`/殿軍遮蔽`RearguardScreen`(本隊の2割で最大)/本隊損害`MainBodyLosses`/殿軍の犠牲`RearguardLosses`。会戦後の損害解決のみ＝`FleetAI`撤退とは別)／暗殺(`AssassinationRules`＋enum`AssassinationOutcome`{失敗/成功/露見}・地球教型＝成功率(浸透×(1−警護))/1roll三分岐`Attempt`/継承ショック`SuccessionShock`(重要度×(1−制度化)＝制度は英雄を殺しても止まらない)/露見の正統性損失。死亡処理=`LifecycleRules`・諜報網=`EspionageRules`へ委譲)／経済制裁(`SanctionsRules`＝実効ペナルティ`OutputPenalty`(強度×(1−抜け穴))/自己コスト`SelfCost`(交易依存比例)/抜け穴成長`LeakageTick`(長引くほど形骸化)/有効判定`IsEffective`。軍事封鎖=`BlockadeRules`とは別＝平時の経済戦)／出兵の政治(`WarPoliticsRules`・アムリッツァ型＝出兵誘因`WarIncentive`(低支持×選挙近接)/勝利回復`SupportAfterVictory`/敗北崩落`SupportAfterDefeat`(規模×損耗)/政権崩壊`GovernmentFalls`/博打の期待値`ExpectedSupportSwing`)／災害(`DisasterRules`＋enum`DisasterKind`{疫病/飢饉/天災}＝救援緩和`ReliefFactor`/人口・安定度被害/対応→正統性`LegitimacyDelta`(救援+・放置−×深刻度)/疫病ロジスティック増殖`EpidemicTick`。平時動態=`DemographicsRules`とは別)／教育(`EducationRules`＝学校の質`SchoolQualityTick`(投資で漸近・無投資で減衰)/人材の質`TalentQualityTick`(世代遅延で追従＝今日の削減は20年後に祟る)/研究・産出ボーナス/教育負債`HasEducationDebt`。研究=`ResearchRules`とは別)／艦齢(`ShipAgingRules`＝性能`PerformanceFactor`(寿命内満額→漸減・下限)/維持費`UpkeepFactor`(単調増)/更新需要`NeedsReplacement`/`ReplacementAge`/艦隊加重平均`FleetPerformanceFactor`。損傷回復=`RepairRules`とは別＝直しても戻らない経年)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**治安・外交・名誉の純ロジック追加（Wave6・配線待ち）**：宇宙海賊(`PiracyRules`＝治安の空白×流量で湧き`PiracyTick`(略奪で複利に肥える)/交易被害`TradeLossRatio`/封じ込め`IsContained`。国家の通商破壊=`CommerceRaidingRules`とは別＝無主の暴力)／星間交易(`TradeRules`・フェザーン型＝総利得`TotalGain`(補完性増幅)/取り分`ShareOfGain`(交渉力)/仲介口銭`BrokerProfit`(断絶時のみ＝戦争が仲介者を太らせる)/断絶損失`WarDisruptionLoss`/依存判定`IsDangerouslyDependent`。域内市場=`MarketRules`とは別)／戒厳令(`MartialLawRules`＝即効鎮静`UnrestTick`/継続コスト`LegitimacyCost`/`HopeCost`(限界超過で倍速)/自傷判定`IsOverstaying`/解除推奨`ShouldLift`。秘密警察=`SecurityRules`(恒常・隠密)とは別＝公然・時限)／儀礼(`CeremonyRules`＝効果は盛大さ×情勢`LegitimacyEffect`/`MoraleEffect`・敗勢下の豪華な式典は空疎`IsHollow`＝逆効果/費用対効果`Efficiency`)／亡命政権(`GovernmentInExileRules`＝持ち出し正統性`InitialExileLegitimacy`(目減り)/忘却`LegitimacyTick`(承認国が遅らせる)/抵抗支援`ResistanceSupport`(×故地の不満)/帰還条件`ReturnViable`(不満＞新支配の安定))／栄典(`HonorsRules`＝価値の半減期カーブ`AwardValue`(乱発インフレ)/士気・忠誠ボーナス/形骸化`IsDebased`/一括授与の限界効用逓減`BatchMoraleTotal`。軍功爵位=`MeritRankRules`とは別＝名誉の経済)／捕虜交換(`PrisonerExchangeRules`＝階級価値`PrisonerValue`(tier比例)/釣り合い`OfferRatio`/受諾`Acceptable`(切迫が許容を広げる)/成立`DealStruck`(相互受諾・タダ取り拒否)。個別処遇=`CaptivityRules`へ委譲)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**戦術深化の純ロジック追加（Wave7・配線待ち）**：伏兵(`AmbushRules`＝成功率(秘匿×(1−警戒))/初撃倍率`FirstStrikeFactor`/隊形不備の時間回復`VictimCombatFactor`(下限0.5)/士気ショック。探知=`ReconRules`の結果を入力に取る)／包囲(`EncirclementRules`＝包囲度`Coverage`/士気減`MoraleDrain`/降伏`SurrenderChance`(完全包囲＋低士気のみ＝逃げ道がある限り0)/突囲損害`BreakoutCasualtyRatio`。個人捕虜化=`CaptivityRules`へ委譲)／陽動(`FeintRules`＝信憑性`Credibility`(規模×(1−敵情報))/見破り`SeenThrough`/吸引`DrawnForce`/本命の手薄化`MainFrontWeakening`/露見損害`ExposedLosses`＝張った芝居の代金)／機雷原(`MinefieldRules`＝通過損害`TransitLossRatio`/減速`SpeedFactor`/掃海と敷設の綱引き`DensityTick`/所要`TimeToClear`＝血で払うか時間で払うか。艦隊封鎖=`BlockadeRules`とは別＝無人の地帯拒否)／艦載機(`CarrierRules`・ワルキューレ型＝打撃`RawStrikePower`/防空の削り`InterceptedRatio`/実効打撃`EffectiveStrike`/出撃損耗`SortieLosses`/母艦喪失の宿無し`OrphanedLosses`。`ShipClass`には足さず独立系統)／通信(`CommunicationsRules`＝遅延`CommandDelay`(距離×妨害増幅)/途絶`IsCutOff`/命令の鮮度`OrderFreshness`(半減期×戦況テンポ)/指揮実効度`CommandEffectiveness`(途絶時は自律性が頼り＝`AutonomyRules`をread-only))／電子戦(`ElectronicWarfareRules`＝実効妨害`EffectiveJamming`(出力−ECCM)/命中・探知低下/被逆探知`EmitterExposure`(ECCMでは消えない)/損益`WorthJamming`＝沈黙か優位か。環境=`TerrainRules`・自前センサー=`ReconRules`とは別＝能動妨害)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**消耗・諜報戦の純ロジック追加（Wave8・配線待ち）**：脱走(`DesertionRules`＝基礎率＋低士気＋長期摩耗`DesertionRate`/補給切れで倍率/出血判定`IsHemorrhaging`。金銭離反=`MercenaryRules`・抗命=`DisciplineRules`とは別＝無言の損耗)／戦場回収(`SalvageRules`＝残骸プール`WreckPool`/支配度で回収権`ClaimableWrecks`/修理・解体の排他配分`RecoveredStrength`/`RecoveredResources`/保持の利得差`ControlPremium`。拿捕=`BoardingRules`とは別)／作戦立案(`OperationPlanRules`＝**未使用だった`operation`能力の初の使い道**。質`PlanQuality`(能力×準備)/初期条件倍率`InitialAdvantageFactor`(質の差)/補給`SupplyEfficiencyFactor`/接敵後の陳腐化`DecayedQuality`(モルトケ＝計画は敵との接触に耐えない)。参謀補完=`CommandStaffRules`へ委譲)／虐殺(`AtrocityRules`・ヴェスターラント型＝恐怖の即効`ImmediateSuppression`/露出比例の汚点`PerpetratorStain`/敵への宣伝素材`PropagandaValue`/黙認者の隠れた汚点`CondonerHiddenStain`＋証人数で発覚`CondonementExposed`/遅い風化`StainTick`)／防諜(`CounterIntelligenceRules`＝摘発`SweepTick`/`CatchChance`(深い網ほど尻尾)/転向`TurnChance`(低忠誠のみ)/二重スパイの偽情報`DisinformationBias`(上限あり)。攻めの諜報=`EspionageRules`とは別＝守りと毒)／暗号解読(`CodebreakingRules`・ウルトラ型＝解析蓄積`AnalysisTick`(傍受量×解析力)/先読み`ReadChance`/**使用が発覚を招く**`ExploitationDetectionChance`(読めても動かなければ源泉は守られる)/暗号更新で振り出し`CipherReset`。物理探知=`ReconRules`とは別＝信号情報)／レジスタンス(`ResistanceRules`＝強度は未統合の土壌`Intensity`(亡命支援`GovernmentInExileRules`で増幅)/破壊`SabotageLoss`/情報漏れ`IntelLeak`/弾圧`CrackdownTick`(速いが怨恨`CrackdownResentment`で統合後退)vs懐柔`ConciliationTick`(遅いが根治)。統合収束=`GovernanceRules`をread-only)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**権力の闇の純ロジック追加（Wave9・配線待ち）**：テロ(`TerrorRules`・地球教型＝直接被害は小さく恐怖は媒体で増幅`FearSpread`(テロは劇場)/過剰反応コスト`OverreactionCost`(脅威超過分のみ)/弾圧×不満が細胞を生む`CellsTick`/報復の自滅判定`IsSelfDefeating`。要人暗殺=`AssassinationRules`とは別)／摂政(`RegencyRules`・エルウィン・ヨーゼフ型＝幼君の正統性割引`EffectiveLegitimacy`(年齢で回復)/摂政実権の時間成長`RegentPowerTick`/簒奪誘惑`UsurpationTemptation`(実権×野心×成人接近＝今しかない)/円満返上`HandoverChance`。傀儡一般=`PowerRules`とは別＝時限つき代理統治)／住民投票(`PlebisciteRules`・バーラト和約型＝票`VoteShare`(民意＋不公正×動員)/監視団の公正底上げ`EffectiveFairness`/結果の正統性`ResultLegitimacy`(公正×票差)/禍根`IsContested`(僅差×不正)。定例選挙=`PartyRules`とは別)／恩赦(`AmnestyRules`＝和解利得`ReconciliationGain`/正義の不満`JusticeGrievance`/再発`RecidivismRisk`(原因未解決×範囲＝許す前に直せ)/損益`NetStabilityEffect`)／粛清(`PurgeRules`・リップシュタット後型＝統制利得`ControlGain`/人材毀損`TalentLoss`/恐怖の萎縮`FearParalysis`/冤罪`FalsePositiveRatio`(証拠の質)/表面忠誠と本音の乖離`SurvivorProfessedLoyalty`(out 選好偽装)/純効果`NetEffect`＝粛清は基本高くつく。`CoupRules`の粛清結末とは別＝政策としての粛清)／財産没収(`ConfiscationRules`・門閥解体型＝資本逃避`RemainingAssets`(遅延で逃げる)/国庫`TreasuryGain`(執行は漏れる)/反発`EliteBacklash`/投資萎縮`InvestmentChill`/損益`NetEffect`＝弱い門閥から速く狭く。`RedistributionRules`とは別＝一回性の収奪)／扇動政治家(`DemagogueRules`・トリューニヒト型＝訴求力`Appeal`(雄弁×(1+恐怖)+敵作り＝怖がる群衆が燃料)/責任回避`DodgeChance`(説明責任だけが檻)/逃げ切れば無傷`SupportRetention`/統治の静かな浸食`GovernanceErosion`)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**動乱と経済の影の純ロジック追加（Wave10・初の並列7実装・配線待ち）**：殉教(`MartyrdomRules`・ハイネセン/キルヒアイス型＝劇的な死は生前の名声を超える`MartyrIntensity`(最大2.0)/動員`MobilizationBonus`/遺志の継承主張`LegacyClaimStrength`(語りの独占＞故人との近さ)/後継正統性`SuccessionLegitimacyBonus`/遅い風化`IntensityTick`。生者の名声=`ReputationRules`とは別＝死者の力)／内戦(`CivilWarRules`・リップシュタット型＝経済崩壊`EconomicCollapse`(双方が痩せる)/対外無防備`ExternalVulnerability`(投入の1.25倍空く＝外敵の好機)/勝者総取り`VictorConsolidation`(長期戦の勝者は荒野の王)/雪崩`DefectionMomentum`(優勢リードの2乗)。`CoupRules`=短期決着とは別)／人質(`HostageRules`＝価値`HostageValue`(tier×血縁)/譲歩圧力`ConcessionPressure`/処刑の総損失`ExecutionCost`+`ExecutionLeverageLoss`(殺せば材料も外聞も失う＝生かしてこそ価値)/救出`RescueChance`/風化`ValueDecay`。`CaptivityRules`/`PrisonerExchangeRules`へ委譲)／平時移民(`MigrationRules`＝引力`MigrationPull`(経済0.6+自由0.4の格差)/流動`FlowTick`(国境開度で絞る)/頭脳流出の偏り`BrainDrainRatio`/人材移転`TalentTransfer`/閉鎖の不満圧縮`ClosedBorderResentment`(壁は中に不満を溜める)。`RefugeeRules`=戦火とは別＝足で投票)／戦時インフレ(`InflationRules`＝物価`PriceLevelTick`(増発−産出成長＝貨幣数量説簡易形)/実質賃金`RealWageFactor`/見えない税`HiddenTaxRevenue`/不満`DiscontentFromInflation`(率の2乗＝急激ほど非線形に痛い)/制御不能`IsHyperinflation`。`FiscalRules`=国債側とは別＝通貨価値の劣化)／闇市(`BlackMarketRules`＝湧き`MarketSizeTick`(統制×品不足が需要・黙認で複利)/統制の骨抜き`ControlLeakage`(規模で飽和)/取締コスト`EnforcementCost`/市民の命綱`SurvivalValue`/黙認の均衡`ToleranceEquilibrium`/根絶安全`IsEradicationSafe`(配給ほぼ完璧な時だけ)。`MobilizationRules`=統制側とは別＝統制の鏡像)／戦後復興(`ReconstructionRules`＝回復`RecoveryTick`(投資比例)/放置の固定化`OssificationTick`/`IsOssified`(閾値で回復不能)/復興需要`ReconstructionBoom`(荒廃が深いほど限界効用大)/産出`OutputFactor`/所要`TimeToRecover`。`ColonizationRules`=新規入植とは別・焦土(バックログScorchedEarth)と対)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**講和・大計の純ロジック追加（Wave11・並列7・配線待ち）**：賠償金(`ReparationsRules`・ヴェルサイユの罠＝絞殺`EconomicStrangulation`(非線形)/取り分`AnnualPayment`(**ラッファー曲線**＝全力で取るとゼロ)/持続上限の解析解`SustainableBurden`(≈0.577)/復讐主義`RevanchismTick`(寛大閾値以下は風化)/支払不能`DefaultRisk`。`WarGoalRules`=講和受諾とは別＝講和後の長期帰結)／独占(`MonopolyRules`＝吊り上げ`PriceMarkup`(競争域0・超過の2乗)/政治買収`PoliticalCapture`/停滞`InnovationStagnation`/解体反発`BreakupBacklash`(share²＝早い介入は安い)/寡占ドリフト`ShareTick`(規模の自己強化)。`MarketRules`=競争市場とは別＝市場の失敗)／巨大事業(`MegaProjectRules`/`MegaProject`＋enum`MegaProjectKind`{要塞/大シップヤード/遷都/記念碑}・`ProjectStage`{基礎/構造/完成}・**PIL-1 #1090 正仕様**＝資金比例の進捗`ProgressTick`/継続支出`UpkeepDrain`(中断で停止)/発起人死の頓挫判定`SuccessionOnFounderDeath`((1−制度化)×係数のroll＝#812の式の事業適用・頓挫は不可逆)/埋没費用`AbandonmentSunkCost`/種別効果`CompletionEffect`。CalendarDispatcher/FiscalRules/SuccessionRulesへの接続は配線時)／技術伝播(`InnovationDiffusionRules`＝漏出速度`DiffusionRate`(格差×接触×(1−機密遮断上限0.95)＝接触があれば必ず漏れる)/追い上げ`CatchUpTick`(差に比例＝後発者利益・追い越せない)/独占余命`MonopolyDuration`(接触ゼロのみ∞)/模倣割引`ImitationDiscount`/跳躍`LeapfrogPotential`。`ResearchRules`=自前研究とは別)／緩衝国(`BufferStateRules`・フェザーン型＝均衡`PowerBalance`/生存余地`SurvivalSpace`(均衡×不可欠性＝片方欠けても0)/肩入れの危険`TiltPenalty`/併呑リスク`AnnexationRisk`(三死因＝均衡崩壊/偏り/用済み)/仲介影響力`BrokerLeverage`。`TradeRules.BrokerProfit`=口銭とは別＝政治力)／要衝価値(`ChokepointValueRules`＝希少性`ScarcityValue`(1/(1+迂回路))/経済`EconomicValue`/前線`FrontlineValue`/加重合成`TotalValue`(イゼルローン型=唯一の道×前線直結が満点)/守備優先度`GarrisonPriority`。AI・自動配備の判断材料＝盤面非依存のplain引数)／名将の衰え(`SenescenceRules`＝加齢倍率`AgeFactor`(峠45→漸減・下限0.6・実効値パターン)/体力系は早く判断系は遅い`PhysicalVsJudgment`(峠+10年・率半分)/自己認識の乖離`SelfAwarenessGap`(8年前の自分のつもり＝引き際を誤る)/引き際の窓`GracefulExitWindow`。`LifecycleRules`=死亡・`RetirementRules`=停年・`GrowthRules`=上りとは別＝下り坂)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**絆と備えの純ロジック追加（Wave12・並列7・配線待ち）**：テラフォーミング(`TerraformingRules`＝改造可能性`Feasibility`(過酷0.9以上は不能)/進捗`ProgressTick`(投資×可能性・放棄はわずかに逆行)/居住化`IsHabitable`/総コスト`CostToComplete`(不能は∞)/放棄退行`AbandonmentRegression`(完成後は自立＝退行なし)。`ColonizationRules`=入植の前段＝星造り)／宿敵(`RivalryRules`・ヤンvsラインハルト型＝強度`RivalryIntensity`(対戦×互角度＝一方的な相手は宿敵にならない)/成長倍率`GrowthMultiplier`/対面集中`FocusBonus`/喪失の空虚`VoidAfterDeath`(強い宿敵ほど深い)/緩やかな回復。`GrowthRules`へ掛ける関係性ボーナス)／盟友(`FriendshipRules`・キルヒアイス/双璧型＝紐帯`BondTick`(修羅場で5倍速)/共同作戦`JointOperationBonus`(bond²＝深い盟友のみ真価)/喪失悲嘆`LossGrief`(bond²)＋癒えない残滓`GriefFloor`/反目`EstrangementRisk`(野心差×政治圧力・最深の友情でも0.4は裂けうる)。`CommandStaffRules`=職制とは別＝個人的紐帯)／師弟(`MentorshipRules`・メルカッツ型＝教える力`TeachingQuality`(技量×忍耐＝強い武人≠良い師)/成長倍率`LearningMultiplier`/天井`SkillCeiling`(師の9割＝超えるには独り立ち)/独立判定`IndependenceTest`/長居の摩耗`OverstayPenalty`/師の死の遺産`LegacyOnMentorDeath`(準備度の分水嶺で飛躍/崩壊の二面性)。師は階段であり天井)／歴史叙述(`HistoriographyRules`＝公式評価`OfficialVerdict`(政権整合で歪む)/改竄幅`DistortionGap`/政権交代の再評価`RevisionOnRegimeChange`(真実には戻らない)/証人退場で検証可能性が痩せる`TruthErosionTick`/歴史家の抵抗力`HistorianIntegrity`(学問の自由のみが源泉)/最終審判`LongRunVerdict`(自由×記録が真実へ収束)。`ReputationRules`=存命中とは別＝死後の評価戦)／即応態勢(`ReadinessRules`＝維持費`UpkeepFactor`(0.6〜2.0)/出撃遅延`ResponseDelay`/奇襲脆弱性`SurpriseVulnerability`(`AmbushRules`の警戒入力へ)/疲労`FatigueTick`(持続可能水準0.5超過で蓄積)/実効警戒`EffectiveAlert`(張り詰めた弦は緩む＝常時最高警戒は不可能)。`VeterancyRules`=長期熟練とは別)／強行軍(`ForcedMarchRules`＝速度`SpeedFactor`(最大1.5倍)/疲労蓄積（回復は蓄積より遅い＝返済は倍払い）/到着直後の戦闘ペナルティ`CombatPenalty`/落伍`StragglerRatio`(閾値0.6の内側で駆けるのが疲労管理の芸)/回復時間。`StrategicFleet`非改変＝係数のみ・`DesertionRules`=脱走とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**梯子と螺旋の純ロジック追加（Wave13・並列7・配線待ち）**：モスボール(`MothballRules`＝保管維持費0.2倍/再就役の時間・整備費（長く眠るほど高い）/保管劣化`ConditionDecayTick`(保守で緩和)/再就役直後の実効`ReactivatedEffectiveness`＝平時の節約は有事の遅れで払う。`ShipAgingRules`=経年とは別＝保管状態)／縦深襲撃(`RaidRules`＝到達率`InfiltrationChance`/破壊`TargetDamage`(占領なし)/帰還率`ExfiltrationChance`(**深さの二乗で落ちる**＝深く刺すほど抜けなくなる・警報後半減)/期待戦果`ExpectedValue`(損益分岐)/警戒上昇`AlarmLevelTick`(反復は逓減)。`CommerceRaidingRules`=船団狩りとは別＝固定目標)／焦土(`ScorchedEarthRules`＝拒否価値`DeniedValue`/侵攻減速`InvaderSlowdown`/住民の恨み`PopulationResentment`(退避で軽減・ゼロにはならない)/生む荒廃`DevastationCreated`(`ReconstructionRules`の入力＝対)/損益`NetValue`＋分岐点`BreakEvenTime`(既定75＝すぐ取り返すなら焼くだけ損)＝時間を買って未来を売る)／艦隊反乱(`MutinyRules`＝不満の三因合成`GrievanceAccumulation`(待遇/思想/敗勢)/リスク`MutinyRisk`(軍紀が蓋・首謀者が触媒)/ロジスティック伝播`SpreadTick`/分裂`LoyalistSplit`(拮抗で同士討ち最大)/鎮圧の禍根`SuppressionAftermath`(寛大でも基礎分は残る)。`DisciplineRules`=個別と`CoupRules`=国家の中間スケール)／エスカレーション(`EscalationRules`＝事件の重さ`IncidentSeverity`(死者で跳ねる)/昇り圧力`EscalationPressure`(固有圧＝誰も望まなくても梯子は昇る)/降りる威信コスト`DeescalationCost`/仲介者の出口`FaceSavingExit`/梯子`RungTick`/引き返し不能点`PointOfNoReturn`(宣戦は`DiplomacyRules`へ委譲))／抑止(`DeterrenceRules`＝報復能力`RetaliationCapability`(第二撃残存性)/信憑性`Credibility`(実績×死活度＝空脅しの前科は信用を殺す)/抑止力＝**能力×信憑性の積**(どちらかゼロなら無意味)/開戦誘惑`AttackTemptation`/退路を焼く罠`CommitmentTrap`)／軍拡競争(`ArmsRaceRules`・リチャードソン簡易形＝脅威認知`ThreatPerception`/対抗建艦`ReactionBuildRate`(猜疑増幅)/螺旋`SpiralTick`(猜疑ゼロの拮抗は疲労で自然軍縮)/経済圧迫`EconomicBurden`/相対優位`RelativeAdvantage`(**対称螺旋20tickで圧迫10倍・優位は不変の物語テスト**)/相互自制の配当`MutualRestraintGain`。`ShipyardRules`=生産・`GameTheoryRules`=利得一般とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**静かな武器の純ロジック追加（Wave14・並列7・配線待ち）**：軍縮条約(`ArmsControlRules`＝条約超過`ComplianceGap`/発覚率`DetectionChance`(超過×査察の積＝大きな嘘ほど隠しにくいが査察ゼロなら見えない＝信頼するが検証せよ)/裏切り誘惑`CheatingTemptation`(脅威×不信の積)/発覚の条約死`ExposureFallout`/軍縮の配当`MutualSavings`/査察は手の内も晒す`VerificationDilemma`。`TreatyRules`=条約一般とは別＝検証問題)／宥和(`AppeasementRules`・ミュンヘン型＝**同じ譲歩が相手次第で満足`SatiationEffect`/食欲`AppetiteGrowth`に分かれる鏡像構造**/要求の学習`DemandTick`(譲る相手だと学んだ拡張主義者は次をもっと要求)/見られた譲歩の信用毀損`ReputationCost`/見誤りの代償`MisjudgmentCost`(過小評価のみ＝罪は宥和でなく誤読))／勢力圏(`InfluenceRules`＝三経路浸透`PenetrationTick`(経済0.4/軍事0.35/政治0.25)/政策追従`PolicyCompliance`(浸透度²)/綱引き`RivalContestation`(代理紛争リスク)/民族主義反発`BacklashRisk`/属国化転換`SoftToHardThreshold`(成立後は`DiplomacyState`へ委譲)＝旗を立てない帝国)／債務外交(`DebtDiplomacyRules`＝レバレッジ`DebtLeverage`(閾値0.5以下は0＝返せる借金は力にならない)/差押`AssetSeizureValue`(99年租借型)/罠設計`DebtTrapDesign`(ハンバントタ型)/反発`DebtorResentment`/**借り手の逆カード**`DefaultCardStrength`(同じ債務が双方の武器)。`BankRules`/`FiscalRules`とは別)／対外援助(`ForeignAidRules`＝好感度`OpinionGain`(困窮時3倍＝雪中の炭)/敵対国災害救援2倍`DisasterDiplomacyBonus`/紐付き`StringsAttached`(露骨な買収は逆効果)/依存形成`DependencyTick`(形成は速く自立回復は遅い)/カットの威力`SuddenWithdrawalShock`/援助疲れ`DonorFatigue`。`DisasterRules`=国内とは別＝越境)／戦犯裁判(`TribunalRules`＝裁きの正統性`PerceivedFairness`(勝者の罪を裁かない法廷は0.5倍＝復讐の劇場)/区切り`ClosureEffect`(公正×有罪率＝不公正は全員有罪でも閉じない)/殉教者リスク`MartyrdomRisk`/不処罰の不満`ImpunityGrievance`/真実和解`ReconciliationPath`(南ア型)。`AtrocityRules`の後段・`AmnestyRules`=赦しと対)／国勢調査(`CensusRules`＝可視性`LegibilityTick`(投資で上がり社会変化で陳腐化＝統計は生もの)/政府が見る人口`PerceivedPopulation`(roll決定論)/徴税効率`TaxCollectionEfficiency`/政策外れ`PolicyMisfireRisk`/見えない人口`ShadowPopulation`/監視の反発`SurveillanceResentment`＝見えない国は治められないが見えすぎる国は息が詰まる。`DemographicsRules`=真値とは別＝認識)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**共和制の構造の純ロジック追加（Wave15・並列7・配線待ち）**：連立(`CoalitionRules`＝必要判定`NeedsCoalition`/政策希釈`PolicyDilution`(党数×思想幅＝最小公倍数化)/**キングメーカー**`KingmakerPower`(シェアでなくピボット＝3%党が0.92・30%党が0の非対称)/結束`StabilityTick`/崩壊`CollapseRisk`/閣僚配分`PortfolioAllocation`(ガムソンの法則)。`PartyRules`=党勢とは別)／弾劾(`ImpeachmentRules`＝訴追`CaseStrength`(証拠×罪の積)/成立`ConvictionChance`(党派忠誠の壁＝証拠完璧でも0.3)/**失敗の逆風**`FailedImpeachmentBacklash`(弱訴追失敗+0.5=魔女狩り・強訴追失敗−0.2=燻り・分岐0.714)/罷免の正統性(手続きゲート)。`CoupRules`の制度版)／任期制限(`TermLimitRules`＝延長誘惑`ExtensionTemptation`(危機×人気×在任)/慣習強度`NormStrength`(違反で乗算減・回復天井0.8＝**満点には戻らない**)/違反コスト`ExtensionLegitimacyCost`(最初が最も高く二人目は安い)/政権交代の制度資本`PeacefulTransferValue`(対数逓増)/`RepublicDecayTick`(違反一撃0.3・回復30年＝壊すのは一瞬築くのは一生)。`ConstitutionRules`=範囲とは別＝時間軸)／国家緊急権(`EmergencyPowersRules`＝危機処理加速/権力集中`PowerConcentrationTick`(集中は集中を呼ぶ)/常態化リスク`NormalizationRisk`(**危機が本物なら何年でも0・去ったのに解除されない延長だけが罠**＝全権委任法)/制度萎縮`InstitutionalAtrophyTick`/復帰困難`RestorationDifficulty`/時限条項`SunsetClauseValue`(紙の時限は0)。`MartialLawRules`=治安とは別＝憲法停止)／連邦制(`FederalismRules`＝地域適合`LocalFitness`/統一行動`UnifiedActionSpeed`(分権ほど鈍い)/州の実験場`PolicyExperimentValue`(収穫逓減)/分離の滑り坂`SecessionGradient`(`CultureRules.SeparatismRisk`の土壌)/戦時集権圧`CentralizationPressure`/均衡点`OptimalDevolution`＝平時は分権・戦時は集権の振り子。`SeparationOfPowersRules`=水平に対する垂直)／市民権(`CitizenshipRules`＝二級市民の不満`SecondClassGrievance`(排除年数の複利＝世代を跨ぐ恨み)/包摂の統合`IntegrationGain`(ローマ・モデル)/既得反発`OldCitizenBacklash`(速度0なら反発なし＝門は徐々に)/従軍の血の代価`MilitaryServicePath`/体制外化`StatelessRisk`(排除比の二乗)/純効果`NetOpennessEffect`＝壁ではなく門に。`CultureRules`=文化同化とは別＝法的地位)／猟官制(`PatronageRules`＝買えた忠誠`LoyaltyPurchased`/行政劣化`AdministrativeQuality`/実力者流出`MeritocracyExodus`(閾値0.5超で雪崩)/論功圧力`SpoilsExpectation`(勝っても恨まれる)/改革抵抗`ReformResistance`/損益`NetRegimeValue`＝**全面猟官化は縁故者が天才揃いでも赤字**＝忠誠は買えるが有能は買えない。`SeniorityRules`とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**宮廷と民衆の純ロジック追加（Wave16・並列7・配線待ち）**：寵愛(`CourtFavorRules`＝寵`FavorTick`(追従は速く実績は遅いが安定・常時減衰＝日は必ず動く)/専横`FavoriteTyranny`(寵²×監督の緩み)/讒言`SlanderEffect`(君主の眼力が防波堤＝眼力1で無効)/寵の揮発性`FavorVolatility`(追従の寵は一夜で消える)/陰謀度`CourtIntrigueLevel`。`PowerRules`=実権とは別＝寵という通貨)／野心(`AmbitionRules`・ロイエンタール型＝成長`AmbitionGrowth`(功績×(1−天井)＝行き場のない実力)/猜疑`SuspicionTick`/**共鳴**`SpiralFeedback`(野心×猜疑の積＝どちらか0なら回らない＝自己成就予言)/反逆圧`RebellionPressure`(自発最大0.4＜追い込まれ0.6＝追い込まれて立つ)/解除手`DefusionOptions`(名誉職は天井↓で野心再燃の副作用)。`LoyaltyRules`とは別)／病臥(`IllnessRules`・ラインハルト型＝発症`OnsetChance`(加齢×激務×虚弱)/執務低下`CapacityFactor`/隠蔽の侵食`ConcealmentTick`(側近が多いほど漏れる)/漏洩衝撃`LeakImpact`/継承レース`SuccessionRaceTick`(**立太子で鎮静**)/病床の権威`DeathbedAuthority`(遺言1.5倍)。`LifecycleRules`=加齢とは別＝突発)／醜聞(`ScandalRules`＝露見`ExposureChance`(報道と政敵の独立合成)/ダメージ`ReputationDamage`(**偽善プレミアム＝軽罪の偽善者0.4＞重罪の野人0.3**)/もみ消し`CoverupGamble`＋失敗の倍返し`CoverupBackfire`(必ず元より重い)/醜聞慣れ`ScandalFatigue`/政治兵器`TimingWeaponValue`(選挙前夜2倍))／農奴解放(`SerfdomRules`＝**J字カーブ**`FreeLaborProductivityTick`(直後は谷0.3＜農奴0.6→10年で1.2)/追い越し`CrossoverYears`(10/3年＝果実を見ずに種を蒔く)/ショック・領主反発(補償で牙を抜く)/解放者への恩義`FreedomLoyalty`(30年で風化)/流動性`MobilityGain`)／戦争記憶(`GenerationalMemoryRules`＝半減期30年`MemoryIntensity`/証人退場`WitnessShare`/開戦閾値`WarThresholdModifier`(記憶最大で1.5倍＝抑止の内面化)/美化`RomanticizationTick`(記憶と反比例)/教育保存`EducationPreservation`(上限0.4＝体験の代替にならない)/世代ギャップ＝**平和の最大の敵は平和の長さ**。`WarGoalRules.WarWeariness`=短期とは別)／パンとサーカス(`BreadAndCircusesRules`＝ガス抜き`PacificationEffect`(根は治さない)/限界`SubstitutionFailure`(本物の不満0.9はサーカスで隠せない)/依存`DependencyTick`(均衡0.833で飽和)/刺激の逓減`ToleranceEscalation`(コスト最大2倍)/停止の反動`WithdrawalRage`(元本+利子)/政治的無関心`PoliticalApathyEffect`。`HopeRules`=意味の実体とは別＝代替財)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**銃後と辺境の純ロジック追加（Wave17・並列7・配線待ち）**：退役軍人政治(`VeteranPoliticsRules`＝圧力団体規模`VeteranBlocSize`/恩給負担`PensionBurden`/冷遇の不満`NeglectGrievance`/街頭動員`StreetPower`(ボーナスアーミー型)/好戦ロビー`MilitarismLobby`/厚遇の配当`LoyaltyDividend`＝厚遇は財政を食い冷遇は街頭に出る＝どちらかで必ず払う。`RetirementRules`=個人とは別＝集団の政治力)／労働運動(`StrikeRules`＝交渉力`BargainingPower`(組織率×人手不足)/決行`StrikeThreshold`/消耗戦`ProductionLoss`/基金枯渇`StrikeFundDepletion`/弾圧の逆風`RepressionOutcome`(`NonviolenceRules`と同型)/妥結`SettlementWage`＝**先に飢えた方が折れる我慢比べ**)／基軸通貨(`ReserveCurrencyRules`＝基軸度`ReserveStatusTick`(築くのは遅く信認だけ速く壊れる非対称)/法外な特権`ExorbitantPrivilege`/発行益`SeigniorageIncome`/濫用の我慢`DebasementToleratedTick`/**崩壊閾値は代替通貨の出現で下がる**`TrustCollapseThreshold`(無代替なら崩れない)/返済ショック`CollapseShock`。`FiscalRules`=国内為替とは別＝通貨覇権)／配給制(`RationingRules`＝配給量/生存ライン`SubsistenceGap`/公平感`FairnessPerception`(**特権層の例外が乗算で殺す**＝指導層が同じ列に並ぶ国は耐える)/欠乏下の士気`MoraleUnderScarcity`(公平比重0.7＞量0.3＝公平な欠乏0.85＞不公平な豊富0.44)/闇市圧`BlackMarketPressure`(`BlackMarketRules`と対)/買いだめパニック`HoardingPanicTick`(不足の予想が不足を作る))／軍産複合体(`WarIndustryRules`＝シェア拡大`IndustryShareTick`(深まりやすく解けにくい粘性)/講和抵抗`PeaceResistance`(雇用を握ると倍化＝平和は失業)/脅威誇張`ThreatInflationIncentive`/天下り`RevolvingDoorCorruption`/転換費用`ConversionCost`(share²＝軍縮の最大の敵は経済構造)/恒久戦争均衡`PerpetualWarEquilibrium`(完全監督だけが崩す))／探査(`ExplorationRules`・**G-2 #119のCore部分**＝進捗`SurveyTick`/探索済み`IsSurveyed`(`ColonizationRules.CanColonize`のexplored供給源＝連結テスト済み)/発見`DiscoveryChance`/喪失`HazardChance`(帰らぬ探査艦)/データ陳腐化`SurveyDecayTick`(再探査)/開拓価値`FrontierValue`(√＝最初の地図ほど価値大))／宙域気象(`SpaceWeatherRules`＋enum`SpaceWeatherKind`{恒星嵐/重力波バースト/放射線帯活性化}＝発生`OnsetChance`/山なり強度`StormIntensityTick`/回廊封鎖`CorridorClosure`(強度²)＋`IsPassable`/通信途絶`CommsDegradation`(`CommunicationsRules`のjammingへ加算)/突破損害/予報`ForecastAccuracy`(観測網×先読み半減)/軍事利用`WeatherWindowValue`(半端な嵐が最高の隠れ蓑)。`TerrainRules`=恒常とは別＝時限イベント)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**防御・諜報・統治の影の純ロジック追加（Wave18・並列7・配線待ち）**：遺失技術(`RelicRules`＝発掘`ExcavationChance`/格で指数的に跳ねる技術価値`RelicTechValue`/解析`ReverseEngineeringTick`(科学水準が低いと宝の持ち腐れ)/独占の漏れ`SecrecyErosionTick`/未理解の暴走`CurseRisk`＝遺物は拾った文明の格を映す鏡。`ResearchRules`/`DisclosureRules`とは別)／縦深防御線(`DefenseLineRules`＝一点突破vs広正面`BreakthroughChance`/浸透`PenetrationDepthTick`/予備隊の反撃`ReserveCounterattack`(早いほど効く)/突出部の逆包囲`EncirclementOpportunity`/連鎖崩壊`LineCollapseRisk`＝防衛線の本体は前縁でなく予備隊。`Fortress`(単一要塞)とは別)／私掠免許(`PrivateerRules`＝応募`CommissionAttraction`/通商破壊`RaidingEffectiveness`(後ろ盾が拿捕を合法化)/統制の緩み`ControlSlippageTick`/中立事故`NeutralIncident`/**戦後の海賊化**`PostwarPiracyConversion`(退役金で買い取れる)＝借りた暴力は返却日に高くつく。`PiracyRules`の制度化＝対)／救護衛生(`MedicalRules`＝損耗内訳`CasualtySplit`(保存則・重傷の生死は救護が決める)/黄金の1時間`GoldenHourEffect`/医療崩壊`CapacityOverloadPenalty`/衛生投資の配当`MedicalDividend`(見えない兵力)/経験者温存`VeteranPreservation`＝衛生兵は戦わない最強の兵科。`RepairRules`(艦)の人員版)／官僚制肥大(`BureaucracyBloatRules`・パーキンソンの法則＝定員の自己増殖`HeadcountTick`(仕事量と無関係)/管理コスト`AdminOverheadRatio`(人数の二乗)/実効産出`EffectiveOutput`(適正規模を境に下がる山なり)/適正規模`OptimalHeadcount`/行革抵抗`ReformResistance`(肥大ほど・行革が遠いほど固い)/行革`ReformEffect`(削減と混乱の交換)。`MinistryRules`(編制ツリー)とは別＝人数の動態)／秘密結社(`SecretSocietyRules`・地球教型＝浸透`InfiltrationTick`(絶望が深い社会ほど招きに応じる)/要職到達`HighOfficeReach`(浸透の二乗)/政策の歪み`PolicyDistortion`/摘発の限界`CrackdownEffect`(見えている分しか剥がせない)/再生`RegrowthTick`＝見えない敵とは戦えない。`ReligionRules`/`EspionageRules`とは別)／報道の自由(`FreePressRules`＝腐敗発見`CorruptionDetectionRate`/見えない腐敗の蓄積`HiddenCorruptionTick`(統制下は全量溜まる)/政権の恥`RegimeEmbarrassment`/体制の健全配当`SystemicHealthDividend`/検閲の爆発`CensorshipBlowback`(チェルノブイリ型相乗)/資本の籠絡`PressCaptureRisk`(検閲なき統制)＝政権の敵で体制の友。`PropagandaRules`(発信側)とは別＝監視側)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**集合行為と覇権の純ロジック追加（Wave19・並列7・Issue補充後・配線待ち）**：圧力団体(`LobbyRules`＝影響力`InfluenceStrength`(金×数×コネ)/政策の歪み`PolicyDistortion`(公益一致なら無害)/集中利益分散費用`ConcentratedBenefitDiffusedCost`(オルソン＝少数受益ほど超線形)/規制の虜`RegulatoryCapture`/対抗ロビー`CounterLobbyBalance`/全体厚生損失`AggregateWelfareLoss`。`PartyRules`/`WarIndustryRules`(軍需特化)とは別)／選好偽装(`PreferenceFalsificationRules`・クーラン型＝表明支持`ExpressedSupport`(抑圧で本音より高く偽装)/乖離`PreferenceGap`/閾値カスケード`CascadeTick`(臨界で雪崩)/体制の盲目`RegimeBlindness`(強権ほど足元が見えない＝突然倒れる)。`ConsentRules`とは別＝表明と本音の乖離)／価格統制(`PriceControlRules`＝品不足`Shortage`/行列`QueueLength`(金でなく時間で払う)/闇値プレミアム`BlackMarketPremium`/供給の自己破壊`SupplyDestructionTick`(採算割れが生産者を撤退)/失敗度`ControlFailureIndex`(闇値乖離)/安さの幻想`ConsumerSurplusIllusion`。`RationingRules`(量)と対＝価格の統制)／土地改革(`LandReformRules`＝生産意欲`ProductivityGain`(小作が多いほど割に合う)/農民支持`PeasantSupport`/地主反発`LandlordBacklash`(補償で和らぐ)/短期混乱`ShortTermDisruption`/細分化の弊害`FragmentationPenalty`/長期純産出`NetOutput`。`SerfdomRules`(身分)とは別＝資産の再分配)／辺境気質(`FrontierRules`＝辺境度`FrontierIndex`(距離×通信遅延)/自立`SelfReliance`/統制浸透`ControlPenetration`(駐留で補う)/独立志向`IndependenceSentiment`(辺境×冷遇)/辺境自衛`FrontierMilitia`(戦力にも反乱基盤にも)/辺境の自由`InnovationFreedom`。`LogisticsRules`(物理連結)とは別＝距離の文化的効果)／アサビーヤ(`AsabiyyaRules`・ハルドゥーン型＝紐帯の世代減衰`AsabiyyaDecayTick`(**繁栄が連帯を溶かす**)/軍事的活力`MilitaryVigor`/奢侈`LuxuryCorruptionTick`/王朝サイクル`DynastyLifecycle`(三世代≈120年)/新興勢力の優位`ChallengerAdvantage`(爛熟中枢＜紐帯の辺境)/中興`RenewalChance`。`DynastyRules`(天命/腐敗)とは別＝集団紐帯の寿命)／覇権移行(`HegemonyRules`・トゥキディデスの罠＝力の比`PowerRatio`/移行危険`TransitionDanger`(**比1.0=追い越しの瞬間がピークの山形**)/覇権国の恐怖`HegemonFear`(スパルタ)/台頭国の野心`RisingPowerAssertiveness`(アテネ)/予防戦争`PreventiveWarTemptation`/平和的移行`PeacefulTransitionChance`(制度的紐帯が罠を緩める)。`ArmsRaceRules`(量の螺旋)とは別＝構造的な力の遷移)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**同盟・分配・連鎖経済の純ロジック追加（Wave20・並列7・配線待ち）**：過剰拡張(`OverextensionRules`・ポール・ケネディ型＝防衛公約`CommitmentBurden`/負担比`BurdenToCapacityRatio`/過伸張ペナルティ`OverstretchPenalty`(**比1.0までは0・超えると守るほど弱くなる**)/帝国衰退`ImperialDecayTick`/戦略的収縮`StrategicRetrenchmentGain`(撤退でしか治らない)/正面あたり防御`DefensibilityPerSector`。`LogisticsRules`(一体化)とは別＝負担と国力の比)／同盟負担分担(`BurdenSharingRules`＝ただ乗り誘因`FreeRiderIncentive`(盟主が供給するほど小国は払わない)/応分貢献`OptimalContribution`/実貢献`ActualContribution`/盟主の肩代わり`HegemonOverpayment`/強制の軋み`CoercionStrain`/同盟結束`AllianceCohesion`。`TreatyRules`/`LoyaltyRules`(静観)とは別＝平時の費用分担)／集団安全保障(`CollectiveSecurityRules`・国際連盟型＝制裁参加`SanctionParticipation`(自分が損し脅威が遠いほど不参加＝対岸の火事)/集団対応`CollectiveResponse`(足並み閾値未満は無力)/信頼性`CredibilityTick`(一度の見過ごしで崩れる満州事変型)/抑止力`DeterrenceValue`/離脱誘因`FreeRiderDefection`/体制崩壊`SystemCollapse`。`DiplomacyRules`(二国間)とは別＝多国間の脆さ)／戦後分割(`PartitionRules`＝公正な取り分`FairShare`/不満`ShareGrievance`(勝者間の火種)/分割線の恣意性`PartitionLineArbitrariness`(住民無視の机上線)/将来紛争`FutureConflictRisk`/占領コスト`OccupationCost`(取りすぎは消化不良)/勝者連合の分裂の種`VictorRivalrySeed`＝分割は次の戦争の地図を引く。`TreatyRules`/`ReparationsRules`とは別)／親衛隊(`PraetorianRules`・プラエトリアニ型＝身辺安全`ProtectionStrength`/政治力`PoliticalLeverage`(君主に近く強いほど発言力＝守る者が支配する)/簒奪リスク`KingmakerRisk`/厚遇のジレンマ`PamperingTick`＋`LeverageCreep`(**忠誠と政治力が同時に育つ**)/冷遇の脆弱性`NeglectVulnerability`/最適規模`OptimalGuardStrength`(強すぎても弱すぎても危ない谷)。`CivilianControlRules`/`CoupRules`とは別＝近衛の特殊問題)／連産品(`CoupledProductionRules`/`JointRecipe`・#1110＝律速入力で決まる最大稼働`MaxRuns`(リービッヒの最小律)/律速財`BottleneckInput`/固定比の同時産出`Produce`/投入消費`InputConsumption`/従産物の強制産出`ForcedByproduct`(主産物を作れば従産物が望まずとも出る＝連産の宿命)/稼働率`CapacityUtilization`。`MarketRules`(単一財需給)/`ResourceProductionRules`とは別＝生産の結合・連鎖経済の起点)／スプレッド(`SpreadRules`・#1111＝粗マージン`GrossSpread`(出力−入力)/純マージン`NetMargin`/採算判定`IsProfitable`/操業意欲`ProductionIncentive`(損益分岐割れで停止)/マージン圧迫`MarginSqueezeTick`(原料高×製品安の挟み撃ち)/操業停止点`ShutdownThreshold`(可変費基準)＝原料高で工場は静かに止まる。`FiscalRules`(国家財政)とは別＝個別工程の採算)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**連鎖経済CPL完結＋孫子ドクトリンの純ロジック追加（Wave21・並列7・全Issue由来・配線待ち）**：副産物グルット(`ByproductGlutRules`・#1113＝従産物量`ByproductSupply`/供給過剰`GlutSeverity`/価格暴落`PriceCollapse`(需要の数倍で二束三文)/廃棄コスト`DisposalCost`(貯蔵不能は捨てるのに金がかかる)/有効利用`ByproductValorization`(川下産業があれば価値に転じる)/主産物への足枷`PrimaryProductionDrag`(処理コストが採算を蝕む)。`CoupledProductionRules`/`MarketRules`とは別＝連産×市場)／連鎖の脆さ(`ChainFragilityRules`・#1112＝上流グルット`UpstreamGlut`/下流欠品`DownstreamShortage`/カスケード段数`CascadeDepth`(バッファ薄いほど深く伝播)/単一障害点`SinglePointRisk`/復旧時間`RecoveryTime`/頑健性`Resilience`(冗長0.6+在庫0.4)＝1点の遮断が上流下流を同時に殺す。`SupplyRules`とは別＝生産網の伝播)／連続運転硬直(`ContinuousOperationRules`・#1115＝最低稼働`TurndownFloor`(連続炉はゼロに絞れない)/停止コスト`ShutdownCost`(連続性²＝炉が冷えて固まる)/再起動時間`RestartTime`/硬直の過剰生産`OverproductionFromRigidity`/操業判断`OperatingDecision`(止めるより回す方が安い領域＝戦時硬直)/戦時ロックイン`WartimeLockIn`。`MobilizationRules`とは別＝プラントの慣性)／中間体貯蔵不能(`IntermediateBufferRules`・#1116＝貯蔵可能性`Storability`(溶融物・気体は0)/緩衝在庫`BufferCapacity`/ショック増幅`ShockAmplification`(バッファ無しは増幅1.0)/結合の固さ`CouplingTightness`(バッファ無し＝密結合)/強制同期`ForcedSynchronization`(貯蔵不能は秒単位同期)/投資価値`BufferInvestmentValue`。`ResourceStockpile`とは別＝貯められない中間財)／現地調達(`ForageRules`・#1128孫子「糧を敵に因る」＝徴発量`ForageYield`(豊か×敵対が低いほど集まる)/補給線軽減`SupplyLineRelief`/住民の恨み`PopulationResentment`(略奪は次の反乱を育てる・友好でも0でない)/星系の枯渇`DepletionTick`(絞り続けで涸れる)/機動加速`MobilitySpeedBonus`(輜重を引かない軍は速い)/養兵限界`SustainabilityLimit`(大軍は1星系を食い潰す)。`SupplyRules`(後方補給線)とは別＝前線の自律調達)／攻勢終末点(`CulminatingPointRules`・#1129クラウゼヴィッツ＝補給効率`SupplyEfficiency`(距離で逓減)/実効戦力`CombatPowerFactor`/終末点距離`CulminatingDistance`(攻撃側が防御側を下回る地点)/終末点超過判定`IsPastCulmination`/超過ペナルティ`OverreachPenalty`/最適停止点`OptimalHaltPoint`(終末点の手前で止まるのが上策)。`OverextensionRules`(版図)とは別＝作戦距離の戦力減衰)／謀攻優先(`SunziDoctrineRules`＋enum`StrategicMeans`{謀略/外交/野戦/攻城}・#1130孫子謀攻篇＝各手段スコア`MeansScore`/最善手段`BestMeans`(謀略不可なら順に下る)/無血勝利`BloodlessVictoryValue`(戦わずして勝つは善の善)/攻城の罰`SiegePenalty`(下策＝最も兵を損なう)/伐交`DiplomaticIsolation`(敵の同盟を断つ)/謀略成否`StratagemSuccess`(敵の聡明さが防壁)＝AIの戦略選択。`PlanetSiegeRules`(攻城実体)とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**欺瞞・通貨・会計・統治の純ロジック追加（Wave22・並列7・全Issue由来・配線待ち）**：戦略的欺瞞(`DeceptionRules`・#1126孫子「兵者詭道也」＝信憑性`DeceptionCredibility`(敵の聡明さが防壁)/認識バイアス`PerceptionBias`(`ReconRules`の推定へ注入)/誘導`MisdirectionEffect`/矛盾崩壊`ConsistencyDecayTick`/露見`Exposure`/信用焼失`BacklashOnExposure`(ばれた嘘は二度と使えない)。`FeintRules`(戦術)/`PropagandaRules`(世論)とは別＝戦略AIの認識操作)／通貨改鋳(`CoinageRules`・#1072＝地金価値`IntrinsicValue`/発行益`Seigniorage`(品位を下げるほど儲かる)/信用浸食`DebasementTick`/悪貨が良貨を駆逐`GreshamEffect`/実質購買力`RealPurchasingPower`/回復コスト`RestorationCost`(片道切符)＝品位を下げれば今日儲かるが額面が紙になる。`InflationRules`(増発)とは別＝硬貨の品位)／複式簿記(`LedgerRules`＋enum`AccountType`/`JournalEntry`・#974＝個別仕訳の貸借一致`IsBalanced`/`TotalDebits`/`TotalCredits`/**Σ借方=Σ貸方**`IsLedgerBalanced`/勘定残高`AccountBalance`(正常側で符号)/正常残高側`NormalBalanceSide`/試算表`TrialBalance`＝会計整合の中核。`FiscalRules`(集計)とは別＝記帳エンジン)／王道覇道(`WangDaoRules`・#1059孟子＝主義ドリフト`DriftTick`(仁政→王道/武断→覇道)/服従の質`SubmissionQuality`(王道は心服で安定・覇道は力服で高コスト離反含み)/道による反乱圧`RebellionPressureFromDao`/自発帰順`AllyAttraction`(王道は戦わずして人を集める)/覇道の即効`CoerciveEfficiency`/徳の正統性`LegitimacyFromVirtue`＝力服は心服に非ず。`ReputationRules`(個人)とは別＝勢力の道)／工期と品質(`QualityScheduleRules`・#1091Pillars＝工期加速`ScheduleSpeedup`/構造品質`StructuralQuality`(急ぐほど落ちる)/事故確率`AccidentChance`(崩落/火災/襲撃)/事故判定`AccidentOccurs`(roll)/後退`SetbackSeverity`(低品質ほど数年失う)/完成後の脆さ`LongTermDurability`＝急造は崩れて数年を失う。`MegaProjectRules`(#1090)へ接続＝方針レバー)／技術は人に宿る(`TechBearerRules`・#1092Pillars＝保持度`TechRetention`(1人の頭は脆い)/死による喪失`LossOnDeath`(最後の名工が死ねば工法も死ぬ)/引き抜き価値`PoachingValue`/亡命伝播`DefectionTransfer`/徒弟伝承`ApprenticeshipTick`/文書化`CodificationTick`(暗黙知→形式知)。`InnovationDiffusionRules`(国レベル)とは別＝人という乗り物)／利権と特許状(`CharterRightsRules`＋enum`CharterType`・#1093Pillars＝利権価値`CharterValue`/授与忠誠`GrantLoyaltyBonus`/**取消怨恨`RevocationResentment`(授与忠誠より重い非対称＝与えるは易く取り上げるは難し)**/管轄争い`DisputeIntensity`/独占利権`MonopolyRent`/聖俗の争い`JurisdictionConflict`＝利権は怨恨の通貨。`MagnaCartaRules`(王権制約)とは別＝個別利権)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**都市・継承・会計の純ロジック追加（Wave23・並列7・全Issue由来・配線待ち）**：都市成長(`CityGrowthRules`＋enum`SettlementTier`・#1094Pillars＝人口流入`PopulationInflowTick`(事業が雇用を生む・中断で流出)/段階`TierOf`/市場成立`MarketEmergence`(集積が市場を生む)/集積ボーナス`AgglomerationBonus`/中断衰退`DeclineOnProjectHalt`(職人が去る)/Province寄与`ProvinceGrowthContribution`＝大事業は街を生む。`MegaProjectRules`(#1090)へ接続・`GovernanceRules`とは別)／継承戦争(`SuccessionWarRules`・#1095The Anarchy＝危機`CrisisSeverity`(請求者多×後継曖昧)/請求者総合力`ClaimantStrength`/諸侯の旗幟`NobleAllegiance`(強者になびく＋私心)/無政府度`AnarchyLevel`(拮抗並立で中央権威消失)/権威崩壊`CentralAuthorityCollapse`/決着`WarResolution`＝曖昧な継承は国を割る。`LoyaltyRules`(会戦カスケード)の国家版・`SuccessionLawRules`/`CivilWarRules`とは別)／聖職キャリア(`ClericalCareerRules`＋enum`ClericalRank`・#1096Pillars＝昇進`PromotionScore`(敬虔も野心も後ろ盾も登らせる)/霊的権威`SpiritualAuthority`/世俗権力`TemporalPower`(司教は諸侯)/理想と野心の綱引き`IdealVsAmbitionDrift`(出世が初心を侵食)/共同体の希望`CommunityHopeContribution`/腐敗`CorruptionRisk`(世俗権力×低敬虔＝地球教の影)＝同じ階段を理想家と野心家が別の動機で登る。`CareerPipelineRules`の第4系統=聖・`ReligionRules`/`HopeRules`へ接続)／艦艇設計(`ArmamentDesignRules`＋enum`ModuleType`/`ShipModule`/`HullSpec`・#1066＝総重量/電力/コスト集計/妥当性`IsValidDesign`(搭載量・電力・スロットの3制約＝過積載不可)/利用率`WeightUtilization`/戦闘力`CombatRating`/電力収支`PowerBalance`/特化ボーナス`SpecializationBonus`(同種集中で相乗＝砲艦/空母)＝積みたいものは全部積めない。`ShipClass`(固定艦種)/`ShipyardRules`とは別＝設計の自由度)／貸借対照表(`BalanceSheetRules`/`BalanceSheet`・#975＝総資産`TotalAssets`/総負債`TotalLiabilities`/純資産`Equity`/**資産=負債+純資産**`IsBalanced`/負債比率`DebtToEquityRatio`/支払能力`Solvency`(債務超過判定)/運転資本`WorkingCapital`＝`LedgerRules`のenum再利用の集計表＝ストックのスナップショット)／損益計算書(`IncomeStatementRules`/`IncomeStatement`・#976＝総収益`TotalRevenue`/総費用`TotalExpenses`/純損益`NetIncome`(収益−費用)/黒字判定`IsProfit`/利益率`ProfitMargin`/営業利益率`OperatingMargin`/利益剰余金繰越`RetainedEarningsTick`(黒字は純資産に積み赤字は食い潰す＝P/LとB/Sを繋ぐ)＝`LedgerRules`のフロー集計。`FiscalRules`とは別＝企業会計の様式)／財務ガードレール(`FiscalPolicyRules`＋enum`PolicyBreach`/`FiscalPolicy`・#1013＝債務上限逸脱`DebtCeilingBreached`/準備金下限`ReserveFloorBreached`/税率レンジ`TaxRateInRange`/最重大逸脱`WorstBreach`(債務>準備金>税率)/財政余裕`FiscalHeadroom`/遵守度`PolicyComplianceScore`＝財政の手すり。`FiscalRules`(実体)へ被せる国策レイヤー＝逸脱検知のみ・`AutoTreasuryRules`(バックログ)が自動行動)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**財務AI・調達・生産網の純ロジック追加（Wave24・並列7・全Issue由来・配線待ち）**：自律財務(`AutoTreasuryRules`＋enum`TreasuryAction`・#1014＝自動行動選択`DecideAction`(準備金割れ→起債/取崩・金利低下→借換・上限接近→緊縮)/起債額`BondIssuanceAmount`/借換利得`RefinanceBenefit`/緊縮の深さ`AusterityDepth`/支払能力経路`IsSolventPath`/自動運用の信頼`TouchlessConfidence`(激動期は人の判断が要る)＝タッチレス財務。`FiscalRules`(実体)/`FiscalPolicyRules`(#1013逸脱検知＝入力)とは別)／CF予測(`CashFlowForecastRules`＋enum`CashFlowWarning`・#1015＝予測残高`ProjectedBalance`/**債務超過までの残期間`PeriodsUntilInsolvency`(赤字が尽きる日を逆算)**/燃焼率`BurnRate`/警告段階`WarningLevel`/必要改善`RequiredCorrection`/予測信頼度`TrendConfidence`＝早期警告器。`FiscalRules`の将来投影＝予測のみ)／異常検知(`FinancialAnomalyRules`＋enum`AnomalyType`・#1016＝台帳不整合`LedgerInconsistency`/異常支出`ExpenseOutlier`(σ外れ値)/粉飾`RevenueSmoothing`(利益の不自然な平準化)/簿外債務`UnrecordedLiabilityRisk`/総合異常度`AnomalyScore`/監査優先度`AuditPriority`＝数字は嘘をつく。`LedgerRules`/`BalanceSheetRules`を read-only 監査)／サプライヤー評価(`SupplierRatingRules`・#1004＝納期遵守`OnTimeDeliveryScore`/品質`QualityScore`/信頼性の非対称更新`ReliabilityTick`(築くのは時間・崩すのは一度)/関係`RelationshipScore`/総合評価`OverallRating`/優先選定`PreferredSupplier`(最安≠最良)/乗り換えリスク`SwitchingRisk`(ロックイン)。`TradeRules`とは別＝調達先評価)／逆オークション(`SourcingAuctionRules`/`Bid`・#1005＝最安`LowestBid`/総合最適`BestValueBid`(最安≠最良)/競争激しさ`CompetitionIntensity`(入札者多で買い手有利)/価格発見`PriceDiscovery`/談合の兆候`CollusionRisk`(価格が揃いすぎ)/予定価格`ReservePriceMet`(不落判定)。`MarketRules`(連続市場)とは別＝離散入札)／契約管理(`SupplyContractRules`＋enum`ProcurementMode`・#1006＝価格安定`ContractPriceStability`(長期契約)/スポット曝露`SpotExposure`/柔軟性喪失`FlexibilityLoss`(長期は需要変動に弱い)/破棄ペナルティ`BreachPenalty`/**破棄の外交余波`BreachDiplomaticFallout`(大国との契約破棄は制裁/戦争＝`DiplomacyRules`へ波及)**/最適契約比率`OptimalContractMix`＝安定を買い柔軟を売る。`TreatyRules`の商業版)／部品表(`BomRules`/`BomNode`・#983＝多層展開`Explode`(艦→部品→素材を再帰積算)/特定素材総量`TotalMaterialRequirement`/階層深さ`BomDepth`/末端素材`LeafMaterials`/総部品点数`ComponentCount`/共通部品の節約`SharedComponentSavings`(標準化)＝1隻の艦は何千の素材に展開される。`CoupledProductionRules`(連産)とは別＝分解の木・循環防止の深さ上限)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**生産網SCM完結＋企業FRMの純ロジック追加（Wave25・並列7・全Issue由来・配線待ち）**：MRP所要量(`MrpRules`・#984＝正味所要`NetRequirement`(在庫で足りる分は発注しない)/計画オーダー`PlannedOrderQuantity`(ロット丸め)/発注時期`OrderReleaseTiming`(リードタイム遡り)/安全在庫`SafetyStockRequirement`/発注点`ReorderPoint`/在庫予測`InventoryProjection`＝必要なものを必要な時に必要なだけ。`BomRules`(#983展開＝入力)/`ProductionOrderRules`とは別)／発注オーダー(`ProductionOrderRules`＋enum`OrderType`/`FacilityType`/`ProductionOrder`・#985＝種別判定`DecideOrderType`(素材は購買・部品は生産)/設備割付`AssignFacility`(最も空いた所へ)/内製外注`MakeOrBuyDecision`(作るか買うか)/優先度`OrderPriority`(納期×重要度)/まとめ`ConsolidateOrders`/能力引当`CapacityReservation`。`MrpRules`計画を設備へ・`ShipyardRules`就役とは別)／有限能力(`CapacitySchedulingRules`・#987TOC＝ボトルネック`BottleneckStage`(鎖は最弱の輪で切れる)/スループット`SystemThroughput`(最弱工程が律速)/仕掛在庫`WipAccumulation`/工程別稼働率`UtilizationByStage`/完成時間`ScheduleCompletion`/ボトルネック強化`BottleneckElevationGain`(**改善は次工程へ移動する**)＝非ボトルネックを速くしても無駄。`ProductionOrderRules`を有限設備へ詰める)／バリューチェーン(`ValueChainRules`/`ValueChainStage`・#1023＝付加価値`AddedValue`/累積価値`CumulativeValue`(森→木→製材→家)/連鎖歩留`ChainYield`(各段の積＝1段悪いと全体が痩せる)/各段の取り分`ValueCaptureByStage`(スマイルカーブ)/歩留律速`BottleneckStageByYield`/垂直統合`VerticalIntegrationGain`(中間マージン取り込み)。`CoupledProductionRules`(単一工程)を繋ぐ網)／企業(`FirmRules`/`Firm`・#1024＝生産量`ProductionOutput`/売上`Revenue`/営業利益`OperatingProfit`(`IncomeStatementRules`へ)/稼働率決定`CapacityUtilizationDecision`(需要に応じて操業)/倒産リスク`SolvencyRisk`/退出判断`MarketExitDecision`(営業赤字が続けば市場退出)＝経済を動かすミクロ主体。`StockMarketRules`/`MarketRules`を結ぶ)／資本投下(`CapitalInvestmentRules`＋enum`FinancingSource`・#1025＝投資利回り`ExpectedReturn`/投資可否`InvestmentDecision`(ハードルレートの壁・リスクで上がる)/回収期間`PaybackPeriod`/資金調達`FinancingChoice`(内部留保→負債コストvs希薄化)/過剰投資`OverInvestmentRisk`(低稼働で拡大＝遊休設備)/能力増設`CapacityExpansionTick`(建設ラグ)＝ハードルレート超でのみ正当化。`FirmRules`の拡大再生産・`CapitalRules`(ピケティ)とは別)／公私分離(`PublicPrivateSeparationRules`・#1035＝分離度`SeparationLevel`(制度化で家産国家→近代国家)/私物化リスク`PrivatizationRisk`(分離弱×貪欲＝汚職)/国庫漏出`TreasuryLeakage`/分離の正統性`LegitimacyFromSeparation`(法の支配の土台)/継承安定`SuccessionStability`(**私物なら相続争い・公的制度なら円滑**)/既得層の抵抗`ReformResistanceFromElites`。`RegimeRules`(腐敗)/`FiscalRules`(国庫)とは別＝所有の帰属)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**所有・人事・諜報・均衡の純ロジック追加（Wave26・並列7・全Issue由来・配線待ち）**：私有財産(`PropertyRightsRules`・#1036＝保護強度`ProtectionStrength`(法×契約×(1−没収リスク))/投資意欲`InvestmentIncentive`(財産権は成長の土台)/資本逃避`CapitalFlight`/蓄財速度`WealthAccumulationRate`/地下経済`InformalEconomyShare`/没収ショック`ExpropriationShock`(一度の没収が信頼を長く損なう)＝守られない財産は築かれない。`ConfiscationRules`/`MagnaCartaRules`とは別)／相続(`InheritanceRules`＋enum`InheritancePattern`・#1038＝相続人の取り分`HeirShares`(長子=大半・分割=均等)/世代の集中度`DynastyConcentration`(長子は家を保ち分割は細る＝ガベルカインドの呪い)/相続税`InheritanceTax`/相続争い`DisputeRisk`/世代細分化`FragmentationOverGenerations`/無相続人の国庫帰属`EscheatToState`＝相続法が家の盛衰を決める。`SuccessionLawRules`(爵位)とは別＝資産・封土)／人事評価(`PerformanceReviewRules`＋enum`TalentBox`・#995＝9-box区分`Box`(実績×潜在)/昇進適性`PromotionReadiness`(スター人材最優先)/育成優先`DevelopmentPriority`(有望株が最大)/離職リスク`RetentionRisk`/後継準備`SuccessionReadiness`/評価の甘辛補正`CalibrationAdjustment`＝今の成果と未来の伸びは別軸。`SeniorityRules`(席次)とは別)／報酬賞罰(`CompensationRules`＋enum`RewardType`・#996＝士気効果`MoraleEffect`(信賞必罰＝不当な賞罰は逆に削る)/忠誠効果`LoyaltyEffect`/公平感`FairnessPerception`/報酬インフレ`RewardInflation`/罰の抑止`PunishmentDeterrence`(見せしめ)/信賞必罰の一貫性`MeritRewardBalance`＝功に報い過を罰す。`HonorsRules`/`MeritRankRules`を束ね`LoyaltyRules`へ)／用間5種(`SpyRoleRules`＋enum`SpyRole`{郷間/内間/反間/死間/生間}・#1127孫子用間篇＝情報量`IntelYield`(内間=深い)/発覚リスク`ExposureRisk`(死間=必発覚・生間=低リスク)/偽情報`Disinformation`(死間)/反間の二重スパイ価値`TurncoatValue`/運用コスト`RoleCost`/目的別最適`BestRoleForObjective`/神紀`GodlikeWebFactor`(五間同時で発覚割引)＝間者の種類で効果とリスクが変わる。`EspionageRules`(諜報一般)の役割分類)／ブルウィップ(`BullwhipRules`・#1114CPL-4＝1段増幅`AmplificationPerStage`/上流変動`UpstreamVariance`(段数で指数的増幅＝鞭の根元が大きく振れる)/発注変動`OrderVariability`/在庫の振れ`InventorySwing`/情報共有の緩和`MitigationByInfoSharing`(末端を上流が見れば増幅が止まる)/平準化の緩和`MitigationBySmoothing`＝末端の揺れが上流で大きく振れる。`ChainFragilityRules`(遮断の伝播)とは別＝変動の増幅・連鎖経済CPL完結)／多極均衡(`BalanceOfPowerRules`・#1103三国志＝一強の突出`HegemonThreat`/連衡圧力`CoalitionPressure`(一強が強いほど弱小が結束＝バランシング)/包囲標的`BalancingTarget`/バンドワゴン`BandwagonTemptation`(連衡が頼りないなら強者に従う)/システム安定`SystemStability`(多極は安定・一強は不安定)/均衡回帰`EquilibriumShift`(総国力保存で差を縮める)＝一強は包囲される。`DiplomacyRules`(二国間)/`HegemonyRules`(覇権移行=二者間)とは別＝多極のシステム圧力)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**三国志の権謀＋狼と香辛料の商人経済の純ロジック追加（Wave27・並列7・全Issue由来・配線待ち）**：献策(`CounselRules`＋enum`CounselQuality`・#1104三国志＝献策の質`StratagemQuality`(知略×状況)/採択確率`AdoptionLikelihood`(暗愚な君主は良策も退ける＝袁紹×田豊)/帰結修正子`OutcomeModifier`/献策無視の代償`MissedOpportunityCost`(神算却下が最大の後悔)/参謀の信用`CounselorCredibilityTick`/却下の不満`RejectionFrustration`(陳宮の離反)＝良策も君主が採らねば意味がない。`CommandStaffRules`(能力補完)とは別)／盟誓(`PledgeRules`＋enum`PledgeType`・#1105桃園結義＝誓いの拘束力`PledgeStrength`(公的な誓いほど破りにくい)/拘束力`BindingForce`(修羅場で固まる)/破棄の代償`BetrayalPenalty`(天下の信を失う)/義の名声`HonorBonus`(関羽の義)/誓いの義務`PledgeObligation`/忠誠連鎖`CascadingLoyalty`＝個人の契りが制度の拘束力を持つ。`LoyaltyRules`(旗幟)/`FriendshipRules`(紐帯)とは別)／離間の計(`AlienationStratagemRules`・#1106三国志＝不和を植える`SowDiscordEffect`(既存の不和に付け込む・固い信頼は防壁)/opinion打撃`OpinionDamage`/発覚`ExposureRisk`/**逆効果`Backfire`(ばれれば標的が結束する)**/同盟崩壊`AllianceCollapseChance`/疑心蓄積`SuspicionTick`＝敵同士を仲違いさせる。`EspionageRules`拡張・`DiplomacyRules`へ波及)／屯田制(`MilitaryColonyRules`・#1107曹操＝食糧自給`FoodSelfSufficiency`(兵農トレードオフ)/補給線軽減`SupplyLineRelief`/即応低下`CombatReadinessPenalty`/屯田成熟`ColonyMaturityTick`(初年は実らない)/恒久入植`PermanentSettlement`/戦略縦深`StrategicDepth`＝兵が農を兼ね補給線を断つ。`ForageRules`(一時徴発)とは別＝恒久自給)／改鋳投機(`CoinageSpeculationRules`・#1073狼と香辛料＝噂の相場力`RumorImpact`/投機ポジション`SpeculativePosition`/損益`SpeculativeProfit`(噂どおりで儲け外れで損)/内部情報の優位`InsiderAdvantage`/相場混乱`MarketPanic`/退蔵圧力`HoardingPressure`(グレシャムの前倒し)＝改鋳の噂が改鋳より先に相場を動かす。`CoinageRules`(改鋳の実体)の投機版)／情報非対称(`InformationAsymmetryRules`・#1074狼と香辛料＝情報優位`InformationEdge`/情報裁定`ArbitrageProfit`(知る者が知らぬ者から取る)/風説の相場`RumorPriceMovement`(真偽問わず噂で動く)/逆選択`AdverseSelection`(レモン市場)/優位の減衰`InformationDecay`(早い者勝ち)/シグナリング費用`SignalingCost`＝情報格差が利得を生む。`MarketRules`(均衡)とは別)／空間裁定(`SpatialArbitrageRules`・#1075狼と香辛料＝価格差`PriceGap`/裁定利得`ArbitrageProfit`(差が輸送費を超えてはじめて儲かる)/成立判定`IsArbitrageViable`/価格収束`ConvergenceTick`(安い方↑高い方↓＝一物一価へ)/収束先の差`EquilibriumGap`(輸送費ぶんは残る)/市場統合度`MarketIntegration`＝裁定が価格差を埋める。`TradeRules`(交易利得)とは別＝価格収束の動学)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**投機・商人信用・艦隊運用の純ロジック追加（Wave28・並列7・全Issue由来・配線待ち）**：買い占めバブル(`CorneringRules`・#1076狼と香辛料＝市場支配`MarketControl`/価格吊り上げ`PriceManipulation`(非線形)/買い占めコスト`CornerCost`(自分の買いが値を上げる)/バブルの脆さ`BubbleFragility`(実需なき価格は砂上の楼閣)/崩壊`Burst`(roll)/手仕舞い損失`UnwindLoss`(売り抜けられず破産＝ハント兄弟)＝実需なきバブルは自分の在庫もろとも崩れる。`MonopolyRules`(構造的独占)とは別＝投機)／商人信用(`MerchantCreditRules`・#1077狼と香辛料＝信用更新`CreditworthinessTick`(失墜は速い)/与信枠`CreditLimit`/為替手形`BillOfExchangeValue`(信用割引)/レバレッジ`Leverage`/レバレッジ収益`LeveragedReturn`(両刃)/破産リスク`BankruptcyRisk`(過大なてこは一撃破産)＝信用は元手だが過大なてこは一度の失敗で破産。`BankRules`(銀行)とは別＝商人個人)／惑星防衛3層(`PlanetaryDefenseRules`＋enum`DefenseLayer`・#1070Almagest＝各層の迎撃`LayerInterception`/惑星到達残存`PenetratingForce`(各層を抜くたび痩せる)/層の相乗`LayerSynergy`(単独の和を超える)/最弱層`WeakestLayer`(突破口)/縦深ボーナス`DefenseDepthBonus`/制空権争い`OrbitalSupremacyContest`＝3層で守り層の相乗が和を超える。`PlanetSiegeRules`(攻城)の防御側)／指揮容量(`FleetCapRules`・#1067Almagest＝指揮容量`CommandCapacity`(統率×階級)/配備可能艦数`DeployableShips`/階級上限`RankCapLimit`/実効配備`EffectiveDeployable`(容量と階級の二重制約のmin)/超過ペナルティ`OverCapacityPenalty`＝能力と階級の二重の上限。`OrderOfBattle`(編制)とは別＝指揮容量の制約)／功績昇進(`MeritPromotionRules`・#1064Almagest＝戦功`MeritGain`(格上撃破)/昇進閾値`MeritForNextPromotion`(逓増)/昇進可否`PromotionReady`/最大編成`MaxFormationSize`/功績減衰`MeritDecay`/昇進頭打ち`PromotionStagnation`＝戦功が昇進を生み上に行くほど壁は高い。`MeritRankRules`(軍功爵位)とは別＝指揮できる部隊数)／作戦適性(`OperationalAptitudeRules`＋enum`CombatType`/`AptitudeGrade`・#1063Almagest＝適性倍率`AptitudeMultiplier`(S大幅増/E大幅減)/実効能力`EffectivePerformance`(基準非破壊)/等級判定`GradeFromScore`/最適戦闘`BestCombatType`(適材適所)/不適合の罰`MismatchPenalty`(守りの名手を攻めに使う愚)/適性のぶつかり合い`TerrainMatchBonus`＝提督には得意な戦場がある。`AdmiralData`能力の地形別補正)／調達三択(`RefitPurchaseRules`＋enum`AcquisitionMethod`・#1068Almagest＝改装コスト`RefitCost`(古い艦体ほど割増)/改装の性能上限`RefitPerformanceCeiling`(老朽艦は新造に及ばない)/建造vs購入`BuildVsBuyDecision`(急ぐなら買う)/購入依存`PurchaseDependency`/改装vs新造`RefitVsReplaceValue`(艦齢が分岐点)/最適調達推奨`AcquisitionRecommendation`＝改装は安いが限界・新造は高いが自由・購入は速いが依存。`ShipyardRules`(新造)/`ArmamentDesignRules`(設計)とは別＝改装の経済)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**技術ツリー・エンディング分岐・会戦×政治の純ロジック追加（Wave29・並列4・配線待ち・適合Issueプール枯渇＝自律量産の終端）**：技術ツリー(`TechTreeRules`/`TechNode`・ALM #1065＝前提依存グラフ＝基礎→前提充足`IsUnlockable`で解禁・前提充足率`PrerequisitesMet`・解禁最前線`AvailableTechs`/`ResearchableFrontier`・段数`TechDepth`(再帰・`maxDepth`で循環打切)・到達総コスト`TotalCostToReach`(合流前提は1回)・飛び級不可`Leapfrog`。`ResearchRules`=研究進捗とは別＝前提グラフ)／エンディング分岐(`EndingBranchRules`/`EndingType`{覇道統一/王道統一/共和革命/割拠均衡/滅亡/隠遁}/`EndingCondition`・ALM #1061＝評判×同盟数×必須イベントの全充足`MeetsCondition`・到達可能`EligibleEndings`・優先度最高を選び滅亡フォールバック`SelectEnding`・近さ`EndingProximity`・王道/覇道分岐`PathDivergence`・真エンディング`IsTrueEnding`(全条件×統一のみ)。`DisclosureRules`=開示連鎖/`WangDaoRules`=王道覇道とは別＝最終分岐スコアリング)／君主の親征(`RoyalPresenceRules`・#899＝臨御士気`MoraleBonus`(カリスマ比例・後方0)→戦力`CombatBonus`・戦死リスク`MonarchCasualtyRisk`(激戦↑護衛↓後方0上限)＋`MonarchFalls`(roll)・威信`PrestigeFromPresence`(親征勝+/敗−/後方は常減＝戦わぬ王は侮られる)・戦死の継承危機`SuccessionCrisisOnDeath`(→`SuccessionWarRules`)・損得`PresenceDecision`/`ShouldTakeField`＝勝てば英雄・死ねば国傾く・退けば侮られる両刃。`ReputationRules`/`IllnessRules`とは別)／会戦指揮の正統性(`CommandLegitimacyRules`/`ObedienceLevel`{完全服従/渋々服従/部分的不服従/不服従}・#898＝形式権威×個人武勇×将兵忠誠の積`CommandLegitimacy`→服従段階・命令実行倍率`OrderComplianceFactor`(実効値)・部分不服従`PartialDisobedienceRisk`/`PartialDisobedienceOccurs`(roll)・非正統の士気減`MoralePenaltyFromIllegitimacy`・勝利の正統性強化`AuthorityFromVictory`＝なぜプレイヤーは会戦を指揮できるのか。`CivilianControlRules`=クーデターとは別＝戦場の命令服従)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**群集の狂気・金融危機の純ロジック追加（Wave30・並列7・配線待ち・第5次補充で量産再開）**：告発カスケード(`AccusationCascadeRules`・マッカイMNIA-4 #1625＝損失×責任転嫁の告発圧力`ScapegoatPressure`/ロジスティック自己増殖`CascadeTick`(一件が次を呼ぶ・制度抑制`InstitutionalBrake`で減衰＝強い法治だけが止める)/次の標的`NextTargetEmergence`(roll)/冤罪比率`FalseAccusationRatio`(熱狂で上昇)/魔女狩り`IsWitchHunt`/沈静`Subsidence`。`PurgeRules`/`TerrorRules`とは別＝マニア崩壊後の責任転嫁連鎖)／逆張り迫害(`ContraryPositionRules`・MNIA-3 #1624＝迫害コスト`PersecutionCost`(ピークで逆張りは最も叩かれる)/事後名声`VindicationGain`(崩壊後にのみ報われる)/純損益`NetPayoff`(生存で逆転・潰されれば損のみ)/生存`SurvivalChance`(roll)/タイミング`TimingQuality`/予言者`IsProphet`＝早すぎる正しさは破滅と紙一重。`PreferenceFalsificationRules`とは別)／バブル価格解離(`BubblePriceRules`・MNIA-2 #1622＝熱狂の吊り上げ倍率`BubbleMultiplier`/市場価格`MarketPrice`(上側オーバーシュート)/底割れ倍率`OvershootMultiplier`/崩壊価格`CrashPrice`(適正以下＝下側オーバーシュート)/解離度`Deviation`/バブル判定`IsBubble`/実需回帰`MeanReversionTick`＝両側の行き過ぎは時間で均衡へ。`CorneringRules`/`MarketRules`とは別＝信念が値を実需から解離)／ブーム詐欺(`BoomFraudRules`・キンドルバーガーKNDB-5 #1621＝好況の詐欺生起`FraudEmergenceChance`(熱狂が審査を緩める)/好況中の隠蔽`ConcealmentDuringBoom`/収縮の発覚`ExposureChance`(潮が引くと露わ)/信頼崩壊`TrustCollapse`/詐欺ストック動学`AccumulatedFraudTick`/連鎖リスク`SystemicRisk`＝好況が詐欺を育て不況が暴く。`EspionageRules`/`ScandalRules`とは別・EventEngine接続想定の純ロジック部)／信念感染SIR(`ManiaRules`/`ManiaState`・MNIA-1 #1620＝感受性→感染→回復の`Tick`(βSI/γI・保存則)/基本再生産数`BasicReproduction`(R0=β/γ)/流行判定`WillSpread`/ピーク`PeakInfected`/集団免疫閾値`HerdImmunityThreshold`(1−1/R0)/感受性`Susceptibility`(既往感染は免疫)/横断熱狂`CrossDomainIntensity`(経済/政治/宗教)＝信念は感染症のように広がり醒める。`ReligionRules`/`PropagandaRules`とは別＝SIR数理の汎用)／負債デフレ(`DebtDeflationRules`・フィッシャー型KNDB-4 #1619＝実質債務`RealDebtBurden`(物価下落で膨張)/膨張率`RealDebtChange`/投げ売り圧`DistressSelling`/投げ売りの物価下押し`PriceImpactOfSelling`(薄市場で増幅)/スパイラル`DeflationSpiralTick`(物価→実質債務→投げ売り→物価下落の自己強化)/突入判定`IsDebtDeflation`/債務減免の出口`DebtRelief`/深刻度`SpiralSeverity`＝返済努力が傷を深める逆説。`InflationRules`/`FiscalRules`とは別)／金融伝染(`FinancialContagionRules`・KNDB-3 #1615＝伝染力`TransmissionStrength`(エクスポージャー×震源)/伝染`ContagionTick`(隣のストレスが流入・防火壁で減衰)/防火壁`FirewallEffectiveness`(流動性供給＋資本規制)/相関崩壊`CorrelationBreakdown`(危機で1へ＝分散の幻想)/実効分散`EffectiveDiversification`(危機で消える)/取付け`BankRunProbability`(roll)/系崩壊リスク`SystemicCollapseRisk`/封じ込め`ContainmentThreshold`＝つながりが太いほど速く伝わり防火壁だけが断つ。`BankRules`/`ChainFragilityRules`とは別＝金融ショックの面的波及)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**輸送コスト動学・金融危機の制度の純ロジック追加（Wave31・並列7・配線待ち）**：規格化の外部性(`StandardizationRules`・レビンソンCNTR-3 #1614＝採用度の二乗で増すネットワーク価値`NetworkValue`/輸送コスト低減`TransportCostReduction`/ロジスティック自己強化`AdoptionTick`(互換コストが抵抗)/誘因`AdoptionIncentive`/孤立コスト`HoldoutPenalty`/臨界点`TippingPoint`/規格戦争`StandardWar`(勝者総取り)＝使う者が増えるほど価値を増す。`TradeRules`/`LogisticsRules`とは別)／最後の貸し手(`LenderOfLastResortRules`・バジョット/キンドルバーガーKNDB-2 #1613＝必要流動性`LiquidityNeeded`/取付け鎮静`PanicArrest`(無制限供給)/罰則金利`PenaltyRate`(高金利の選別)/担保掛け目`CollateralHaircut`(優良担保)/モラルハザード蓄積`MoralHazardBuildup`(救済常態化)と抑制`MoralHazardMitigation`/流動性不足vs支払不能`IlliquidVsInsolvent`/三原則`IsBagehotCompliant`＝高金利×優良担保がなければ救済が無謀を育てる。`BankRules`/`FinancialContagionRules`とは別)／ハブ積み替え(`TransshipmentRules`・レビンソンCNTR-2 #1612＝処理量`HubThroughput`(能力か需要が律速)/規模の経済`ScaleEconomyFactor`/周辺コスト低減`NeighborCostReduction`/産出波及`OutputBoost`/混雑逓減`CongestionPenalty`/ハブ重力`HubGravityTick`(集まるほど効率→さらに集まる)/採算`HubViability`＝荷が集まるほど効率が上がり混雑で頭打ち。`LogisticsRules`とは別＝物流ハブの集約)／輸送コスト係数(`TransportCostRules`・レビンソンCNTR-1 #1611＝回廊コスト`CorridorCost`(距離×規格化割引×混雑割増)/実効距離`EffectiveDistance`/連結重み`ConnectionWeight`(二値連結の連続化)/加重一体化`WeightedCohesion`/経路コスト`CostToTraverse`/コンテナ化低減`ContainerizationGain`/要衝プレミアム`ChokepointPremium`＝安い回廊が多いほど版図の実効一体化が強まる。`LogisticsRules`の二値連結を連続化)／危機サイクル(`CrisisCycleRules`/`MinskyPhase`{変位/熱狂/恐慌/収縮}・ミンスキーKNDB-1 #1610＝相判定`PhaseOf`/相循環`NextPhase`/レバレッジ蓄積`LeverageBuildup`(安定が不安定を生む)/脆弱性`FragilityIndex`(熱狂ピークで最も脆い)/反転トリガー`ReversalTrigger`(roll)/相進行`PhaseProgressTick`/ヘッジ投機ポンツィ`HedgeSpeculativePonzi`/ミンスキーモーメント`IsMinskyMoment`＝安定が脆弱性を積み熱狂が恐慌へ反転し循環。`ArmsRaceRules`の螺旋とは別)／制度の儀礼性(`CeremonialismRules`・ヴェブレンVEBL-4 #1603＝儀礼的価値`CeremonialValue`(威信×伝統)/機能不全でも存続`SurvivalDespiteDysfunction`(機能ゼロでも儀礼性で残る)/廃止抵抗`AbolitionResistance`/形骸化ドリフト`CeremonialDrift`(実質→形式)/格式維持費`PrestigeUpkeepCost`/空虚な制度`IsHollowInstitution`/改革難度`ReformDifficulty`＝制度は機能を失っても威信と前例で生き延びる。`BureaucracyBloatRules`の人数肥大とは別)／社会保護のラチェット(`SocialProtectionRules`・ポランニー二重運動POLA-5 #1602＝保護需要`ProtectionDemand`(市場圧力×不安定化)/保護の積み上がり`ProtectionBuildup`(危機で速い)/ラチェット抵抗`RatchetResistance`(受益者が盾)/縮みにくさ`ProtectionDecay`/効率トレードオフ`MarketEfficiencyTradeoff`/安定寄与`StabilityGain`/二重運動の緊張`DoubleMovementTension`/過保護`IsOverprotected`＝市場の圧力が保護を生み撤廃に抵抗する。`RedistributionRules`の税再分配とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**有閑階級・市場社会の純ロジック追加（Wave32・並列7・配線待ち）**：誇示的浪費(`OstentationRules`・ヴェブレンVEBL-3 #1601＝正統性上昇`LegitimacyGain`(逓減)/財政圧迫`FiscalStrain`(身の丈超過の二乗)/純効果`NetLegitimacy`(両刃)/慣れ`DiminishingReturns`/長期崩壊リスク`LongTermCollapseRisk`/適正浪費`OptimalSpending`/破滅的奢侈`IsRuinousLuxury`＝目立つ浪費が正統性を買うが身の丈を超えると国庫を蝕む。`CeremonyRules`/`HonorsRules`とは別)／多極経済秩序(`InternationalOrderRules`・ポランニー四本柱POLA-4 #1599＝秩序安定`OrderStability`(最弱柱が律速)/相互支持`MutualSupport`/連鎖`CascadeTick`(一本が弱ると隣も弱る)/最弱柱`WeakestPillar`/崩壊伝染`CollapseContagion`/崩壊判定`OrderCollapsed`/再建難度`RestorationDifficulty`/冗長性`Resilience`＝複数の柱の相互依存で立ち最弱の一本が連鎖崩壊を呼ぶ。`BalanceOfPowerRules`/`CollectiveSecurityRules`とは別)／競争的民主主義(`CompetitiveDemocracyRules`・シュンペーターSCHU-6 #1598＝選挙競争`ElectoralCompetition`(指導者を選ぶ競争市場)/置換ショック`DisplacementShock`/扇動の隙`DemagogueOpening`/民主的品質`DemocraticQuality`/品質劣化`QualityErosionTick`/説明責任`AccountabilityStrength`/後退判定`IsDemocraticBacksliding`＝民主主義は票を競う手続きで経済の置換ショックが扇動を呼び品質を蝕む。`DemagogueRules`/`PartyRules`とは別)／金銭的模倣(`EmulationRules`・ヴェブレンVEBL-2 #1597＝模倣圧`EmulationPressure`/消費規範底上げ`ConsumptionNormShift`/下方カスケード`CascadeDownward`(滝の伝播)/見栄コスト`KeepingUpCost`(身の丈超過)/安定低下`StabilityErosion`/需要押し上げ`DemandBoost`/地位のランニングマシン`StatusTreadmill`＝人は一つ上を真似て消費規範が下方へ波及し見栄が安定を削る。`MarketRules`とは別)／擬制商品(`FictitiousCommodityRules`/`FictitiousCommodity`{労働/土地/貨幣}・ポランニーPOLA-3 #1596＝商品化ストレス`CommodificationStress`/労働摩耗`LaborWear`/土地破壊`LandDegradation`/貨幣不安定`MoneyInstability`/保護需要`ProtectionDemandFromStress`(二重運動)/紐帯崩し`SocialUnravelingTick`/脱商品化緩和`DecommodificationRelief`＝本来商品でない労働土地貨幣の完全商品化が固有ストレスを生み保護を呼ぶ。`MarketRules`/`SocialProtectionRules`の対)／知識人批判(`IntellectualCritiqueRules`・シュンペーターSCHU-5 #1595＝知識人余剰`IntellectualSurplus`(繁栄が養う)/不満`UnderemployedDiscontent`(職にあぶれる)/批判圧`CritiquePressure`/正統性侵食`LegitimacyErosionTick`/取り込み`CooptationEffect`(飼い慣らし)/弾圧の逆効果`RepressionBacklash`/自壊度`SelfUnderminingIndex`/知識人反乱`IsIntelligentsiaRevolt`＝繁栄が職にあぶれた知識人を生み批判が正統性を内側から侵食する。`FreePressRules`/`SecretSocietyRules`とは別)／地位財(`VeblenGoodsRules`/`StatusGood`・ヴェブレンVEBL-1 #1593＝逆需要`VeblenDemand`(高価格ほど需要増)/誇示価値`ConspicuousValue`/希少プレミアム`ExclusivityPremium`/スノッブ効果`SnobEffect`(普及で凡庸化)/地位シグナル`StatusSignal`/最適高価格`OptimalVeblenPrice`/地位財判定`IsVeblenGood`(正の価格弾力性)/模倣品希釈`CounterfeitDilution`＝地位財は高くて他人が買えないことに価値があり普及すると地位財でなくなる。`MarketRules`の右下がり需給とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**創造的破壊・道徳感情の純ロジック追加（Wave33・並列7・配線待ち）**：創造的破壊(`CreativeDestructionRules`・シュンペーターSCHU-1 #1581＝破壊力`DestructionForce`/旧産業萎縮`IncumbentDecayTick`/創造の成長`CreationGain`/純成長`NetGrowth`(嵐の収支)/置換ショック`DisplacementShock`/適応の遅れ`AdaptationLag`/束の間の超過利潤`SchumpeterianRent`/ディスラプション`IsDisruption`＝成長は創造と破壊の嵐。`InnovationDiffusionRules`/`ResearchRules`とは別＝新陳代謝の破壊側)／企業家類型(`EntrepreneurRules`/`EntrepreneurType`{企業家/管理者}・SCHU-2 #1584＝企業家精神`EntrepreneurialDrive`/類型弁別`TypeOf`/新結合産出`InnovationOutput`/運営の逆相関`ManagerialEfficiency`(破壊者は運営に向かない)/起業の成否`StartupSuccess`(roll)/破壊圧`CreativeDisruption`/破滅確率`RiskOfRuin`/起業環境`ClimateMultiplier`＝企業家は均衡を壊すイノベーターで管理者とは別の資質。`PersonRules`/`FirmRules`とは別)／官僚化(`BureaucratizationRules`・SCHU-3 #1587＝官僚化進行`BureaucratizationTick`(成功が手続きを増やす)/革新力`InnovationCapacity`(逆相関)/革新のルーティン化`RoutinizationOfInnovation`/人材流出`EntrepreneurExodus`/自壊`SelfUnderminingTick`/硬直化`Ossification`/改革コスト`RevitalizationCost`/革新死`IsInnovationDead`＝成功が組織を官僚化させ革新を窒息させる墓掘り。`BureaucracyBloatRules`の人数肥大とは別)／長波(`InnovationWaveRules`/`KondratievPhase`{上昇/繁栄/後退/不況}・SCHU-4 #1591＝相判定`PhaseOf`/相循環`NextPhase`/クラスター形成`ClusterFormation`(不況が次の革新を準備)/普及`DiffusionTick`(S字)/飽和の後退`SaturationDrag`/波の振幅`WaveAmplitude`/景気の勢い`EconomicMomentum`/長波の谷`IsLongWaveTrough`＝革新は束で現れ普及と飽和が数十年の長波をつくる。`CrisisCycleRules`の金融循環とは別)／共感評判(`EmpathyRules`・スミス道徳感情論TMS-1 #1578＝共感反応`SympatheticResponse`/是認否認`MoralApproval`/適宜性`Propriety`(中庸)/支持`SupportModifier`/忠誠`LoyaltyModifier`/opinion`OpinionShift`/憤り`ResentmentFromInjustice`/道徳的運`MoralLuck`＝人は他者の境遇に共感して是認/否認しそれが支持忠誠opinionを動かす。`ReputationRules`/`JusticeRules`とは別)／公平な観察者(`ImpartialObserverRules`・スミスTMS-2 #1582＝自己欺瞞バイアス`SelfDeceptionBias`/観察者の強さ`ObserverStrength`(見られる意識が良心を育てる)/公平への補正`CorrectedJudgment`/腐敗ブレーキ`CorruptionBrake`/自己合理化`MoralRationalization`/良心の涵養`ConscienceTick`/自己腐敗`IsSelfCorrupted`/実徳vs虚栄`PraiseworthinessVsPraise`＝私利は自己を甘く見る自己欺瞞を生み内なる観察者がそれを正す。`RegimeRules`の腐敗とは別)／三徳統治(`MoralStyleRules`/`MoralVirtue`{慎慮/仁愛/正義}・スミスTMS-3 #1586＝慎慮`PrudenceEffect`/仁愛`BenevolenceEffect`/正義`JusticeEffect`(重み最大)/統合安定`StabilityModifier`/徳のバランス`VirtueBalance`/正義の土台ゲート`JusticeAsFoundation`(欠けると他徳が活きない)/支配的な徳`DominantVirtue`/徳の過剰`ExcessOfVirtue`＝慎慮仁愛正義の三徳が統治スタイルを決め正義は社会を支える大黒柱。`GovernanceRules`/`WangDaoRules`とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**商業道徳・人口論・儒家政治の純ロジック追加（Wave34・並列7・配線待ち）**：商業誠実性(`CommercialIntegrityRules`・スミスTMS-4 #1590＝信頼蓄積`TrustAccumulation`(反復で積む)/裏切り`TrustBreach`(一気に崩す非対称)/誠実評判`IntegrityReputation`/opinion修正`OpinionModifier`/裏切り誘惑`DefaultTemptation`(失うものが大きいと裏切らない)/反復配当`RepeatDealingBonus`/回復の非対称`RecoveryAsymmetry`/信頼相手`IsTrustedPartner`＝繰り返しの交易が信頼を積み裏切りは一気に崩すが回復は遅い。`TradeRules`/`DiplomacyRules`とは別)／食糧天井(`CarryingCapacityRules`・マルサスMALT-1 #1574＝収容力`CarryingCapacity`/食糧ストレス比`FoodStressRatio`(マルサス層の主要出力)/余剰`FoodSurplus`/生存圧`SubsistencePressure`(天井超過で非線形)/マルサスの罠`MalthusianTrap`(技術だけが出口)/収容力漸増`CapacityGrowthTick`/過剰人口`IsOvercapacity`/安全人口`OptimalPopulation`＝食糧が人口の天井を決める。`DemographicsRules`/`ResourceProductionRules`とは別)／マルサスチェック(`MalthusianCheckRules`・MALT-2 #1575＝予防的妨げ`PreventiveCheck`(出生抑制)/積極的妨げ`PositiveCheck`(死亡増)/出生変調`BirthRateModifier`/死亡変調`DeathRateModifier`/純人口圧`NetPopulationPressure`/飢饉死亡`FamineMortality`/均衡引き戻し`EquilibriumPull`/危機`IsMalthusianCrisis`＝食糧逼迫が出生を抑え死亡を増やし人口を食糧水準へ引き戻す。`DemographicsRules`のVitalRatesとは別＝FoodStressRatioを入力)／救貧の逆説(`PoorLawRules`・MALT-4 #1580＝短期救済`ShortTermRelief`/出生刺激`FertilityStimulus`/人口圧`PopulationPressureTick`/賃金希釈`WageDilution`/純効果`NetWelfareEffect`(時間で正→負)/逆説度`ParadoxIndex`/生存水準収束`LongRunSubsistence`(マルサスの鉄則)/福祉の罠`IsWelfareTrap`＝救貧は短期に救うが出生を刺激し長期に賃金を帳消しにする慈悲の逆説。`RedistributionRules`/`SocialProtectionRules`とは別)／四端(`MoralSproutsRules`/`MoralSprouts`{惻隠/羞悪/辞譲/是非}・孟子MENC-1 #1564＝涵養`CultivationTick`(善政が育てる)/萎縮`WitheringTick`(暴政が萎ます)/端→徳`VirtueFromSprouts`(仁義礼智)/公徳心`CivicVirtue`/社会調和`SocialHarmony`/性善の下限`InnateGoodness`/義憤`RighteousIndignation`/道徳的覚醒`IsMoralAwakening`＝人は四つの善の芽を持ち善政が育て暴政が萎ませる民の道徳的素地。`MoralStyleRules`の為政者三徳とは別＝被治者側)／仁政vs覇道(`GovernanceStyleRules`/`GovernanceStyle`{王道/覇道}・孟子MENC-3 #1568＝覇道力`HegemonicPower`(短期最強)/王道力`KinglyPower`(遅効)/王道蓄積`KinglyAccumulationTick`/覇道減衰`HegemonicDecayTick`/実効統治力`EffectivePower`(時間軸で逆転)/追い越し時点`CrossoverTime`/持続可能性`Sustainability`/民心`HeartsAndMinds`/覇道の脆さ`IsRegimeBrittle`＝王道は遅いが長く持続し覇道は短期最強だが長続きしない。`WangDaoRules`の主義ドリフトとは別＝時間動態)／浩然之気(`MoralForceRules`/`MoralForce`・孟子MENC-4 #1570＝義の蓄積`AccumulationTick`(ゆっくり育つ)/不義の急落`InjuryFromInjustice`(一気に損なう)/助長の弊害`NoForcingPenalty`(無理に育てると枯れる)/忠誠係数`LoyaltyCoefficient`/カリスマ係数`CharismaCoefficient`/不動の意志`UnshakableResolve`(至大至剛)/一貫性`ConsistencyTick`/浩然の気`IsFloodlikeQi`＝義の積み重ねでしか育たず一度の不義で損なわれ無理に育てると枯れる不動の気力。`FocusRules`の三密一時バフとは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**諸子百家・自由主義の純ロジック追加（Wave35・並列7・配線待ち）**：守城ギルド(`DefenseGuildRules`/`DefenseGuild`・墨子MOZI-1 #1555＝守城ボーナス`DefensiveBonus`(要塞と相乗)/攻城耐性`SiegeResistance`(寡兵でも守り抜く)/請負判断`WillDefend`(大義重視・金だけでは動かない)/攻撃拒否`RefusesOffense`/名声`ReputationGain`/籠城消耗`AttritionUnderSiege`/技術伝承`KnowledgeTransfer`/難攻不落`IsImpregnableDefense`＝守城専門の非国家請負＝攻めず大義に応じ守る。`MercenaryRules`/`FortressRules`とは別)／尚賢(`CompetenceLegitimacyRules`・墨子MOZI-4 #1565＝能力本位度`MeritocracyIndex`/正統性保全`LegitimacyPreservation`/無能登用ペナルティ`IncompetencePenalty`/人材誘引`TalentAttraction`/民の信頼`PopularConfidence`/縁故の侵食`NepotismDecayTick`/選抜`ElevateWorthy`/尚賢判定`IsMeritocraticRegime`＝賢者を能力本位で登用すれば正統性が保たれ人材が集まる。`MeritPromotionRules`/`SeniorityRules`とは別)／節用(`FrugalityDoctrineRules`・墨子MOZI-5 #1567＝財政効率`FiscalEfficiency`/産出増`OutputGain`/貴族合意低下`NobleConsent`/民の益`CommonerBenefit`/倹約疲れ`AusterityFatigue`/総合`NetGovernanceEffect`/奢侈禁止`SumptuaryEnforcement`/過度な倹約`IsExcessiveAusterity`＝倹約は国庫を富ませ民を益すが誇示的浪費を奪われた貴族の合意を下げる。`OstentationRules`の対極)／非攻(`NonAggressionDoctrineRules`・墨子MOZI-2 #1560＝外交信用`DiplomaticCredibility`(実績重視)/信用資本`TrustCapital`/攻撃放棄`OffenseForfeit`/防衛の正統性`DefensiveLegitimacy`/同盟誘引`AllianceAttraction`/違反の信用崩壊`CommitmentBreach`(一気・非対称)/評判抑止`DeterrenceViaReputation`/信頼できる非攻国`IsCrediblyPacifist`＝自己拘束が外交信用を生むが先制攻撃を放棄し破れば一瞬で崩れる。`DiplomacyRules`/`DeterrenceRules`とは別)／柔弱(`WaterDoctrineRules`・老子LAOZ-4 #1558＝短期の屈服`ShortTermYield`/長期の回復`LongTermRecovery`/回復の非対称`RecoveryAsymmetry`(柔は戻り剛は折れたら戻らない)/柔が剛に勝つ`SoftOvercomesHard`(水が岩を穿つ)/浸透力`PenetrationForce`/士気の床`ResilientMoraleFloor`/受け流し`AdaptiveResponse`/折れない`IsUnbreakable`＝柔弱は短期に押されて退くが折れず長期の回復力と浸透力で剛強に勝つ。`WuWeiRules`/`ReversalRules`とは別)／自生的秩序(`SpontaneousOrderRules`・ハイエクHAYK-6 #1556＝秩序の漸成`OrderFormationTick`(自由×分散知識で遅く育つ)/介入の侵食`InterventionErosion`(速い)/市場効率`MarketEfficiency`/知識活用`KnowledgeUtilization`(中央計画が殺す)/秩序の頑健性`OrderResilience`/回復の非対称`RecoveryAsymmetry`/設計過剰`OverDesignPenalty`/崩壊`IsOrderCollapsing`＝自生的秩序は自由と分散知識から遅く育ち強制介入で速く侵食される。`MarketRules`/`GovernanceRules`とは別)／市場の埋め込み(`EmbeddednessRules`/`MarketEmbeddedness`・ポランニーPOLA-1 #1588＝埋め込み度`EmbeddednessLevel`/市場効率`MarketEfficiency`(脱埋め込みで↑)/社会安定`SocialStability`(埋め込みで↑)/脱埋め込み`DisembeddingTick`/埋め戻し`ReembeddingTick`(保護と連動)/効率安定トレードオフ`EfficiencyStabilityTradeoff`(中庸で最大)/混乱リスク`DislocationRisk`/脱埋め込み判定`IsDisembedded`＝市場が社会に埋め込まれるほど安定だが効率は低く自由化は効率を上げ社会を不安定化させる。`SocialProtectionRules`/`FictitiousCommodityRules`の二重運動を完結)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**マクロ経済学・計画経済批判の純ロジック追加（Wave36・並列7・配線待ち）**：有効需要(`EffectiveDemandRules`・ケインズKEYN-1 #1540＝総需要`AggregateDemand`(C+I+G)/現実産出`ActualOutput`(需要が産出を決める＝セイの法則否定)/産出ギャップ`OutputGap`/遊休資源`IdleResources`/失業`UnemploymentFromGap`(オークン)/需要刺激`DemandStimulus`/インフレギャップ`InflationaryGap`/需要制約`IsDemandConstrained`＝需要不足が潜在産出を遊休させる。`MarketRules`/`FiscalRules`とは別＝マクロ需要)／財政乗数(`MultiplierRules`・KEYN-2 #1542＝乗数`SpendingMultiplier`(k=1/(1−c))/総効果`TotalImpact`/漏れ調整`LeakageAdjustedMultiplier`(貯蓄税輸入)/連鎖`MultiplierRound`(幾何級数)/収束`ConvergedTotal`/減税乗数`TaxMultiplier`(支出より小)/均衡財政乗数`BalancedBudgetMultiplier`(=1ハーヴェルモ)＝支出が所得連鎖でk倍に増幅し漏れが縮める。`FiscalRules`とは別)／血気(`AnimalSpiritsRules`/`InvestmentClimate`・KEYN-3 #1545＝投資意欲`InvestmentAppetite`(心理が駆動)/信認変動`ConfidenceTick`(群れるほど振れる)/投資凍結`InvestmentFreeze`/悲観スパイラル`PessimismSpiral`(自己強化)/楽観スパイラル`OptimismSpiral`/美人投票`BeautyContest`(他人の予想を予想)/血気の回復`SpiritRevival`/信認崩壊`IsConfidenceCollapse`＝投資は血気に左右され信認崩壊が投資を凍結し悲観を強める。`StockMarketRules`/`CrisisCycleRules`とは別)／流動性の罠(`LiquidityPreferenceRules`・KEYN-4 #1548＝貨幣需要`LiquidityDemand`(取引/投機/予備)/均衡金利`MoneyMarketRate`/ゼロ下限`ZeroLowerBound`/流動性の罠`IsLiquidityTrap`/金融政策の無力化`MonetaryPolicyEffectiveness`/財政政策の有効`FiscalPolicyEffectiveness`/投機的需要`SpeculativeMoneyDemand`(金利と逆相関)/退蔵`HoardingPressure`＝金利がゼロ下限に達すると金融政策が無効化し財政だけが有効。`BankRules`/`FiscalRules`とは別)／節約のパラドックス(`ThriftParadoxRules`・KEYN-5 #1552＝個人の便益`IndividualBenefit`(ミクロの美徳)/総需要低下`AggregateDemandDrop`/所得縮小`IncomeContraction`/逆説的貯蓄変化`ParadoxicalSavingsChange`(総貯蓄もかえって減る)/合成の誤謬`FallacyOfComposition`/集合的行為問題`CollectiveActionProblem`/協調失敗`CoordinationFailure`/デフレ罠`IsDeflationaryTrap`＝全員が一斉に節約すると総需要が崩壊し合成の誤謬になる。`MultiplierRules`とは別)／計画経済ドリフト(`PlanningDriftRules`/`PlanningDrift`/`PlannerChoice`・ハイエクHAYK-1 #1541＝介入ラチェット`InterventionRatchet`(失敗が次の介入を呼ぶ)/強制`CoercionTick`/自由の侵食`FreedomErosion`/権威主義圧力`AuthoritarianPressure`/知識代替`KnowledgeSubstitution`/坂道`SlipperySlope`/計画者のジレンマ`PlannersDilemma`(撤回か統制か)/隷属への道`IsRoadToSerfdom`＝中央計画は失敗を補う介入を累積させ自由を蝕み権威主義へ滑り落ちる。`SocialProtectionRules`の保護ラチェットとは別)／経済計算問題(`CalculationProblemRules`・ハイエクHAYK-2 #1544＝価格シグナルの質`PriceSignalQuality`/計算能力`CalculationCapacity`(計画化が二乗で削る)/配分効率`AllocationEfficiency`/生産性ペナルティ`ProductivityPenalty`/誤配分の累積`MisallocationTick`/情報損失`InformationLoss`(知識問題)/不足と過剰`ShortageAndSurplus`(共存)/計算混沌`IsCalculationChaos`＝価格なしには希少性を計算できず計画化が進むほど配分が非効率になる。`MarketRules`/`SpontaneousOrderRules`とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**自由主義・老荘・全体主義の純ロジック追加（Wave37・並列7・配線待ち）**：逆淘汰(`AuthoritarianSelectionRules`・ハイエクHAYK-3 #1547＝従順な大衆`DocileMassAppeal`/否定的結束`NegativeSolidarity`(共通の敵)/非情の優位`RuthlessnessAdvantage`(良心が障害)/選別圧`SelectionBias`/逆淘汰`AdverseSelectionTick`/良心が足枷`MoralityAsLiability`/従順の連鎖`ConformityCascade`/最悪が上`IsWorstOnTop`＝全体主義は従順な大衆・否定的結束・良心の不在ゆえ非情な者を選別する。`CoupRules`/`PurgeRules`とは別)／法の一般性(`LegalGeneralityRules`・HAYK-4 #1549＝法の支配指数`RuleOfLawIndex`(一般性×予測×平等適用)/恣意性`ArbitrarinessLevel`/法的安定`LegalCertainty`/合意の侵食`ConsentErosion`/抵抗権`ResistanceRight`/予測可能性`PredictabilityBonus`/法の前の平等`EqualityBeforeLaw`/法による支配`IsRuleByLaw`＝法は万人に等しい事前ルールで恣意的個別命令が増えると合意が撤回され抵抗権が発動する。`MagnaCartaRules`/`ConstitutionRules`とは別)／二つの自由(`EconomicFreedomRules`・HAYK-5 #1553＝経済的自由`EconomicFreedom`/政治的自由の連動`PoliticalFreedomLink`/協力係数`CooperationCoefficient`/国家依存`DependencyOnState`/自由に基づく安定`StabilityFromFreedom`/統制の逆説`ControlBackfire`/自由の補完`FreedomComplementarity`/隷従ドリフト`IsServitudeDrift`＝経済的自由は政治的自由の前提で経済統制は協力を強制に変える。`ConsentRules`/`SpontaneousOrderRules`とは別)／無為(`WuWeiRules`・老子LAOZ-1 #1546＝自然な安定`NaturalStability`/介入の逆U字`InterventionEffect`(最適点で最大・超えると負)/過統治ペナルティ`OverGovernancePenalty`/控えめのボーナス`MinimalInterventionBonus`/自己秩序化`SelfOrderingTick`/最適介入点`OptimalInterventionPoint`/過介入のダメージ`MeddlingDamage`(小魚を煮崩す)/統治しすぎ`IsOverGoverned`＝少ない介入は自然な安定を生むが最適点を超えると逆効果。`GovernanceRules`とは別)／反者道之動(`ReversalRules`・老子LAOZ-2 #1550＝逆U字`InvertedU`(汎用数学)/反転点`TippingPoint`/ピーク後の反転`ReversalAfterPeak`/極端の反動`ExtremeBacklash`/逓減から反転`DiminishingThenReversing`/循環`CyclicalReturn`(盛衰の円環)/禍福`FortuneMisfortune`/ピーク通過`IsPastPeak`＝何事も極まると反対へ転じる汎用逆U字。`EscalationRules`とは別＝他モジュールが使う数学ユーティリティ)／知足(`ContentmentRules`・老子LAOZ-3 #1554＝満足度`ContentmentLevel`/適正規模ボーナス`AdequacyBonus`/貪欲のペナルティ`GreedPenalty`(足るを知らざる禍)/小国寡民`SmallStateStability`/拡大願望`AspirationDrift`/知足の安定`SatisfactionStability`/適正規模`OptimalScale`/過拡張`IsOverreaching`＝適正規模に満足する国は安定し足るを知らず拡大を求めると禍を招く。`LogisticsRules`の一体化ペナルティ側の正側補完)／悪の凡庸性(`ThoughtlessnessRules`/`BanalityState`・アーレントBNAL-1 #1530＝道徳的主体性`MoralAgencyFactor`(階層×服従が奪う)/責任の拡散`ResponsibilityDiffusion`/加担リスク`AtrocityRisk`/無思考の深まり`ThoughtlessnessTick`(日常化)/官僚的距離`BureaucraticDistance`(数字としての人)/良心の覚醒`ConscienceActivation`(思考が悪を止める)/服従vs良心`ObedienceVsConscience`/悪の凡庸性`IsBanalEvil`＝階層の深さと服従規範が道徳的主体性を奪い思考停止が巨悪への加担を可能にする。`AtrocityRules`の虐殺実行とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**全体主義・政治社会学の純ロジック追加（Wave38・並列7・配線待ち）**：複数性(`PluralityRules`/`PoliticalSpace`・アーレントBNAL-2 #1532＝公的領域の活力`PublicRealmVitality`/共に行動する力`ActionCapacity`/原子化`AtomizationTick`/複数性の侵食`PluralityErosion`/全体主義`IsTotalitarian`/自発的結社`SpontaneousAssociation`/共にある権力`PowerFromTogetherness`/孤独の脆弱性`LonelinessVulnerability`＝視点の多様性が公的領域を成り立たせ全体主義は原子化で複数性を破壊する。`FreePressRules`/`ThoughtlessnessRules`とは別)／全体主義の動態(`TotalitarianRules`/`TotalitarianPressure`・BNAL-3 #1535＝恐怖`TerrorTick`/恐怖ループ`TerrorLoopGain`(核＝恐怖が原子化を強めさらに恐怖が効く)/イデオロギー代替`IdeologySubstitution`(虚構が現実を上書き)/原子化`AtomizationTick`/全体的支配`TotalControl`/抵抗の不可能`ResistanceImpossibility`/イデオロギーの暴走`IdeologicalMomentum`/固定`IsTotalitarianConsolidated`＝原子化・恐怖・イデオロギー代替で自己強化し全人格を掌握する。`SecurityRules`/`MartialLawRules`とは別)／戦争責任(`WarCrimesRules`/`AccountabilityChain`/`TrialOutcome`・BNAL-4 #1536＝個人有責性`IndividualCulpability`(階層×裁量×関与)/服従の抗弁`CanClaimObedience`(裁量なしのみ一部成立)/上官責任`SuperiorResponsibility`/命令者の責任`OrderGiverCulpability`/傍観者の共犯`BystanderComplicity`/判決`TrialVerdict`/強要の減刑`MitigationFromDuress`/明白な違法命令`IsManifestlyIllegal`＝命令に従っただけの抗弁は裁量がなければ一部成立するが明白に違法な命令や裁量ある関与は免責されない。`TribunalRules`/`AtrocityRules`とは別)／政治倫理(`PoliticalEthicsRules`/`PoliticalEthicsType`{心情倫理/責任倫理/均衡}・ウェーバーWEBR-2 #1528＝倫理の軸`EthicsOrientation`/類型`TypeOf`/心情の純粋さ`ConvictionPurity`/責任の強さ`ConsequenceResponsibility`/原則のコスト`PrincipleCost`(無責任の罠)/実用の摩耗`PragmaticErosion`(原則喪失)/成熟した判断`MatureJudgment`(両方要る)/無責任な理想主義`IsIrresponsibleIdealism`＝政治には動機の純粋さと結果への責任の両方が要る。`MoralStyleRules`/`JusticeRules`とは別)／政治の職業化(`PoliticalVocationRules`/`VocationOrientation`{召命型/生業型/均衡}・WEBR-3 #1531＝召命vs生業の軸`VocationOrientationValue`/類型`TypeOf`/理想への献身`IdealismDrivenService`/出世主義`CareerismDrift`/党機械の官僚化`PartyMachineBureaucratization`/腐敗傾性`CorruptionPropensity`/政治家三要件`PassionResponsibilityProportion`/自己犠牲`SacrificeWillingness`/職業政治家`IsCareerPolitician`＝政治を使命とする者は理想に殉じうるが生計の手段とする職業政治家は官僚化・腐敗しやすい。`PatronageRules`/`PartyRules`とは別)／ツェーザリズム(`PlebiscitaryRules`・WEBR-4 #1533＝直接の負託`DirectMandate`(カリスマ×大衆動員)/議会の迂回`ParliamentaryBypass`/人民投票の正統性`PlebiscitaryLegitimacy`/ツェーザリズムのリスク`CaesarismRisk`/喝采の力学`AcclamationDynamics`/指導者大衆の短絡`LeaderMassShortCircuit`/正統性の移ろい`MandateVolatility`/ツェーザリズム`IsCaesarist`＝カリスマ指導者が大衆を直接動員し議会を飛び越して人民投票で正統性を得る。`DemagogueRules`/`PlebisciteRules`とは別)／足による投票(`PopulationMigrationRules`・孟子MENC-2 #1566＝統治の魅力`GovernanceAttractiveness`/移住の引力`MigrationPull`/仁政の流入`BenevolenceInflux`(隣国の苛政から吸引)/苛政の流出`MisruleExodus`/人口移動量`PopulationFlow`/好循環`VirtuousCycleBonus`/頭脳流出`BrainDrainFromMisrule`(有能層を先に失う)/過疎化`IsDepopulating`＝仁政は民を水のように吸引し苛政は民を流出させる徳治版の足による投票。`MigrationRules`の汎用版とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。／**君主論・戦争論の純ロジック追加（Wave39・並列7・配線待ち）**：征服地統治(`ConquestGovernanceRules`/`ConquestStrategy`{駆逐/植民/傀儡}・マキャヴェッリMKV-1 #1139＝統合速度`IntegrationSpeed`/裏切りリスク`BetrayalRisk`/初期コスト`UpfrontCost`/恨み`Resentment`/長期安定`LongTermStability`/傀儡の忠誠`PuppetLoyalty`/最適戦略`OptimalStrategy`/統合`IsConsolidated`＝征服地保持の三様は統合速度と裏切りリスクがトレードオフ。`GovernanceRules`/`ColonizationRules`とは別)／恐怖と憎悪(`FearVsHatredRules`・MKV-2 #1140＝恐れ`FearLevel`(規律ある強制)/憎悪`HatredLevel`(恣意×略奪)/恐れの安定`ControlFromFear`/憎悪の転覆`SubversionFromHatred`/恐れて憎まれない領域`FearWithoutHatred`/憎悪への越境`CrossingIntoHatred`/純安全`NetSecurity`/憎まれ`IsHated`＝恐れられるのは安全だが憎まれてはならない。`TerrorRules`/`MartialLawRules`とは別)／直言と佞臣(`AdvisorCandorRules`・MKV-3 #1141＝情報品質`InformationQuality`(直言vs追従)/佞臣圧力`FlatteryPressure`/政策の乖離`PolicyRealityGap`(裸の王様)/真実の到達`TruthReachingRuler`/直言の涵養`CandorCultivation`/イエスマンの連鎖`YesManCascade`/意思決定の質`DecisionQuality`/エコーチェンバー`IsEchoChamber`＝佞臣に囲まれると情報品質が落ち政策が現実から乖離する。`CounselRules`/`CourtFavorRules`とは別)／力量と運命(`VirtuFortunaRules`・MKV-4 #1142＝結果修正子`OutcomeModifier`(運命の半分＋力量の半分)/備えの緩和`FortunePreparedness`(堤防)/好機をつかむ`SeizeOpportunity`(roll)/運命の振れ`FortuneVolatility`/時代への適応`AdaptationToTimes`/果断vs慎重`BoldnessVsCaution`/逆境の回復力`ResilienceToMisfortune`/運命制御`IsFortuneMastered`＝運命は人事の半分を支配するが残りは力量で備えと果断が運命をねじ伏せる。`GrowthRules`/`SenescenceRules`とは別)／作戦摩擦(`FrictionRules`・クラウゼヴィッツCLZ-1 #1133＝摩擦`FrictionLevel`(命令深度×補給×士気)/実行成功`ExecutionSuccess`(計画が摩擦で目減り)/計画の陳腐化`PlanDegradation`/遅延の累積`CompoundingDelays`/戦場の霧`FogOfWarPenalty`/摩擦の緩和`FrictionMitigation`(経験×単純さ)/士気への影響`MoraleUnderFriction`/停滞`IsOperationBoggedDown`＝戦争では単純なことが摩擦で難しくなる。`OperationPlanRules`/`CommunicationsRules`とは別)／三位一体(`TrinitarianTensionRules`/`TrinityPillar`{政府/軍/国民}・CLZ-3 #1135＝均衡`TrinityBalance`(最弱要素が効く)/最弱の柱`WeakestPillar`/戦争遂行能力`WarSustainability`/政軍の乖離`PoliticalMilitaryGap`/民衆の厭戦`PopularWarWeariness`/崩壊検知`CollapseDetection`/情念と理性の緊張`PassionRationalityTension`/補強の必要`RebalancingNeed`＝戦争は政府の理性・軍の武勇・国民の情念の三位一体で均衡が崩れると遂行が破綻する。`FactionStateRules`/`WarPoliticsRules`とは別)／重心(`CenterOfGravityRules`/`CoGType`{主力艦隊/首都星系/同盟関係/民衆支持/補給拠点}・CLZ-4 #1136＝重心の重み`GravityWeight`/重心判定`IsCenterOfGravity`/崩壊の波及`CollapseImpact`/攻撃優先度`AttackPriority`(重要×脆弱×到達可能)/決定的弱点`CriticalVulnerability`/間接アプローチ`IndirectApproach`/戦力集中`ConcentrationOfForce`/自軍重心の防護`ProtectOwnCoG`＝重心は敵の力が集中する一点でそこを叩けば全体が崩れる。`ChokepointValueRules`/`LogisticsRules`とは別)。**いずれも盤面/UI未配線＝純ロジックのみ。Game層型不参照、TestHar/EditModeで担保**。全2881テスト。
- シーン/UI/MonoBehaviour 挙動はエディタ Play で目視検証（テスト対象外）。新規の純ロジックを足したら EditMode テストを併記する。
- **Unity 無し環境での検証＝`TestHarness/`**（リポジトリ直下・Assets外）：純ロジック＋EditModeテストを Unity スタブで `dotnet test` 実行（1020テスト・詳細は `TestHarness/README.md`）。新規 MonoBehaviour は csproj の Exclude へ、`Formation`/`ShipClass` enum を変えたら `Stubs/GineiShims.cs` も同期。クラウドセッションでは毎回ここで回帰確認する。

## 運用
- 新コンポーネントを作ったら、この「既存コンポーネント」表に1行追記する。
- 変更は Git でコミットしながら進める（権限「Don't Ask」運用のため、レビュー・巻き戻し前提）。
- 詳細な機能ロードマップは別ファイルの「機能追加ロードマップ」を参照（必要時に手動で読み込ませる）。
