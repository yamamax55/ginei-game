# 仕様1 fix2：ChatGPTレビュー・Unity実機検証

対象：Issue #2253、fleet-formation-hold-20260910-fix2 revision 3（編集停止）。2026-09-10実施。

## 判定

今回の主要動作は合格。先行のPlayMode 3失敗は修正後に解消した。仕様1全条件の最終完了とは区別し、未確認の組合せを下記に残す。仕様2は未着手。Unity Editor 6000.6.0f1のGame画面で検証し、終了時はPlay OFF。独立したWindowsビルドの検証ではない。

## レビューと自動試験

- 初版レビューで指摘した総退却中の受付、選択軍団の一括指定範囲、明示指定の後勝ち、実経路試験をfix1で確認。
- fix2の3失敗対策は、軍団総退却発令を待つ、敗走回復を抑えて入力条件を維持する、実際の支援判断に必要なゲーム時計を進めるという試験前提の修正。製品コードを変更して期待値に合わせた修正ではない。
- ChatGPTがUnity Test RunnerでFleetFormationHoldPlayModeTestsを一括実行：14/14合格、7.0847673秒。
- 前回失敗した総退却・敗走・支援要請の3件も個別に各1/1合格。一括とは別実行。
- Claude報告のTestHarness 9,676件合格は別の結果であり、ChatGPTのUnity試験件数に合算しない。

証跡：fleet-formation-hold-fix2-playmode-all-20260910.xml、fleet-formation-hold-fix2-single-retreat-20260910.xml、fleet-formation-hold-fix2-single-rout-20260910.xml、fleet-formation-hold-fix2-single-support-20260910.xml（すべて本フォルダー）。

## 会戦画面の確認

| 操作・条件 | 観測結果 |
|---|---|
| 自軍艦隊へ円陣を直接指定し、移動・交戦 | HUD「円陣（保持・直接命令）」が継続 |
| 選択した自軍団の配下艦隊を方陣に一括指定 | 自軍団2/2通知と自軍2艦隊の保持を確認。他軍団は鶴翼陣のまま |
| 軍団隊形を横陣へ変更、その後「軍団指定を解除」 | 艦隊の方陣保持は残る。軍団内配置と艦隊陣形の解除は独立 |
| 陣形メニュー「陣形の保持を解除」 | QA自軍1に解除通知。HUDから「保持」が消える |
| 敵から距離105の自軍2に直接移動＋保持、士気0を誘発 | 「保持解除（敗走）」通知。移動指定を維持して座標が進み、後に指定地点付近へ到達 |
| 自軍団2隊の残存比を0.30へ低下、総退却閾値0.50、指揮官士気50 | 総退却発令と「保持解除（総退却）」通知。自軍1はAI撤退で後退。自軍2の直接移動は既存仕様どおり継続 |
| 総退却発令済みの直接移動艦に円陣を再指定 | 「撤退中のため陣形を変更できません」で拒否。保持は再設定されない。ポイント不変はPlayModeで確認し、会戦HUDでは数値未照合 |
| 協力的な他軍団のQA他軍1へ円陣変更を要請 | 「指揮系統外1隊は支援要請」「陣形変更を要請しました」を確認。再開・交戦後もHUD「円陣（保持・支援要請）」。承諾通知の瞬間は新着通知に流れて画面上では未捕捉 |

## 緊急中断の座標記録と解釈

敗走：fleet-formation-hold-fix2-rout-runtime-20260910.txt。
直接移動の初期座標(-52.5,-15.0)、行き先(-83.3,31.4)。10秒後(-61.0,-2.2)、同じ行き先・直接命令・移動中を維持。ただし非交戦で士気回復が直ちに始まり、最初の進行フレームで敗走状態が解けた。これは敗走による保持解除とその後の移動を確認した結果であり、持続的な敗走中の長時間移動を実機で確認した結果ではない。持続敗走条件はPlayModeで検証した。

総退却：fleet-formation-hold-fix2-retreat-runtime-20260910.txt。
自軍1は(-48.1,-43.0)から(-64.6,-65.7)へ進みAI=撤退。自軍2は(-82.9,30.7)から(-83.2,5.4)へ進み、直接命令の行き先(-83.3,-15.5)を維持。記録見出し「撤退状態=観測なし」は主対象の自軍2だけの集計であり、軍団総退却が起きなかった意味ではない。全艦隊の強制撤退を合格扱いしない。

## 未確認・継続事項

- 倍速、攻撃命令終了との組合せ、所属変更、会戦終了時の保持解除の画面上の通し確認。所属変更・移動到着・AI復帰はPlayMode合格と区別する。
- 自然戦闘で敗走が持続する条件の通し確認、支援承諾通知の画面捕捉、ポイント前後の実機数値照合。
- 会戦途中保存自体が未対応のため、保持状態の保存復元は未実装。
- 既知の入力取りこぼしが継続。短いボタン内ドラッグで停止・再開できたが、通常クリック・キーが反応しない場面がある。
- Play終了時の未解放オブジェクト警告が継続。今回の陣形保持の失敗と断定しない。
- 保持解除直後もHUDに旧指定元「直接命令」が残り、自動復帰が少し分かりにくい。表示改善候補。費用明細や形成完了表示は仕様2/3の範囲。

既存セーブSHA256：E850E1F3F11290EEB7E4620FFE80455CA6AF7C5BD9BBF4E5D67A34858DF71367（検証前後一致）。ChatGPTによる製品コード変更、commit、pushなし。Issue #2253は他のAI調整と未確認事項があるためOPENを維持する。
