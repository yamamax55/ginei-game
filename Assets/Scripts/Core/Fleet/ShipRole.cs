namespace Ginei
{
    /// <summary>
    /// 部隊の役割（#128・戦闘 vs 非戦闘）。#80 の艦種（戦艦/巡航/駆逐＝戦闘艦の中の種別）とは直交する別軸。
    /// 非戦闘艦は攻撃せず交戦を避け、勝敗カウントから外れる（撃沈はされうる＝護衛/通商破壊が成立）。
    /// 固有機能は各専用Issueへ：偵察=探索#119／入植=#117/#109／輸送=Convoy #94。拡張可。
    /// </summary>
    public enum ShipRole
    {
        戦闘艦, // Combatant（既定＝従来動作）
        偵察艦, // Scout（探索・フォグ開示 #119）
        入植艦, // Colony（新規植民 #117/#109）
        輸送艦  // Transport（補給輸送 Convoy #94）
    }
}
