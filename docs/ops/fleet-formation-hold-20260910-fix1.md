# 陣形保持 初版レビュー差戻し
Task: fleet-formation-hold-20260910-fix1
Issue: https://github.com/yamamax55/ginei-game/issues/2253
初版revision2編集停止を確認しコードレビュー。Unity Play OFFのまま。以下を修正し再報告してください。仕様2には進まない。

1. 撤退中の陣形受付判定漏れ（必須）。Squadron.RequestFormation は FleetStrength.IsRetreating または士気敗走だけを参照する。一方軍団総退却は FleetAI.currentState=撤退を設定し、FleetStrength.BeginRetreat は戦場端で呼ばれる。士気正常で退却移動中は新しい手動陣形を受理でき、次の軍団周期で解除するちらつき/費用消費が発生し得る。実際の退却状態と軍団総退却の発令状態を判定に反映すること。直接移動命令を維持する例外艦も総退却発令中の新規保持は拒否する。直接移動/攻撃自体は変更しない。
2. テストの実態を補強。DirectOrder_HoldsAcrossAiCycles は窓口を手動で10回呼ぶだけで実AIの複数周期を保証していない。Hold_SurvivesMovementCompletion は到着をassertせずAIも動作中で行き先を書き換え得る。実際の到着と直接命令終了を確認後に保持をassert。軍団総退却の実経路（士気正常・離脱前）と陣形再指定の拒否/消費ゼロ、軍団隊形との分離、支援要請受理経路も検証する。今回のPlayMode9件はChatGPT未実行で合格ではない。
3. 実機手順9/10の前提が不正確。支援移動中に敗走させるQAはSupportのみ対象で直接命令の対照にならない。他軍団の総退却QAも直接命令艦の対照には使えない。既存QAの前提に沿って手順を修正し、必要な場合だけ狭いQA補助を追加する。
4. 一括指定の対象明示。ChangeFormationForCorpsは選択軍団でなくActorChainの軍団（空なら直接命令可能全艦隊）を対象にしている。選択軍団の文脈のメニューから別の艦隊を変える危険がある。選択中の艦隊が属する対象軍団に限定し、その中で指揮権限を満たす対象だけ適用、対象なしなら理由表示。複数軍団は各選択軍団に限定。対象ごとの失敗理由も最低限通知する。

補足：Priority関数は直接4/支援3だがCanAcceptは同順位の後勝ち。未使用でも誤用を防ぐため同優先度とするか削除する。HUDの非保持を常に自律とする表示は軍団AI指示との区別がつかないので指定元表示の仕様に合わせる。
状態ファイルを段階が変わる時に更新してください（初版は約30分更新されず、画面確認が必要になった）。実装・テスト結果と未検証を区別し編集停止。既存セーブ/v5/無関係変更保護、commit/pushなし。
